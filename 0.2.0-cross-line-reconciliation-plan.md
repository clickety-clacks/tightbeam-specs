# 0.2.0 cross-line reconciliation plan — 0.1.x deltas since the split

Author: tb02 seat, 2026-08-17, at Mike's direction. Split point: cd72fe0b
(main forked 2026-08-13). Universe: 99 commits cd72fe0b..origin/0.1.9 plus
35 commits origin/0.1.9..origin/0.1.8 (through 60bda7e). Verdicts per
Mike's four buckets: OMIT / USE AS-IS / REFACTOR+APPLY / RETHINK (the 0.1
bandaid names a better 0.2 architecture). Cross-line landings remain
Mike-authorized cherry-pick elections; this plan is the election slate.

## A. OMIT — already on main (identical or same-day twin landings)

| 0.1 commits | What | Main twin |
|---|---|---|
| 03e45a0, 4cd995f, 378807e, 8f22fe8 | Credential-rollback incident both halves | 7c20ed1, 1c0bd76, 3e0c510, d3b6cb4 |
| 83f9b0b, 61d38e8 | Assignment file projection + advisory lists | ec6c727, 5313065 |
| c5c3dc0 | Adapter-death recovery despite cleanup failure | eea3e9a |
| 87eb387 (0.1.8 copy) | Spec homing location-neutral | a18d8b3 |

## B. OMIT — main has something better

| 0.1 commits | What | Why main wins |
|---|---|---|
| 835ebb3, cc32940, 662ca58, 84b0dae, 20d1d69 | 0.1 live session tuning / harness switch | Main's re-land (edcc0a9 + a5d38df chain) adds the stale_election precondition family (harness/host/basis axes) the 0.1 version lacks. AUDIT ITEM: diff 84b0dae's live-smoke redelivery semantics; fold any missing case into main's test suite only. |
| be61cfc | Wire message-type reconcile | Main Phase 1 re-legislated the message surface (classed rows, seams ①②). Verify-then-omit. |
| 98939d8 | Transactional notice publication plans | Main's digest/delivery chain (529b762..ba1bd88) supersedes. Verify-then-omit. |

## C. OMIT — line-specific machinery (never port)

Release/branch/CI/docs of the 0.1 line: af4c146, 2b8d9dd, 42ecc82, ce686fa,
d909a54, becb130, dd0e997, eda754c, e39fcde, fe7a9dc, 8ac4794, merge
commits, 5f9cddf, 87f24de, ab6304d. Main carries its own CONTRIBUTING
(83fb015, f6e5d08). Exception pulled out below: d18fe7b.

## D. USE AS-IS (cherry-pick election slate, in landing order)

1. **fa47a00 + b665614** — onboarding keeps the runtime serving; staged
   install replaces it after. The no-park fix (wi_e86ddd54). Main has the
   identical defect.
2. **0e539b0 + fe3160a + e75b8ce** — onboard emits sign-in URL + one-time
   code as first-class deliverables, with the fixture-backed tee proof
   (wi_0535922b; ledger 8287cd0's bone). Definition and mechanism ship
   together — fold the ruling sentence into main's docs in the same landing.
3. **7cc3646** — /version states version + build + sha. Builds-identify-
   bytes is law on both lines; S9-F1 exists on main too. NOTE: landing this
   on main obligates fixing main's version identity itself (still stamps
   0.1.7) — fold the 0.2.x version-strategy sitting into this election.
4. **acff16d chain (398d7ef, 0a0c790, 920dd79, 3449316, 8d97996, e4f4614)**
   — gh#9 rotation harvest on subscription 401, with its review closures.
   Line-agnostic store logic. Follow-up card: fixture-backed 401 e2e (the
   coverage gap is identical on both lines).
5. **0ebe326 + 1ea30f5** — decision-requests --status literal-all fix +
   dispatch-path test (rides cluster E1 if sequencing prefers).
6. **d18fe7b** — numeric resolution of the active 0.1 branch (shared
   tooling main's glob automation also wants). Small; take if it lives in
   shared scripts, else drop to C.

## E. REFACTOR AND APPLY (the content is right; the shape must be 0.2's)

1. **Operator decision-request lifecycle + router wiring** — 7cdeb44,
   b2b1cc3, 41452a3 (0.1 full surface) + 3fd9c97, 6fd5e2a (gh#11 router
   seam). Main has only seam ③ (create-path, 8b230b3). Port the operator
   verbs (ask/rule/withdraw) INTO the fabric frame: decision-requests are
   §7's typed input-needed carrier; the operator verbs must compose with
   class delivery and §8b promotion tiers (tier-1 auto-execute with notice
   etc.), and the schema lands under main's stamp regime with its own bump.
   The gh#11 wire-dead lesson becomes a conformance test: every verb the
   CLI sends and the gateway implements must appear in the router
   inventory (Sol's derive-the-inventory-from-source pattern, 2ceac8f,
   already exists on main — extend it to the agent-verb table).
2. **Wake-cancellation provenance + retirement retargeting** — e782b01,
   54c551d, d8eddfd, f54afe5, 6c79ea7, d8bd6ad (wi_0abae0fd family). This
   is Bug A's proven core. Land it under the relief PO's reviewed Bug A
   design vocabulary (sender-visible cancellation lifecycle), keeping the
   0.1 tests as the regression floor. Also answers gh#15 (retired session
   retry-looping) — check the fix covers it or card separately.
3. **PR20 GitHub host auth** — c57c190..60bda7e (16 commits: GithubAuth
   gateway brain, CLI split, file-backed banking, env projection, network-
   verb-narrowed hook, operations guard, lease-bound operator wake).
   The capability belongs in 0.2, but three seams re-home:
   (a) agent-env projection → the archetype manifest / provisioning
   election (§6 D3), not ad-hoc projection; (b) the operations guard →
   compiled dispatch-rule law (named refusals), not an in-code guard;
   (c) file-backed banking "where agents can read it" must be reconciled
   with credential-kinds-v1 doctrine (no tightbeam-owned copies) — design
   pass REQUIRED before port; the github token may deserve its own
   credential kind rather than the file bank. Port the brain + tests
   mostly as-is; rewrite the three seams.
4. **Harness-aware health incidents + auth-incident delivery + terminal
   outage evidence** — 708d9d1, 1416ef6, 44a2b04 (0.1.9). Right demand,
   0.1 shape. On 0.2 these are §5 bones vocabulary: health incidents as
   classed rows feeding the prodder/effort-checkin watermark and the
   declared health/forensics follow-up (heard-count observability). Map,
   don't port.

## F. RETHINK — the 0.1 bandaid names the better 0.2 architecture

1. **Execution observability family** — 5f43411, 23d6681, 41b4207,
   69a80cb, 9b62a7c, 54cf663, dc7d595, b0298a3, 53837b1 (durable command
   start evidence, detached-command evidence preservation, receipt poison
   matrix, terminal-status validation, schema bootstrap). This is 0.1
   instrumenting the orphaned-process disease (Bug B) with evidence-only
   observability, because a maintenance line cannot re-architect. 0.2 CAN:
   the terminal-child custody/reactivation spec (in flight, rev'd) makes
   the process a ROW with an owner and a repair verb — observability
   becomes a projection of custody instead of a parallel evidence system.
   DO NOT PORT. Mine instead: the receipt-poison matrix and detached-
   evidence cases become the custody spec's acceptance fixtures; the
   command-execution schema registration informs the custody row shape.
2. **Host toolchain PATH registry** — 5ae9f10. Bandaid for gh#8's disease
   (kungfu hardcoding dev-org hosts). 0.2's answer is host facts in
   archetype/provisioning manifests (assimilate-time declaration), not a
   registry bolted beside them. Fold the requirement into the provisioning
   election design; omit the code.
3. **assign/dispatch refuse role with no live holder** — 29cd808. Correct
   instinct, pre-office addressing. Under r5 the office pattern re-defines
   role address (role binds to desk; unstaffed falls back per existing
   rule; delegation card is the obligation seam). Re-derive the refusal
   against desk-graph addressing — a no-live-holder refusal may become a
   no-desk-and-no-fallback refusal with a different name. Design note into
   the office build cards, not a port.

## Sequencing & mechanics

Wave 1 (small, independent, this week): D1, D2, D3, D5 — each lands with
gates + ledger entries per the no-door process; one stamp coordination pass
because D2/D3 touch no schema but E1 will.
Wave 2: D4 (rotation), then E2 (Bug A vocabulary now settled).
Wave 3: E1 (operator verbs — schema bump), then E3 (PR20 after its design
pass), E4 mapping.
F items produce spec deltas and fixtures, never direct ports; they ride
the existing custody/provisioning card lanes.

Verification debts folded into the waves: the B-cluster verify-then-omit
audits (84b0dae smoke semantics, be61cfc vs Phase 1 surface, 98939d8 vs
digest chain) are one reviewer card, evidence attached to this plan.

# 0.2 build ledger

Status: LIVING RECORD. One entry per landing on main (code repo,
clickety-clacks/tightbeam). §8b law: no door — review evidence lands here
BEFORE the push; gates baseline+after recorded; CI-on-main-push is the
tripwire. Author/reviewer elections per program §8.

## Phase 0 — completion-selection repair (LANDED ba91e61)

- **Branch:** `p0/completion-selection-repair` (base origin/main b3d47e2).
- **Origination:** Opus 5, 2026-08-13, commit 4d42eeb. Defect pinned
  empirically before fixing (probe on unmodified main): round selection
  ordered by lifecycle recency over ALL linked review cards regardless of
  verdict presence — a verdictless newer card erased a standing holder
  verdict, and `attest`'s refusal on closed cards made it a permanent
  wedge (the wi_1b0237fe class). Fix: selection anchored on
  verdict-carrying rounds; new routed `reopen-assignment` verb
  (named refusals, close history recorded in `assignment_reopenings`
  same-transaction, mandatory reason); conformance fixture
  holder-verdict-wins; CLI surface. Gates: baseline 1490 Elixir /
  219 Rust, after 1496 / 220, zero failures both, format clean.
- **Cross-review round 1:** Sol xhigh (gpt-5.6-sol), 2026-08-13, diff
  embedded (gibson codex sandbox cannot read fs). **Verdict:
  changes-requested** — 4 blocking (selection still lifecycle-ordered
  among verdict-carrying rounds, so a reopened older round's fresh
  verdict loses; Rules.decide seam not visibly wired on the new verb;
  holder lacks an independent repair path — opener-or-admin only;
  conformance runner never exercises the verb), 3 important
  (grapheme-vs-codepoint length mismatch can bypass the named refusal;
  reopening history not agent-readable; refusal tests don't pin
  non-mutation), plus one policy boundary to pin by test (independent
  clean verdict revives past a newer self-held verdictless card —
  intended). Full text: session record; failure scenarios all concrete.
- **Findings application:** Claude Sonnet (fresh eyes), commits 0500bbf +
  dcfa092 + 6784417 — all seven findings applied (verdict-recency
  ordering in round selection; Rules.decide wiring + rule-inhibition
  test; opener-or-HOLDER-or-admin authority; named conformance-runner
  clause exercising reopen-assignment incl. the two-round displacement
  shape; code-point length validation; reopenings surfaced in
  assignment-get + enriched marker; non-mutation refusal tests +
  surrendered/revoked/rollback coverage) plus the Minimality-boundary
  policy test. GATES GREEN on gibson (toolchain via mise — the "no mix on
  gibson" map was stale): Elixir 1505 tests 0 failures (baseline 1496),
  Rust 221 passed (baseline 220), format clean both halves.
  Note: the origination-round eezo baseline (1490/219 at origin/main)
  and these counts are consistent: +6 origination, +9 fix round, +1/+1
  Rust. Worktree relocated /tmp → ~/src/tightbeam-p0 (Mike's correction:
  program worktrees never in /tmp).
  STAFFING DEVIATION recorded: program §8 assigns findings-application to
  Sol medium, but codex cannot write files on gibson (bwrap sandbox
  blocked host-wide, wi_424308f6 class) — a Claude agent distinct from
  the Opus originator applies, and Sol xhigh re-reviews the delta, so
  fresh-eyes and cross-vendor both survive at the seams that matter.
  Overridable if Mike wants a different shape.
- **Cross-review round 2 (delta):** Sol high, 2026-08-13. All round-1
  findings ADDRESSED (incl. accepting the contested finding-2 evidence:
  Rules.decide fires at the Dispatch chokepoint for every routed verb);
  assertions strengthened, no weakening, no authority drift. ONE new
  blocking finding, exposed by the reopen verb itself: verdict KIND still
  derived by pre-existing severity/existence logic over the selected
  card's whole verdict set instead of bound to the latest holder-verdict
  row — a reopened card's newer changes-requested could lose to its own
  older reviewed-clean. Fix prescribed (select the latest holder-verdict
  row globally, carry its verdictKind; reverse-direction regression
  test). Round-3 fix in flight.
- **Fix round 3:** Claude (same maintenance agent), commit bf4bbd2 —
  single-source-of-truth refactor as prescribed (candidate round JOINs the
  exact attests row its ordering came from; latestVerdictKind read off
  that row) + reverse-direction regression test. Honest probe note: the
  round-2 scenario was already denied by the old code (both subqueries
  used identical ordering) — the defect was latent divergence-by-refactor,
  not a live bug; fixed anyway. Gates: 1506 Elixir tests 0 failures
  (+10 total, all added tests), Rust unchanged (no cli/ diff).
- **Cross-review round 3:** Sol high, 2026-08-13 — **reviewed-clean**.
  Prescription implemented correctly, verdictless-round exclusion and
  independence guard preserved, no new defects.
- **Landing:** merged to main and pushed directly per §8b, gates green,
  CI-on-push tripwire live. LANDED: merge commit ba91e61 (b3d47e2..ba91e61), 2026-08-13.

Dirt reported by the origination round (not accommodated, for Mike):
- eezo's live checkout `~/src/tightbeam_ex` local main has DIVERGED
  unpushed work — f60239c "Fix latest review round tie ordering" —
  touching the exact selection machinery Phase 0 repairs, plus dirty
  ci.yml/README/test files and an untracked release script. 0.1.x org's
  tree; observed only. Cherry-pick-election review should look at f60239c
  when the 0.1.x org pushes it.
- Empty `door test` commit c59e848 sits on main (Mike's record of the
  prop-lock discovery; his to keep or drop).

## Phase 1 — substrate seams ②①③④ (in progress)

- **Entry gates satisfied 2026-08-13:** r4 READY + Phase 0 merged (ba91e61).
- **Recorded ruling (integrator): live feature-smoke deferral.** Fabric §13
  Phase 1 exit wants live feature-smoke green on both harnesses,
  deferrable "only by recorded ruling if no live gateway exists on the 0.2
  line" — the only gateway is 0.1.x's and it is deliberately PAUSED
  (program §8d, token preservation). Smoke runs when a 0.2-line gateway
  first stands up (Phase 3 entry at latest); the deferral closes then,
  and Phase 3 entry re-checks it.
- **Origination wave 1 (②①):** Opus 5, branch `p1/fabric-seams`, tip
  cafe321 — SHIPPED complete: ② delivery-policy in wakes.ex (per-
  (target,class) digest groups; batcher creates the wake at min(turn
  boundary, ceiling); members consumed as `superseded` with
  replacementWakeId = digest — no new noun, Law 2 audit is one join;
  all-or-nothing materialization; signed provenance incl.
  batcher-inhibited rows), ① wake --class (five seed classes, no CHECK —
  unknown class → fyi + named skew row; class+deliveryRule in transcript
  and toplines; `coordination-share` read = acceptance-№1 metric; shape
  stamp bumped, refuse-by-name) + operating-manual amendment (classes,
  coordination-share, reopen-assignment — gap-audit item 3 closed).
  Gates: baseline 1506/221 → after 1525/222, 0 failures, format clean.
  Scale-check exercised per card: ③④ NOT in this branch — ③ needs a
  third decision_requests CHECK arm (a seam's work, adjudication
  vocabulary must not be borrowed), ④'s toplines half needs true paging
  (ExecutionMap.roster filters in memory; SQL LIMIT pages wrong).
  Six dirt items reported (algedonic human-channel half has no carrier
  on this line; status-query no-desk cell is a routing instruction a
  timing seam can't honor; wi_1100e078 producer re-timing deferred as
  its own increment; held classed wakes count in self_pending_count —
  flagged to reviewer; conformance atomize spelling quirk; a clock race
  in its own first-draft proofs, fixed before push).
- **Cross-review wave 1:** Sol xhigh on ba91e61..cafe321 — verdict
  **changes-requested**: 5 blocking (inhibition branch captures scheduled
  algedonic wakes; member-absorption TOCTOU — a wake filed after the
  trigger is absorbed without its own boundary/ceiling; turn_boundary
  requires busy==0, turning "next boundary" into "next idle" — an
  adjudication smell; coordination-share numerator wrongly excludes
  unclassed wake turns, corrupting the before-window; digest_members
  audit join not provenance-total), 4 important (due_reason double-eval
  → empty trigger signature; carrier falsely claims sender election;
  held classed wakes suppress supervision fallback — Sol ruled the
  flagged item a real defect; dangerous-path tests missing). Transaction
  layout itself judged sound.
- **Fix round 1 (wave 1):** Claude fresh-eyes, commit 0d98c70 — all nine
  applied, none contested; integrator interpretation on finding 1
  (algedonic never held BY POLICY; sender's own --after stays the
  sender's election, both pinned). The existing suite caught the fixer's
  own first attempt at finding 8 (three-valued NULL excluded legitimate
  unclassed self-wakes — fixed with IS). Gates 1534/0 (+9 dangerous-path
  tests).
- **Cross-review round 2 (wave 1):** Sol high — all round-1 findings
  ADDRESSED; candidate/per-member split judged coherent. FOUR new
  findings: 2 blocking (boundary >= created_at absorbs a same-millisecond
  wake into the prior boundary's carrier — must be strict >; the new
  classElection CHECK value never reaches an existing table — SQLite
  CHECKs bake at CREATE, needs the repo-lawful shape/rebuild treatment +
  upgrade test), 2 important (dangling wakeId counts as coordination
  traffic; algedonic assertions accept prefixed rules). Fix round 2 in
  flight.
- **Origination wave 2 (③④):** Opus 5, branch `p1/seams-3-4` off
  cafe321, tip f65c996 — SHIPPED: ③ complete (third CHECK arm kind='agent'
  pinning every gate-usable column NULL; ask/answer routed verbs; outbox
  row+notification atomic, class input-needed composing with seam ②;
  proof-10 enrolled — the proof caught the new site first; tripwire held
  three ways incl. a conformance case proving an open question gates no
  completion and an AST literal-scan; no deadline sweep — a sweep on an
  open question IS the tripwire). ④ honest split: attests true (ts,id)
  keyset with same-ms conformance walk + uniform cursor_not_found;
  toplines paged on immutable (createdAt,id) order key, response bounded
  / work honestly NOT bounded (named in moduledoc+help), --tree refuses
  a cursor by name, no default limits. Shape stamp renamed phase-wide:
  coordination-fabric-v1-phase1, refusal sentence names the SQLite
  baked-CHECK hazard. Gates: 1538 Elixir / 223 Rust, 0 failures.
  Five dirt items reported (ParkRaceDB string-adjacency proxy fixed to
  shape-match; Escalation.list dead :admin option; validate_options!
  adjudication vocabulary; agent questions now visible in toplines
  open_decision_requests — deliberate, not a gate; adjudication_episodes
  remnant).
- **Cross-review wave 2:** Sol xhigh on cafe321..f65c996 — in flight.
- **Integration note:** both waves touch schema.ex shape stamps (wave-1
  fix round: classElection CHECK; wave-2: phase-wide stamp rename).
  Landing order: wave 1 → wave 2 rebased onto it → gates on the merge —
  integrator's job.
- CI tripwire on Phase 0 landing: run 31686280497 in progress at entry.

### Wave 1 close-out (2026-08-13)

- **Fix round 2:** commit 89f6a96 — all four round-2 findings applied:
  strict `>` boundary comparison with deterministic timestamp injection
  (sleeps removed); shape stamp bumped to coordination-fabric-classes-v2
  with an upgrade test proving refusal-by-name against cafe321's
  byte-exact DDL (no migration, stale CHECK untouched); `w.wakeId IS NOT
  NULL` (NULL-safe `IS NOT` verified in sqlite3); exact-equality
  algedonic assertions + due_at pinned. Gates 1537 Elixir / 0 failures.
- **Cross-review round 3:** Sol high — **reviewed-clean**: all four
  correctly implemented, no new defects, no weakened assertions.
- **LANDED on main** (merge sha in git log; direct push per §8b; CI
  tripwire on push). CI on Phase 0's earlier landing: SUCCESS
  (run 31686280497).

### Wave 2 review round 1 (2026-08-13)

Sol xhigh on cafe321..f65c996 — **changes-requested: 6 important, 2 nits,
ZERO blocking, tripwire explicitly NOT tripped** ("no current
open-agent-to-gate consumer in the production hunks"); CHECK fence,
attest keyset, outbox atomicity, proof-10 enrolment, --tree refusals all
judged sound. Findings: agent rows still stamped deadlineAt (adjudication
deadline semantics — must pin NULL); tripwire proof too narrow
(single-literal scan misses assembled SQL, allowlists escalation.ex —
needs production-read-site enumeration + per-family behavioral proofs);
ownerUserId visibility branch leaks to sibling sessions of the asker's
owner; answer/rule reveal existence+kind before authority (needs uniform
refusal); --about is an unscoped assignment existence oracle; empty
toplines page returns boolean false cursor; filtered-out-cursor test
doesn't filter its cursor; "byte-identical unpaged" claim false (new keys
leak into unpaged responses). Fix round in flight (fresh-eyes Claude,
same recorded staffing shape).

### Wave 2 fix round (2026-08-13)

Commit 2a3b24a — all eight applied, none contested. Highlights: deadlineAt
de-stamped from agent rows (nullable; arms partition it); tripwire proof
rebuilt as file-level enumeration + query-level kind-predicate scan — and
it immediately caught a REAL pre-existing generic reader
(execution_map open_requests counting agent questions in telemetry; the
drift class the test was built for, found on first run); visibility
narrowed to the true owner-user principal; answer/rule refusals collapsed
uniform; --about scoped (and assignment_get_result's own unscopedness
reported as dirt); nil cursor; honest unpaged shape (conformance fixture
was itself asserting the wrong shape — fixed). Gates 1543/0 (+5).
Confirmation round in flight.

### Wave 2 confirmation round (2026-08-13)

Sol high — seven of eight addressed cleanly, no weakened assertions;
**changes-requested solely on the tripwire enumeration** (file-level
inventory + exact-text matching still evadable; prescribed durable fix:
centralize all decision_requests reads behind kind-classified helpers
with a pinned helper inventory). Plus the deployment caveat: deadlineAt
DDL changed under an unchanged stamp name — the same stamp-vs-DDL class
wave 1 already fixed; same treatment ordered (final stamp bump + upgrade
refusal test), coordinated with the coming rebase so ONE final stamp
covers the whole phase. Fix round 2 in flight.

### Wave 2 fix round 2 (2026-08-13)

Commit c555dda — both items applied: (1) full read-centralization —
every decision_requests read/write outside escalation.ex migrated to
named kind-classified helpers (execution_map, supervision park sweep,
effort_checkin's whole effort arm, job_trace incl. a text-only UNION
fragment composer, wakes, dispatch); tripwire = zero out-of-file literals
+ exact pinned helper inventory; proof 10 preserved PROPERLY mid-refactor
(effort notification arm moved with the row-writes — proof relocated,
not weakened). (2) Stamp settled: coordination-fabric-v1-phase1-v2 with
byte-exact f65c996-vintage refusal test; offered as the ONE final stamp
for the merged tree (never named any earlier vintage of either branch).
Gates 1545/0.

INCIDENT (self-reported by the fix agent, no harm, recorded for Mike): a
`mix run` sanity check booted Tightbeam.Application against the real
~/.tightbeam/state.db — a violation of the no-gateway-starts rule. The
new shape-refusal machinery crashed the boot cleanly BEFORE any DDL or
write touched the 0.1.x database (state.db mtime unchanged; empty
-wal/-shm sidecars removed). The report-dirt law protected the live
org's data exactly as designed. Agent corrected its procedure.
Confirmation round 3 in flight.

### Wave 2 round 3 (2026-08-13)

Sol high — proof-10 relocation confirmed SOUND (row+notification
transactional atomicity proven, not assumed); centralization materially
improved; **changes-requested on two**: the pinned inventory is
self-declared (map==map; an "any" helper can gain literal-free external
callers), and the offered final stamp already named the pre-merge shape
— reuse on the merged tree would repeat the defect it prevents. Fix
round 3 = derived inventory (source-enumerated helper set, per-helper
query-scope assertions, pinned callers of "any" helpers) + THE REBASE
onto eeb5be4 with a virgin final stamp born in the commit carrying both
DDL sets, refusal tests for both parent vintages. In flight.

### Wave 2 round 4 (2026-08-13)

Sol high on the two unseen commits (derived inventory 2ceac8f; rebase +
final stamp 4883d92) — **stamp settlement CONFIRMED SOUND and closed**
(virgin coordination-fabric-v1-phase1-v3; all four vintage refusal tests
verified real); production code: no findings. Changes-requested solely
on the lint test's evasion-resistance (exclusive-predicate proof not
literal presence; arity-aware AST call analysis vs textual regex; three
named holes: widened IN-list, assembled table names / overloads,
capture/apply/alias callers). Hardening round dispatched — DECLARED THE
FINAL ROUND on this test's adversarial bar by integrator ruling: if a
further round raises new meta-requirements on the same test (rather than
defects), the integrator lands with the residual recorded, Mike may
overrule. Also: third program-doc stale-map clobber repaired (224019c);
org-resume content grafted (0.1.x LIVE again; standing watches re-armed:
bin-1 sweep, f60239c, wi_95805093 recon election).

### Wave 2 close-out — PHASE 1 COMPLETE (2026-08-13)

Rounds 5-7 hardened the tripwire lint to adversarial standard (per-literal
exclusive-predicate parsing with unconditional OR rejection; arity-aware
AST inventories resolving concatenations/attributes; caller analysis over
captures/apply/aliases with absolute-path handling; hash-bound pins
carrying safety witnesses). Round-7's final one-conditional fix (absolute
Elixir.-prefix bypasses alias expansion) applied VERBATIM by the
integrator, 25f61cc — same recorded play as the r4 fold fix round; tooth
proves both directions. Gates 1574 Elixir / 0 failures, 227 Rust.
Production code carried no findings from round 4 onward; the
anti-adjudication tripwire never tripped in any round.
**Wave 2 LANDED: merge 8b230b3 (fde9715..8b230b3).** Phase 1 exit
evidence: conformance fixtures per seam aboard; classes visible in
transcript+toplines; live feature-smoke deferred by the recorded ruling
(closes at first 0.2-line gateway, Phase 3 entry re-checks).

## Phase 2 — neutral-seed anatomy (in progress)

- **Entry:** Phase 1 shipped (8b230b3). Zero substrate changes ruled;
  commit 2 (seed-path registration in identity.ex + test enumerations)
  EXCEEDS that ruling and is separated for integrator disposition —
  without it the new seed paths are claimable by any kungfu bundle and
  CI is red, so keeping it is the honest call (recorded here).
- **Authorship:** Fable, branch `p2/anatomy-seed` (484a459 docs +
  9323269 accompaniment): exec archetype (seed archetypes slot, include
  convention → individually reshapeable via identity edit), office
  convention (§4 dissolution + degraded windows faithful), delegation
  template (§6 verb lists VERBATIM; filing scoping), directive
  vocabulary (five keys, latest-wins, off-cancellation, skeletal floors
  ungateable), manual amendments 1+2 (surrender example no longer
  teaches the fumble). Gates: assemble.sh green; full suite 1574/0
  baseline AND after; format clean.
- **PHASE 3 BLOCKERS surfaced by authorship recon (report-dirt law):**
  (1) NO `directive` attest kind exists — attest kinds are
  progress|completion|surrender|verdict (assignments.ex:2170), lifecycle
  holder-only (:1616); fabric §6's "v1, zero new rows" is not yet true
  of this tree; Phase 3's "file the first directives" is BLOCKED until
  that seam ships (a small Phase-1-class substrate increment).
  (2) `role-bind` is a wire verb with NO CLI surface — the office
  rebind is unreachable via the sanctioned agent path (Q3-class gap).
  Both are named follow-on cards for the Phase 3 entry, not smuggled
  into Phase 2.
- **Cross-review:** Sol high — in flight.

### Phase 2 close-out (2026-08-13)

Sol review: one high finding — the office-convention doc (inheriting the
defect from fabric §4 itself) implied card revocation changes role
binding; revoke-first left the role routing to an off-card desk. Fixed
beyond the prescription: ordering INVERTED to rebind-then-revoke in doc
AND spec (r4.1), eliminating the dangerous direction. Sol micro-review
confirmed the inversion but rejected "benign" for the crash window
(authority-overlap gap: an on-card role-less desk may act on
already-delivered work concurrently). Resolved via Sol's own offered
narrow-the-claim option, applied verbatim-in-substance (81ee759 + spec
r4.2 9f6330f): the window is named a bounded, governed dual-authority
window — duplicated-delivery worst case (interruption, never truth, the
error asymmetry), bounded by the watermark (leftover card = open
obligation, 60-min floor) + effort check-in, ended by the exec
stand-down rule now in the seed guidance (role gone + card standing =
stop delivering, surrender). Everything else reviewed-clean on first
pass: verb lists verbatim, directive semantics exact, manual amendments
faithful, seed-path registration exact and minimal, no invented syntax,
no adjudication smell. assemble.sh green throughout; full suite 1574/0
baseline and after (commit 2 touches lib/ enumerations only).
**Phase 2 LANDED on main** (merge sha in git log; §8b direct push).
Commit 2 (seed-path registration) KEPT by integrator disposition — 
without it the seed paths are claimable and CI is red; the docs-only
ruling deviation is recorded, overridable.

## Completeness fix wave (in progress)

- **Entry:** dual-vendor completeness scan (completeness-scan-2026-08-13.md)
  — both scans NOT-complete; 8 code items + 2 coverage items dispositioned
  to this wave; 3 spec items folded as r4.3 (2c799ce).
- **Application:** Claude fresh-eyes, branch fix/completeness-wave,
  commits e8228e1..5017c7b — all ten applied, none contested. Highlights:
  retarget now carries every sender fact (class/election/digest/summon)
  with delivery policy honestly re-derived for the new target; digest
  groups keyed (targetRole OR session, class) so role carriers re-resolve
  at delivery (closes the spec-§4 contradiction); per-group rescue with a
  named failure row + carrier work-inheritance and a named
  batcher-scoped exemption closes the latent tick-loop crash; the fixer
  SELF-FOUND a second-order gap (carriers with inherited assignmentId
  could satisfy supervision's prod recognition — guarded). Cursor errors
  unified; digest-members got its read verb; skew rows name the true
  rule; ceilings honest; wording fixed both places; Phase 2 docs now
  content-pinned; transcript/toplines projections tested with real
  values. Gates: baseline 1574/227 → after 1589/228, 0 failures, format
  clean.
- **Cross-review:** Sol xhigh, direct-tree mode (codex exec
  danger-full-access — first full review without embedding) — in flight.
- **Cross-review round 1:** Sol xhigh (direct-tree) — changes-requested:
  1 HIGH (retarget RESTARTS the class ceiling — created_at + ceiling_ms
  on the retargeted row extends an fyi to up to 2× its ceiling, Invariant
  3 violation; preserve-or-tighten ordered), 3 MED (digest-members'
  carrier id undiscoverable through any sanctioned surface — verb
  unusable as shipped; the O4 failure row unsigned, §8; orphaned-role
  carriers permanently not_found in the audit verb). Checked clean:
  double-consume impossible, exemption unforgeable, Law 3 holds for
  poisoned groups, the recognition guard correct, O3/O6/O7/O8 + S4/S5
  sound. Fix round in flight.
- **Fix rounds 2-4:** a06f2eb..ba1bd88 — round 2 closed all four
  (retarget preserves due_at verbatim: retargeting changes WHO, never
  WHEN — Sol confirmed correct across digest/immediate/scheduled/
  conditional; digest signs its own wake id; failure rows sign their
  rule; role-carrier audit); round 3 caught the round-2 authority fix
  binding audit to a LIVE role row (deletable/reusable — late-bind the
  future, pin the past) → authority now pinned at materialization in the
  carrier's own transaction; round 4's last HIGH was a lossy pin
  encoding (\S+ readback truncates whitespace-bearing ids — prefix-user
  authority transfer) → URI-encoded per the codebase's own convention,
  collision regression aboard. Round-4 fix is prescription-verbatim
  (recorded precedent). Gates: 1594 Elixir / 0 failures, 228 Rust.
- **LANDED on main** (merge sha in git log; §8b direct push; CI
  tripwire on push). Completeness verdict now COMPLETE modulo the
  recorded deltas + r4.3-scheduled items.

## Fabric r5 fold — CR-001 + CR-002 executed (2026-08-13, spec commons only)

- **Entry:** both CRs AUTHORIZED (Mike, 2026-08-13); executed in ONE
  sitting after the Phase 1 landing, per their terms. Executor: the
  folding authority's spec-writing hand. No code-repo changes in this
  fold; the substrate increments it names are queued build cards
  (program §9.3b).
- **CR-001 §C — all seven items landed** (per-item map in program
  §9.3b): prodder rename + naming law; §5b Avasarala role +
  nurse/Miller division; Phase 4 nurse-contract rewrite; Phase 5b
  Miller-as-playbook; §7 fyi no-reply + blockage-identity dedup; §8b
  promotion pipeline; §8c first boot + Invariant 4 (the substrate never
  staffs). §A's no-acknowledgment law recorded in the program record's
  Invariants (effective now); §B honored — no in-flight rework, typed
  specimen/blocker rows recorded as next-iteration candidates (fabric
  §12 Q10); §D queue recorded in program §2/§3; §E done (this entry +
  program doc, same breath). Per CR-001's own terms this entry, not an
  acknowledgment turn, is the compliance record.
- **CR-002 traceability checklist:** spec halves TICKED at r5; seed/
  build halves are named cards queued at the Phase 3 entry (program
  §9.3b) — recorded honestly, not claimed done:
  - D1 fabric §4/§6 amended TICKED; paired office templates QUEUED.
  - D2 fabric §6 citation requirement specced TICKED; seed dispatch-rule
    template QUEUED.
  - D3 fabric §6/§8 amended, provisioning election + exec matrix
    specced TICKED; manifest schema + prearmed matrix build QUEUED.
  - D4 fabric §6 notes-in-rows specced TICKED; desk playbook QUEUED.
  - D5 fabric §6 granularity + desks-have-no-desks confirmed TICKED.
  - D6 new fabric §6b specced (manifest `desk` section +
    home-projection composition — the one mechanism change) TICKED;
    the Phase 2-class work item to open at Phase 3 entry QUEUED.
  - D7 fabric §6c inception section TICKED; seed SOP + PO playbook +
    charter template QUEUED.
  - D8 altitude rule in fabric §6c TICKED; orchestrator charter
    amendment + statute template QUEUED.
  - D9 CR-001 executed same sitting TICKED (above).
  The fold's completion attest covers the SPEC fold; the queued halves
  block the Phase 3 entry, not this fold — integrator may re-rule.
- **Files:** coordination-fabric-v1.md → r5 (full revision-trail entry);
  v0.2-program-2026-08-12.md (§1 names, §2 queue, §3 r5 additions, §7
  Miller posture, §9.3b landing map, Invariants + Status); this ledger.
  Sealed files untouched. Commits in specs-repo git log (unpushed; the
  integrator pushes).

## r5 fold — CR-001 + CR-002, one sitting (COMPLETE)

Review chain: Sol xhigh fold review (both arms, fidelity vs the CRs +
candidates file) → 8 findings (2 blocking, 6 important) → fix rounds on
both arms + the §6 no-ack residue the seed arm correctly escalated →
round 2: **reviewed-clean both arms**, no new defects, no cross-arm
contradiction. Landings: specs r5 stack pushed (6eee36c..848142b); seed
arm merged to main 3b0c135 (gates 1595 Elixir / 0 failures + assemble
green). The r5 spec status remains FOLDED; a READY declaration for
building the r5-specced mechanisms follows the program's review law when
their build is authorized.

**Named build cards opened by this fold** (join the Phase 3 entry queue
alongside the directive attest kind + role-bind CLI):
- CARD-D6: manifest `desk` section + home-projection composition (the
  CR's one real mechanism change; composition order ruled in §6b; field
  shape = fabric Open Q7). Includes Archetypes.validate! schema change.
- CARD-D3: provisioning election (desk vs workshop) + archetype-keyed
  dispatch-rule enforcement (structural facts only, per the fold's
  containment ruling).
- CARD-AUTH: typed authority-reference field on spawn filings (D2's
  citation rides free-text today, per the desk playbook's marks).

**CR-002 traceability checklist — ticked row by row:**

| # | Tick condition | Status |
|---|---|---|
| D1 | fabric §4/§6 amended; office templates authored | ✅ fabric §4/§6 r5 (6eee36c); role-charter.md + delegation-card.md paired (e697451/852425b) |
| D2 | rule template exists; spec names citation requirement | ✅ dispatch-rules.md (authority-citation, uncited-refused); fabric §6 names it; citation rides the spawn filing (fe08b98) |
| D3 | schema change specced; exec matrix in seed | ✅ provisioning election specced (fabric §6 r5); restrictive matrix prearmed in dispatch-rules.md (structural facts only); enforcement = CARD-D3 |
| D4 | notes-in-rows in playbook | ✅ desk-playbook.md (latest-wins attests, artifacts, disposable scratch, one-spawn-zero-knowledge) |
| D5 | granularity + no-desks-for-desks confirmed | ✅ fabric §6 r5 + desk-playbook.md |
| D6 | mechanism specced; Phase 2 work item opened | ✅ fabric §6b r5 (composition order pinned, four knobs); CARD-D6 opened above |
| D7 | SOP authored; charter template exists | ✅ inception.md (corrected PO-desk flow verbatim) + spirit-charter template embedded; PO playbook amended |
| D8 | charter amended; template authored | ✅ orchestrator.md lifecycle contract; altitude-statute.md |
| D9 | CR-001's own checklist complete | ✅ C1-C7 all landed (§5 prodder rename; §5b Avasarala role; Phase 4 nurse rewrite; Phase 5b Miller playbook; §7 no-ack anchor + blockage dedup; §8b promotion tiers; §8c first boot + Invariant 4); §A in force (this entry is the compliance filing); §B honored (Phase 1 untouched); §D authored (comms-discipline.md, staffing.md, specimen discipline, security-incident kungfu skill); §E done (program doc updated same sitting) |

**COMPLETION ATTEST: the r5 fold sitting is complete.** Every checklist
row ticked against a landed artifact; the three named build cards are the
fold's recorded remainder, queued at Phase 3 entry. Per the
no-acknowledgment law, this entry is the compliance record for CR-001 and
CR-002 — no acknowledgment was filed.

## Completion review №2 — Sol-only, vs r5 (2026-08-14)

Mike-ordered. Sol xhigh, direct tree, main 3b0c135, checklist derived
from fabric r5 whole. STUB SWEEP CLEAN (zero stubs anywhere; every
build-card marker resolves to named scope); all ten recorded exclusions
verified GENUINELY ABSENT rather than half-built; Phase 0-2 paths
re-verified wired end-to-end. Verdict: NOT COMPLETE — three uninvited
gaps:
1. **Prodder watermark population unbuilt** — supervision sweeps only
   open assignment entitlements; the r4-folded population extension
   (unanswered input-needed/blocker at 30-min floor; obligation-quiet
   60-min seed floors) has no implementation and agent questions are
   deliberately unswept. Phase 3 acceptance №3 has no machinery yet.
   → **CARD-WATERMARK opened**: extend the prodder sweep population per
   fabric §5 (population + seed floors as policy data), joins the Phase
   3 entry queue. Note: floors are SEED VALUES — the card must land them
   as data, not constants (razor).
2. **Algedonic human-channel half** — self-named PARTIAL in code
   (wakes.ex:198), in wave-1's dirt list, but never formally recorded as
   a delta. → **CARD-ALGEDONIC opened**: the org-configured human channel
   route, buildable when a notification carrier exists on the 0.2 line
   (Phase 3 entry at the earliest — needs a live gateway's external
   path). Bookkeeping miss acknowledged: recorded dirt is not a recorded
   delta until it has a card.
3. **Naming residue** — three machinery comments still say "avasarala"
   (wakes.ex:185, wakes.ex:1509, escalation.ex:375). → fixed directly
   (comment-only), see log.

Phase 3 entry queue now: directive attest kind, role-bind CLI, CARD-D6,
CARD-D3, CARD-AUTH, CARD-WATERMARK, CARD-ALGEDONIC + deferred live smoke.

## CR-003 queued (2026-08-14) — filing per the no-ack law

CR-003 (8866c19, authorized 2026-08-14) read in full and REGISTERED in the
Phase 3 queue: the mergedown card + landing receipt chain and the
world-fact law (D8), with `unknown` as a legal probe answer extending the
rails never-fabricate invariant to world-facts. Ordering constraint
recorded: lands BEFORE the closure path hardens. §3 HAZARD pinned against
the queue: the readiness-to-card and landing statutes must not stand on
the current prodder (wi_e27eb1a6 — retry-tier exhaustion after failed
turns — leaves the floor silent exactly when a lane struggles); they
follow the Phase-4 unbraid or carry their own liveness proof. Checklist
C1-C10 will tick here at the fold. Phase 3 entry queue is now eight
cards + CR-003 + deferred smoke.

### CR-003 §6 scope amendment registered (2026-08-14) — filing per the no-ack law

§6 (1117554) read and bound to the queued fold: the substrate half is the
GENERAL CONTENTLESS FLOOR only (external-observation receipt: typed ref,
probe, digest, expiry, representable unknown — "merge" in the substrate
half = wrong split); exactly ONE probe, shipped as kungfu (git landing in
agentic-engineering, swap-able); seed slot EMPTY; §0/D8 siblings are
evidence not cards. Sequencing pinned: lands before Phase 3 hardens
closure semantics or the fold becomes a migration.

### CR-003 §6 CORRECTED (27d8991) — registered; filing per the no-ack law

The empty-slot line moved and the correction is bound to the queued fold:
SEED ships the probe list empty (every-domain inheritance); ENGINEERING
KUNGFU ships the EIGHT known probes now, as guidance with probe shapes —
learned domain content is exactly what the bundle exists to carry. New
checklist row C11 (probes written into agentic-engineering; seed slot
verified empty). No ninth probe — that hunt is Miller's. Both-lines
patch: the same ruling reached the 0.1 POs tonight.

### CR-003 §7 registered (0426a52) — filing per the no-ack law

§7 read whole and bound to the queued fold: tightbeam-law-minting
restructured as the design-pattern catalog of rail writing in BOTH lines
(doctrine canonical in wisdom.md, cited by number; the skill = applied
vocabulary). Fourteen patterns each carrying the failure prevented and
the rows that paid for it; four anti-patterns named for reviewers to
refuse. Checklist row C12 added; fold ticks C1-C12.

### Election ruling registered (2026-08-14, d5eb14e) — filing per the no-ack law

Live model/harness switching ELECTED as core 0.2 capability
(wi_6435c199 / wi_1b0237fe / wi_4241a9dc). Bound as CARD-LIVESWITCH in
the Phase 3 entry wave: re-land in 0.2 terms with 0.1.x's 835ebb3 as
REFERENCE (2,240 lines on the exact seams Phase 1 re-legislated — no
cherry-pick); the typed-fields naming and typed-path-only design survive
verbatim. Sweep-process amendment absorbed into the standing §8c duty:
future sweeps scan 0.1.x LANDINGS (commits), not only ready work items —
the merge floor's items missed the largest post-0.1.7 feature because it
carried no ready row. Phase 3 entry queue now: 8 mechanism cards +
CARD-LIVESWITCH + CR-003 fold + workstream-3 fabric-operation slice
(pending Mike's sequencing word) + deferred smoke closure.

### LIVESWITCH upgraded to required; landings sweep opened (2026-08-14) — filing

Mike's directive (d5eb14e + 21eee8f): live switching REQUIRED on both
lines. CARD-LIVESWITCH is IN BUILD (Opus origination, branch
p3e/live-switch, references 835ebb3/662ca58/cc5ac9c read-only,
reference-not-patch, typed-fields + typed-path-only survive; §4 boundary
threaded: agent-initiated election, substrate never initiates). The
cross-line landing ledger's standing probe is running (recon dossiers on
the unruled population; bin judgments follow as a ledger update). The
0.1 side (cc5ac9c mergedown, 0.1.8 cut+deploy) is the 0.1 POs' lane per
Mike's same-night direction — watched, not touched.

### Cross-line landings sweep executed (2026-08-14) — filing

All 15 unruled rows dispositioned in the program doc's landing ledger:
four already on main by patch-id (the CR-003 §0 lesson live — duplicate
SHAs, identical patches); five = the LIVESWITCH cluster (in build; the
be61cfc marker design added to that card as reference, the wire-envelope
re-legislation itself deferred as CARD-WIREENVELOPE); two judged cards
queued (CARD-CANCELTESTS: adapt the two missing regression tests to
main's split modules; CARD-TXNNOTICE: judge the record/publish notice
split against seam ②). Bin-1 picks EXECUTED same sitting per the
three-bin law and LANDED: c5c3dc0 (adapter-death settlement) + 83f9b0b
(declared-files listing, verified bound to the public listing verb) —
main 63e3400..ec6c727, gates 1597/0 (+2 picked tests), CI tripwire on
push.

### CR-004 registered (2026-08-14, 7d2a0fa) — filing per the no-ack law

Smoke slices read in full and QUEUED in the same Phase 3 entry wave as
CR-003, sharing its fold sitting: the slice artifact, the reviewed-clean
coverage clause, the derived release set over the landings probe, the
sliceless-landing release blocker with no-slice declarations, receipts
with blocking unknown, tiering as policy data. Composition verified
against the queue: no new bone — it is CR-003's receipt floor + landings
probe + the world-fact law applied to "was this release exercised".
Checklist C1-C7 will tick here; C7 is both-lines kungfu (0.1 from
0.1.8). Note for the sitting: CR-004's C7 and CR-003's C9/C11/C12 edit
the same coder/reviewer guidance files — one kungfu authorship pass
covers both CRs plus the workstream-3 fabric-operation slice if Mike
sequences it there.

## CARD-LIVESWITCH (in review)

- **Origination:** Opus 5, branch p3e/live-switch @ 67caee0. HEADLINE:
  main already carried the tune backend (set_harness engine swap +
  history barrier; set_model fork/in-place; typed %Model{} fields vs
  per-{host,harness} catalog; generic live-config API already absent) —
  the re-land's real content is the MISSING HALF: the typed `tune` CLI
  (TuneControl enum makes partial naming unrepresentable; closed flag
  set so a typo'd flag cannot silently switch-to-default), named
  refusals (unknown_harness/same_harness/model_unavailable/
  turn_in_progress), the pending-count fence inside the held boundary,
  cc5ac9c's harness-boundary effort rule, owner-or-admin authorization
  (BEHAVIOR CHANGE, flagged: tune previously had NO auth gate), and a
  new C9 conformance class. Law 2 verified not assumed: every Phase 1
  seam is DB-keyed and swap-invariant; the one at-risk in-memory
  watermark was already deliberately deleted; the same-harness
  transcript-hiding defect refused by name. Three pre-existing defects
  fixed (set_reasoning unreachable from /agent/dispatch — param alias;
  invented harness = 500; same-harness barrier move). Gates: baseline
  1595/228 → after 1602/231, 0 failures, census 61 active fixtures.
  Deliberately not done, recorded: fast-mode (own capability), staged
  prepare-verify commit (own card — failure direction today is safe),
  router target-deferral (cosmetic), target.harness/model as Rules
  facts (small follow-up), be61cfc envelope (CARD-WIREENVELOPE; gap
  recorded: today's projection cannot express structured markers —
  bracketed-first-line is the normative limit).
- **Cross-review:** Sol xhigh, direct-tree — in flight.
- **Cross-review round 1:** Sol xhigh — changes-requested: 4 BLOCKING
  (enqueue race across the swap fence — pending-count is an independent
  query, prompt delivery can commit inside the window; §4 VIOLATED —
  --harness-only tune has the substrate electing default model + walking
  an effort preference chain, the exact forbidden mid-session election;
  legacy tune mutations rename/adopt/set_host reachable by ANY session
  token around the new gate; swap and barrier commit in separate
  transactions — swap_barrier_failed leaves silent substitution),
  5 important (duplicate concurrent swap double-moves the barrier with a
  false tombstone; the closed input surface exists only in the CLI —
  device/raw-dispatch seams silently drop malformed model fields into
  default selection; packed-effort refusal misses ultra and future
  tiers; role-origin authorization TOCTOU across rebind; a legitimate
  cross-harness barrier makes digest-carrier provenance unaddressable),
  1 nit (C9's no-catch-all claim not literal). Verified clean: the
  set_reasoning alias fix, serial same-harness refusals, decision
  requests/read states swap-invariant, no autonomous switch initiation.
  Fix round in flight (F2 reshapes the surface: model REQUIRED
  cross-harness, effort REQUIRED where tiered, no defaults anywhere —
  the caller elects or the call refuses).

### OpenAI usage limit hit (2026-08-14 ~19:30) — review lane holds

Codex refuses all calls (limit; retry 2026-08-20 or purchase). Blocked:
smoke-fix review, LIVESWITCH round 2, and any spec-review gates. Per the
preserved WAIT ruling, no same-vendor substitution without Mike's word;
branches hold pushed and unlanded. Proceeding meanwhile: LIVESWITCH fix
round (local), and the e2e closing smoke runs FROM THE BRANCH on both
boxes (branch evidence for the eventual review, not a landing bypass).
- **Fix round 1:** 028baa7 — ALL TEN applied. F1+F4 merged into one
  transaction (count + swap + barrier + tombstone single-writer
  serialized; the race proved closed by a concurrent-prompt probe test);
  F2 scoped to the tune verb — and the scoping lands exactly on §4's
  lawful line: spawn's default composition is CREATION-time selection
  (the sanctioned model-election mechanism), only mid-session election
  is forbidden, and that is now deleted (model_required /
  context_requires_model / effort_required refusals; CLI makes the
  states unrepresentable); F3 legacy mutations gated; F5 fresh in-txn
  re-check (one success, one same_harness, one tombstone); F6 closed
  surface at the GATEWAY (unknown_param / invalid_model_field on both
  paths); F7 at literal strongest (any bracket refused incl. vendor
  [1m] — typed --context is the path); F8 immutable-principal
  authorization; F9 digest carriers discoverable via inspect across
  barriers; F10 catch-all deleted (harness-gate clause was silently
  riding it). Gates 1616 Elixir / 0 failures (+14), 231 Rust. Dirt:
  Dispatch's code-key error classification undocumented (comment
  worthy); one pre-existing flaky O4 test under full-suite load
  (passed twice clean on re-run).
- **HOLD: round-2 review blocked by the OpenAI outage** (usage limit,
  retry 2026-08-20 or purchase). Branch holds pushed-unlanded per the
  WAIT ruling. The spawn-alignment question (should spawn's composition
  also refuse rather than default? — §4 says creation-time defaults are
  lawful, so current answer: no change needed; noted for the
  model-election workstream to confirm when it builds the elections
  walk).

### Fold authorship law registered (2026-08-14) — filing per the no-ack law

Mike's authorship note bound to the CR-003/CR-004 fold tasks: the kungfu
guidance is authored FRESH in 0.2 vocabulary (typed effects, receipts,
statutes, offices, the seam-③ decision-request path) — the 0.1 prose
relayed to the live POs tonight is written in 0.1 verbs (attest-note
probe/readback, hand-assembled release ranges, patrol-as-only-blocker)
and porting it would leave agents citing commands that do not exist on
this line. Same rules, different tooling, from scratch, in the single
authorship pass already planned for C7/C9/C11/C12.

### Advisory assignment file lists — reviewed landing evidence (2026-08-14)

- **Authority and scope:** work item
  `wi_7e9ea2f6-2a21-4aab-8574-d4c6697b6dc1`, pinned
  `p3-observables-producers-v1.md` SHA-256
  `c01a08440a5ce3bcae2d6ae73060faa1c0d8f0463d2690a1c564f97d15eac018`.
  Producer `asg_f76485fb-6da3-456f-b104-1bdbb4247415` made assignment
  file lists optional bootstrap context: create/reopen overlap denial was
  removed while storage, validation, API/wire/JobTrace visibility, and the
  truthful overlap observation remain. Candidate
  `53130652702467453cb9287f6dcb6e06934d091c` is one nine-file commit
  (`85+ / 78-`) on branch `coder/file-list-advisory-02`, with parent
  `ec6c727a36be249a19f6b7dac599c94a56c34b09`.
- **Gates:** clean-main baseline at `ec6c727` passed 9 doctests + 1597
  tests, 0 failures, 11 skipped. Candidate formatting passed; focused
  assignment/rules/conformance/JobTrace suite passed 190 tests, 0
  failures, 10 skipped; full Mix gate passed 9 doctests + 1597 tests, 0
  failures, 11 skipped; Rust formatting plus tests passed 228 tests, 0
  failures. Exact-tip SQLite/Dispatch smoke
  `mix test test/assignments_test.exs:788` passed 1 test, 0 failures and
  observed both overlapping assignments open with distinct ids and
  round-tripped lists. Producer receipts:
  `att_29604626-0c41-4d3a-b73e-3e7f2884787e` (`tests-passed`) and
  `att_a4ab941a-ae6a-4ecc-8638-b23c99f263aa` (`verified`). Immutable
  report `art_f10ec353`, SHA-256
  `a16e6ac6660d5fd2740938f9a62bfe9deb29dbb244770625b5cee76b9aee85eb`.
- **Independent review:** linked review
  `asg_dacb4095-28e1-4034-935c-c2b88746a2d4` filed exact-commit
  **reviewed-clean** verdict
  `att_72bb0a73-bf41-4878-b951-a946f8651f84`; the verdict explicitly
  covers the complete nine-file slice and reports no blocking or important
  finding.
- **Pre-push canonical probe:** at `2026-08-14T18:28:31Z`, expiring
  `2026-08-14T18:33:31Z`, `git ls-remote` returned product `main` =
  `ec6c727a36be249a19f6b7dac599c94a56c34b09` and candidate branch =
  `53130652702467453cb9287f6dcb6e06934d091c`; canonical output SHA-256
  `54105c60065bb1f93b0071b8ab7a11044d78147d1433f522bb02d39df942d7c2`.
  Integration assignment
  `asg_5643eaec-6af3-49b5-b610-02e3912c253b` may advance `main` only by
  direct fast-forward of those exact bytes while this observation remains
  valid. CI status is `UNKNOWN` before the main push; CI-on-push remains
  the tripwire.
- **Landing receipt:** immediately before the write at
  `2026-08-14T18:29:56Z`, canonical `main` still resolved to
  `ec6c727a36be249a19f6b7dac599c94a56c34b09` and the candidate branch
  still resolved to `53130652702467453cb9287f6dcb6e06934d091c`.
  `git push origin
  53130652702467453cb9287f6dcb6e06934d091c:refs/heads/main` advanced
  `main` by direct fast-forward (`ec6c727..5313065`) without changing
  candidate bytes. Canonical readback at `2026-08-14T18:30:20Z`, expiring
  `2026-08-14T18:40:20Z`, returned
  `53130652702467453cb9287f6dcb6e06934d091c refs/heads/main`; output
  SHA-256
  `f6e0c4a4ef23bd114b9bd75dc93d92eafc7e662eeeafb3cef079da722eda2799`.
  Exact-SHA CI status is `UNKNOWN`: `gh run list --repo
  clickety-clacks/tightbeam --commit 53130652702467453cb9287f6dcb6e06934d091c`
  exited 4 because this host has no GitHub CLI authentication. The CI
  tripwire remains externally observable when authenticated access is
  available.

## INCIDENT — credential-swap deadlock (2026-08-14)

Declared an incident by Mike during the e2e: `tightbeam onboard` reported
"Successfully logged in" three times on shrdlu while the stored credential
stayed the original 02:41 one. Root cause in `finish_staged_onboard/4`:
it restored the PRIOR credential whenever activation failed, and activation
always fails while the adapter's circuit is latched open — which is
guaranteed exactly when the old credential has stopped working, the only
reason anyone swaps one. Recovery required hand-editing a credential file.

Second, worse consequence (Mike): the restore is a SILENT-SPEND defect. An
operator whose login fails believes the system is stopped and may have
chosen that; reviving the previous credential resumed real paid turns
against an account they believed was disconnected.

**RULING (Mike):** "we have no business adding security ON TOP of codex or
claude logins." The substrate stores what the operator gave it and reports
the vendor's words verbatim. Credential operations fail CLOSED and VISIBLE.
Accepted cost, explicit: a bad login can displace a working credential.

**SCOPE:** `origin/main`, `origin/0.1.x` and `origin/release-candidate/0.1.8`
all carry it (5 occurrences each). Only the released v0.1.7 tag predates the
rollback. Fix lands on both lines.

**Fix:** delete the rollback (12 now-unreachable snapshot/restore functions);
commit the durable not-onboarded marker BEFORE installing, so no window
leaves stale `onboarded: true` metadata describing a never-activated
candidate.

**Review:** Sol (gpt-5.6-sol) high, two rounds. Round 1: 2 blocking
(stale-health after restart when both metadata writes fail; crash window
between install and the pre-activation marker) + 3 important — all applied,
the blocking pair by reordering mark-before-install. Round 2: "Safe to
merge: yes", no blocking findings; its one remaining test-gap was already
covered by an existing restart assertion, since made explicit.

**Gates (shrdlu, elixir 1.19.5/OTP 28):** baseline unmodified `origin/main`
5313065 = 9 doctests, 1595 tests, 0 failures, 11 skipped. After =
9 doctests, 1598 tests, 0 failures, 11 skipped. Format clean; Rust built.

### INCIDENT fix — LIVE PROOF on shrdlu (2026-08-14)

Package built from main `3e0c510`, installed via npm per the README, service
restarted. Deployed beam verified: `failed_finish` present, `rollback_failed_finish`
/ `restore_prior_state` absent.

The incident reproduced deliberately, then re-run against the fix:

- Spawned a codex session with a healthy catalog, then pointed the `codex-acp`
  SYMLINK at a failing stub (adapter only — `codex login` and the model catalog
  stay intact). Three wakes latched the circuit:
  `{"circuit":"open","consecutiveFailures":5,"generation":6}` — the incident's
  own precondition.
- Onboarded a credential in that state. The gateway reported exactly what it
  reported this afternoon: "Successfully logged in" then
  `{:provider_runtime_start_failed, %{failed: [%{reason: :degraded, harness: "codex"}]}}`.
- **VERDICT:** credential fingerprint `b4818e54…` → `ba34073b…` — CHANGED. The
  operator's credential survived the failed activation. Under the old code this
  fingerprint was identical, which is the bug.
- Durable metadata read `onboarded=false`, `last_health=present_but_unverified`,
  `cause={:error, {:provider_runtime_start_failed, …:degraded…}}` — fails closed
  and visible, with the reason verbatim.
- Everything restored: credential, metadata kind, adapter symlink, gateway healthy.

**Four earlier proof attempts were discarded as invalid rather than banked**, and
the reasons are worth keeping because each is a way to fake a pass:
v1 breaking `CODEX_PATH` did not fail activation at all (adapters boot lazily), so
"credential kept" only proved the happy path; v2 breaking it at boot also broke the
model catalog, so spawn refused; v3 stubbed `codex`, which `codex login
--with-api-key` also uses, so onboarding failed at CAPTURE and installed nothing —
a false negative reading as "still broken"; v3 also wrote through a symlink and
clobbered the real `codex.js` (repaired by reinstalling @openai/codex 0.147.0).

**Gates.** main: baseline `5313065` 1595/0 → after 1598/0. 0.1.x: baseline
`41452a3` 1519/0 → after 1520/0 (+1: 0.1.x lacks main's satellite tests). Format
clean on both. Landed: main `3e0c510`, 0.1.x `378807e`.

### INCIDENT fix — deployed to tars (2026-08-14)

Darwin package built ON tars from main `3e0c510` (no cross-compiling), installed
via npm, foreground gateway restarted in tmux `tb-fg` (the finding-10 form, since
the documented LaunchDaemon cannot run claude turns). Deployed beam verified:
`failed_finish` present, `rollback_failed_finish` / `restore_prior_state` absent.
Credential untouched (fingerprint `7fecc888…` before and after).

Behavioral proof was run on shrdlu only; on tars the verification is deployment +
health. The code is byte-identical and the mechanism is not platform-specific.

**Pre-existing condition found, unrelated to the fix:** tars boots NOT READY —
its **claude OAuth token has expired** (`401 authentication_error`, "OAuth access
token has been…"), so no harness on that host can run a turn. Not caused by the
deploy: the credential file is unchanged and the 401 is server-side. This is
exactly the reactive-swap case the incident fix makes recoverable — and it needs
Mike's browser to re-onboard claude on tars. Note the cold-install record already
flagged that this credential kind gives no pre-expiry warning: an observed 401 in
flight is its only failure signal.

Operator note: a stale credential copy remains at
`~/.tightbeam/auth/codex/auth.json.bak.<ts>` on shrdlu — preserved earlier as the
finding-21 specimen, now superseded by the root cause. Safe to delete; left in
place rather than removing a credential artifact unilaterally.

### tars claude re-onboarded (2026-08-14)

The expired claude OAuth token on tars was re-onboarded through the CLI's
anthropic ceremony (`tightbeam onboard anthropic`), which is an interactive
paste flow: it prints a claude.ai authorize URL carrying a per-process PKCE
challenge, then waits on stdin for the `code#state` the sign-in page shows.
Driven headlessly by holding a FIFO open with a background writer and running
the ceremony in tmux — a naive `< fifo` blocks before the URL is ever printed,
and a fresh invocation invalidates the previous URL.

Result: `{"credentialKind":"subscription","provider":"anthropic","status":"onboarded"}`.
Note it returned `onboarded`, NOT `provider_runtime_start_failed` — activation
committed cleanly on the first attempt, on the freshly deployed fixed build.

Proven with a real turn: `turns.seq 56 | status=delivered | claude |
claude-sonnet-5`. The adapter reads `{"claude:shared@TARS":{"circuit":"closed",
"consecutiveFailures":0,"generation":1}}`. tars had been NOT READY since its
token expired; it now runs turns.

Suite-shape note for anyone reading turn state: the `turns.status` enum is
queued/running/delivered/canceled/failed/failed_unknown. Success is
**delivered** — there is no `completed`, and querying for one silently reports
nothing rather than failing.

### INCIDENT part 2 — the circuit does not gate credential installation

The incident had two mechanisms, not one. Part 1 stopped the rollback discarding
an installed credential. Part 2 is the other half: the adapter circuit breaker
refused `start_provider_runtime`, so even a credential that survived could not be
activated without an operator restarting the gateway.

**Mike's decomposition (the fix):** the circuit protects AGENT CONNECTIONS from a
dead harness; it has no authority over CREDENTIAL INSTALLATION. Onboarding writes
adapter health, it does not ask permission. Conflating them deadlocked recovery
by construction — the circuit is guaranteed open exactly when a credential has
stopped working, which is the only reason anyone replaces one.

**The change is one clause:** `entry.circuit == :open and not authoritative? ->`.

**Six discarded versions, and why that is the lesson.** The first attempt DELETED
the circuit outright; Sol round 1 found the latch was silently pacing
demand-driven starts that backoff does not pace, so deleting it traded a cage for
a spin — reverted. The next five rounds each applied a finding by ADDING
machinery: fence reordering, a bespoke close skipping the park ceremony, a
generation reset, a named lifecycle API. Round 6 found the decisive fact:
**`fenced?/2` counts durable `running` rows, not only park fences, so the fence
reordering would have refused EVERY activation with a live adapter in
production** — and the new tests could not see it because they omitted
`process_identity_dir` and so never created the row every real adapter has.

Mike's ruling: **"the minimum always."** Six rounds of real findings is a signal
about SHAPE, not evidence of thoroughness — nearly every finding concerned
machinery the fix had added rather than the original bug. Cutting back to the
single clause retired all four outstanding findings at once, because they were
all about the additions. Every pre-existing close, fence, park and
death-accounting behaviour is untouched.

**Review:** Sol (gpt-5.6-sol) high, seven rounds. Final verdict on the minimal
patch: *"Safe to merge... I found no blocking or important defect introduced by
the clause."* Its one non-blocking note — that the test comments overstated what
a coordinator-level admission test proves — was applied.

**Carded, not fixed (both pre-date the incident):**
- a same-KIND credential refresh can return the adapter still running the OLD
  credential, because `live_entry?` precedes the context comparison
- `do_close_adapter/2` brackets its work in `begin_park/2` .. `complete_park/2`,
  and `begin_park/2` being INSERT OR IGNORE cannot tell a fence it created from a
  concurrent one, so a close can delete another operation's park fence

**Gates:** baseline `origin/main` 3e0c510 = 9 doctests, 1598 tests, 0 failures,
11 skipped. After = 1599 / 0 (+1 test), format clean. The test was verified to
fail without the clause (22/22 with, 1 failure without).

### INCIDENT CLOSED — both halves landed, deployed and proven (2026-08-15)

| Line | Part 1 (rollback) | Part 2 (circuit) |
|---|---|---|
| main (0.2) | `3e0c510` | `d3b6cb4` |
| 0.1.x | `378807e` | `8f22fe8` |
| release-candidate/0.1.8 | `17d9b83` | `71482dc` |

Gates, each paired against a baseline measured on that branch at that moment
(0.1.x's earlier 1520 was a pre-rebase tree, so it was re-measured rather than
reused): main 1598 → 1599; 0.1.x 1521 → 1522; rc/0.1.8 1513 → 1514. Zero
failures throughout, format clean.

**LIVE PROOF, on the deployed build, with NO gateway restart anywhere in the
script.** The incident's precondition reproduced exactly — codex-acp stubbed,
three wakes, `{"circuit":"open","consecutiveFailures":5,"generation":6}` — then
`tightbeam onboard openai --api-key` run through the latched circuit:

- **returned `"status": "onboarded"`.** Yesterday the identical call returned
  `{:provider_runtime_start_failed, %{failed: [%{reason: :degraded}]}}`.
- **the credential stuck**: fingerprint `b4818e54…` → `ba34073b…`. Yesterday it
  was reverted and the store kept the original 02:41 credential.
- an adapter was started (generation 6 → 7) and the ceremony completed
  `onboarded=true`.

Honest limit of that proof: the circuit still reads open afterwards, because it
closes only on `{:adapter_ready}` and the credential onboarded was the API key,
which genuinely cannot serve Codex-plan models. That is correct behaviour, not
residue. A full green recovery needs a working credential, and the subscription
path needs a browser — so an agent still cannot complete THAT unaided. What is
fixed is that the substrate no longer refuses or discards the attempt; the
remaining human step belongs to the vendor, not to us.

**Deployment:** shrdlu (systemd) and tars (foreground gateway per finding 10)
both rebuilt from main `d3b6cb4` and installed. shrdlu: subscription credential
intact, codex routing confirmed. tars: `READY: claude on TARS can run turns`,
and a real turn proven — `turns.seq 57 | delivered | claude | claude-sonnet-5`.

**Carded, not fixed — both pre-date the incident:**
- a same-KIND credential refresh can return the adapter still running the OLD
  credential, because `live_entry?` precedes the context comparison
- `do_close_adapter/2` brackets in `begin_park/2` .. `complete_park/2`, and
  `begin_park/2` being INSERT OR IGNORE cannot tell a fence it created from a
  concurrent one, so a close can delete another operation's park fence

**Also still open from the wider e2e:** finding 17 (the gate reports a generic
`:turn_error` while the vendor's real words sit in the gate log) and finding 22
(J5 concurrency commit-ordering under codex, reproducible 2/2). Neither belongs
to this incident.

### T2b re-run on the fixed tree + a REAL expiry recovered (2026-08-15)

**T2b re-run against main `d3b6cb4`** (both incident fixes), fresh checkout with
the Rust CLI built first per finding 14, evidence mirrored per finding 16:

- **codex: 17/18 — identical to the pre-deploy run, same single failure.** Step
  10 concurrency, same class: `c_sim_11_10 seq=22 store=23`, frames whose seq
  does not match the store row they carry. That is finding 22 reproducing a
  THIRD time. Restart resilience, queued-turn survival, wakes, model change to
  `gpt-5.6-terra` all still pass. **The one-clause change is inert on everything
  except the deadlock it fixes** — which is the point of the re-run.
- **claude: preflight REFUSED, `credential rejected: {:http_status, 401}`.**
  shrdlu's claude OAuth token had expired. The leg was never booted (SMOKE P3),
  so that column is absent evidence, not a pass and not a regression. The
  preflight refusing before booting is the substrate behaving correctly.

**SECOND EXPIRY IN 24 HOURS** (tars 2026-08-14, shrdlu 2026-08-15). The
cold-install record already noted this credential kind gives no pre-expiry
warning — an observed 401 in flight is its only signal. Two occurrences in a day
makes that worth a finding of its own rather than a footnote.

**THE BEST PROOF OF THE FIX SO FAR — a real expiry, not a simulated latch.**
shrdlu's claude credential was genuinely dead. Re-onboarded via the anthropic
paste ceremony while the box was in exactly the state that deadlocked yesterday:

- `{"credentialKind":"subscription","provider":"anthropic","status":"onboarded"}`
  — first attempt, no `provider_runtime_start_failed`.
- Adapter came up with NO gateway restart:
  `{"claude:shared@shrdlu":{"circuit":"closed","consecutiveFailures":0,"generation":1}}`
- Real turn: `turns.seq 89 | delivered | claude | claude-sonnet-5`.

Yesterday the identical shape returned
`{:provider_runtime_start_failed, %{failed: [%{reason: :degraded}]}}` three times
in a row, rolled the credential back each time, and required hand-editing the
credential file plus a service restart to recover. The full loop — expired
credential, onboard, activate, turn delivered — now completes unaided.

### T2b CLOSED on the fixed tree (2026-08-15)

Both legs now measured against main `d3b6cb4` (both incident fixes), each from a
fresh checkout with the Rust CLI built first per finding 14:

- **claude@shrdlu: PASS — 18/18.** Boot, pair, converse, tool use (the `uname -s`
  placement proof), stream create/rename/retire, cancel, queueing, concurrency,
  /new, /compact, /model, model change (applied `claude-opus-5`), restart
  resilience (pid 905777 → 906917, interrupted turn delivered, pointer "loaded"),
  restart queue survival, wakes, scheduled wakes. 13c MANUAL by design.
- **codex@shrdlu: 17/18** — identical to the pre-deploy run, failing only step 10
  concurrency with finding 22's signature.

**Finding 22 is codex-specific, now pinned by contrast.** claude PASSES step 10
on the same tree, same box, same commit; codex fails it 3-for-3 across runs. It
is not a general commit-ordering defect — codex's timing is what exposes it.

The one-clause incident fix is therefore confirmed inert on the entire client
journey ladder for both harnesses: every step that passed before still passes,
and the only failure is the pre-existing one.

Remaining e2e: T4 acceptance soak (not started, needs nothing from Mike), T3
satellite (not started, needs planning against satellite-e2e-v1's host matrix).

## CARD-LIVESWITCH LANDED (2026-08-15)

Mike's standing directive ("land the harness switch") executed through five Sol
review rounds. Harness switching — the feature 0.1.8 shipped — now exists on
the 0.2 line as the typed, agent-reachable `tune` verb.

**Review history (Sol, gpt-5.6-sol, high/xhigh throughout):**
- Round 1 (xhigh, pre-rebase): 4 blocking, 5 important, 1 nit → fix `028baa7`
  (single-transaction swap domain; no substrate election — model required
  cross-harness; legacy mutations gated; closed surface at the gateway).
- Round 2 (high, on the rebased branch): 3 blocking, 4 important — the root
  named: THE TRANSACTION SERIALIZED WRITES BUT NOT THE ELECTION. Fix
  `19efee5`: the election's snapshot became a transaction PRECONDITION with a
  named `stale_election` refusal; the home pin moved behind the commit; a new
  `on_tune_elected` probe seam (pre-boundary, deliberately) made the races
  deterministically constructible; packed model refs refuse at the gateway on
  set_harness (scoped: same-harness set_model keeps the issued-identity echo
  contract — scoping accepted by the reviewer in round 3); the no-pointer
  retune became one transaction with its marker.
- Round 3: 1 blocking — the election is HOST-dependent too. Fix `9d64527`:
  read_recorded_model returns the host; both retune transactions require
  harness AND host; host-under-retune regression test.
- Round 4: 1 blocking — set_reasoning is a RELATIVE election and its
  derivation basis must hold. Fix `36f1254`: the basis (family+context,
  effort deliberately last-writer-wins) travels into both retune
  transactions; regression test on a new ADDITIVE `fixture-tiered` catalog
  entry (fixture-model's empty-effort refusal is pinned behavior and remains).
- Round 5: **"Safe to merge. Findings: none."** Verified: the basis threads
  everywhere and only where it should; the fixture addition is honest and
  additive ("conformance still checks exact catalog lists rather than
  weakening assertions"); no regressions in rounds 2-4's confirmations.

**The mechanism in one sentence:** every way a tune's election can go stale —
a concurrent harness switch, a set_host, a model change under a relative
effort election — is refused inside the commit transaction by the one name
`stale_election`, and nothing is ever re-derived on the caller's behalf.

**Deliberate follow-ups (reviewer-accepted, not blockers):** the resident
adapter-fork path has no direct race test (the predicate is shared with the
tested no-pointer path); the two-destination-model home-pin race is
inexpressible with a one-model fixture catalog (the pin is structurally
post-commit).

**Gates (shrdlu, elixir 1.19.5/OTP 28):** baseline main `3e0c510` = 1598/0.
Final tree = 1625 tests, 1 failure — the failure is the DOCUMENTED
PRE-EXISTING O4 flake (coordination_fabric_test:820, recorded flaky-under-load
2026-08-14, pre-dating this card), 3/3 clean in isolation; the reviewer ruled
it "an acceptable named flaky condition — CARD-LIVESWITCH should not wait for
an unrelated O4 fix". Gateway+conformance suites 175/0. Rust 231 across 4
binaries. Format clean both languages.

**Patch-vs-landed delta, declared:** round 5 reviewed the five commits before
a two-line test-expectation update (harness_seam_test's exact catalog list
gaining the tiered entry) was folded into the fifth; no mechanism differs.

## PROD-DELIVERY FIX LANDED (2026-08-15) — the ladder advances on heard prods

The 2026-08-10 outage postmortem closed at the mechanism (design agreed with
Mike): prods fired into a dead harness became failed turns, the stored counter
climbed anyway, ladders exhausted into the dead tree — 127 strands stalled in
one day — and nothing re-armed them at recovery.

`branch_for_claim`'s rung decision now reads `heard_prod_count/2`, composing
two boundaries:

- **TRANSPORT:** supervision wakes joined to their turns; counted unless the
  turn failed or was canceled. A failed turn is evidence about the harness,
  not the holder. An admitted escalation's queued turn is the durable transfer
  itself. A wake with no turn proves nothing.
- **EPOCH:** a typed liveness receipt — the holder's named repair verb — opens
  a fresh epoch; heard history from before it never resurrects a rung. No
  receipt → all evidence counts, legacy NULL-generation transfers included.

Effect: an outage FREEZES ladders instead of burning them; the first delivered
prod after recovery resumes the climb where the evidence left it. No new
state, no recovery listener, no reset verb — the existing sweep is the
recovery mechanism. Stored counters unchanged (they pace backoff).

**Review:** Sol high, three rounds, each finding something real:
1. blocking — lifetime count overrode the typed-receipt reset, re-escalating
   an accountable holder immediately after repair (the exact cage this fix
   removes, rebuilt by accident); important — the outage test mutated a
   failed turn to delivered, impossible in production. Both applied (epoch
   boundary; retry-wake delivery).
2. blocking — with no receipts, SQL NULL >= 0 silently un-heard legacy
   retirement transfers. Applied (two-branch epoch clause + regression).
3. **"Safe to merge. No findings."** — both epoch branches verified, the
   legacy fixture confirmed production-shaped.

Declared non-blocking follow-ups: health/forensics still report raw prodCount
without the derived heard count; a long-outage performance fixture for the
per-evaluation COUNT join.

**Tests:** the outage regression (prod fails → re-prod not stall; retry
delivers → escalate), the repair-seam regression (delivered prod → verdict →
prod 1, no stall), the legacy-transfer regression (counts pre-receipt,
history post-receipt — the fixture co-authored by the schema's own coherence
triggers), and two existing tests reconciled to heard-count wire numbering
with their subjects asserted unchanged.

**Gates:** baseline main 1625 (with documented O4 flake) → 1628 / 0 failures,
format clean.

## 2026-08-16 — CONTRIBUTING: release branches evolve until they pass (f6e5d08)

Docs-only. Folds Mike's 2026-08-16 ruling (decisions ledger 3b6c530) into the
release model: quits is earned by testing clean; release-test fixes land on
the release branch as new builds; successor stays empty until quits; a
premature tag (v0.1.8) does not freeze a branch still under test. Gate:
packaging/assemble.sh clean (version smoke 0.1.7 consistent; main's version
stamp remains the declared follow-up). Pushed main directly per the no-door
process.

## 2026-08-17 — 0.2.1 reconciliation waves 1+2: 14 commits elected from the 0.1 line

Mike approved waves 1 and 2 of `0.2.1-cross-line-reconciliation-plan.md`
(dbc0826). Cherry-pick elections from origin/0.1.8, landed as one series.

**D1 — onboarding does not park the runtime** (fa47a00, b665614): the
gateway keeps serving during phased onboarding; the staged install replaces
the runtime after. Main carried the identical defect (wi_e86ddd54), proven
live on gibson 2026-08-16 when an orphaned ceremony wedged /version.

**D2 — onboard emits its sign-in URL + one-time code** (0e539b0, fe3160a,
e75b8ce): the gate-8 extraction of the delivery discipline — `cli/src/
onboard_emit.rs`, structured emission, fixture-backed tee proof for the
OpenAI path. Mike's ruling (decisions ledger 8287cd0) is definitional:
interactive onboarding is INCOMPLETE until the operator holds the URL and
code, so the mechanism and the definition ship together.

**D3 — /version states which bytes the gateway is** (7cc3646): version +
build + sha over the wire; the S9-F1 finding exists identically on main.
OPEN: main still declares 0.1.7 in cli/Cargo.toml, which /version now
reports truthfully — the version-identity ruling (0.2.0 vs 0.2.1) is
Mike's and is not folded here.

**D5 — decision-requests --status all** (0ebe326, fix only): the literal
"all" sentinel never reaches SQL. Conflict resolved NARROWLY: 0.1's version
also advertised operator-ask/-rule/-withdraw in CLI help and a wire test,
but those verbs do not exist on main — shipping the help text would
recreate gh#11's wire-dead defect in reverse. Help now lists the five
statuses main genuinely supports. The wire-crossing test (1ea30f5) is
DEFERRED to wave 3 with E1, where the operator verbs land.

**D4 — subscription-401 rotation harvest** (acff16d, 398d7ef, 0a0c790,
920dd79, 3449316, 8d97996, e4f4614): gh#9's fix with its review closures.
Coverage note carried over: the 401 path has no e2e step on either line
(triggering it needs a vendor 401 on cue) — fixture-backed e2e is a
follow-up card.

**E2 NOT LANDED — refactor confirmed empirically.** The wake-cancellation
provenance family (e782b01 + 5) was attempted as a straight pick and
aborted: e782b01 alone produces 39 conflict hunks (26 in wakes.ex) because
main has since absorbed the fabric Phase 1 delivery-policy work there.
Handed to the relief PO's Bug A lane to be RE-DERIVED under the reviewed
sender-visible cancellation vocabulary, with the 0.1 tests as the
regression floor (w_dcd6d28f). Also asked whether d8eddfd covers gh#15.

**Gates (gibson, elixir 1.19.5/OTP 28, rust 1.97.1):** baseline main a18d8b3
on eezo = 1628 tests / 0 failures. After = 1639 tests / 0 failures, mix
format clean. Cargo: 247 tests / 0 failures across 4 binaries, cargo fmt
clean. Run 1 of the Elixir suite showed 1 failure while the cargo suite ran
concurrently on the same host — the documented O4 flake
(coordination_fabric_test:820, "flaky-under-load", recorded 2026-08-14);
runs 2 and 3 alone were 0 failures at the same tip.

## 2026-08-17 — main states its version: 0.2.0 (02951a9)

Mike's ruling: main is 0.2.0; "0.2.1" was a misspeak and does not exist.
Landing D3 (/version reports the declared version) made the inherited 0.1.7
stamp a lie broadcast on every request, so the identity had to follow the
mechanism. cli/Cargo.toml is the single source; mix.exs derives from it.
Gates: 1639 tests / 0 failures, mix format clean; cargo 247 / 0, fmt clean;
`tightbeam --version` = 0.2.0. The reconciliation plan doc is renamed
0.2.0-cross-line-reconciliation-plan.md to match.

## 2026-08-18 — Artifact-custody rail widened + work-custody guidance (6acc927)

Mike asked for a rail requiring document-producing archetypes to record their
artifacts. The rail ALREADY EXISTED — `completion-requires-results-artifact`
in the shipped verification bundle since 2026-07-23, remedy = wake the holder
with the recording instruction. Its scope was the defect:
`holder_archetype in ["coder"]`, one archetype, while every other
document-producing role completed freely.

WIDENED: archetypes to `[coder, reviewer, spec-writer, recon]`; artifact kinds
from `["report"]` to `["report","spec","doc","data","other"]` (requiring
`report` alone would have blocked a spec-writer holding a good spec — the fact
carries what the holder ACTUALLY recorded).

Evidence, measured live that day: closed assignments with NO artifact on their
work item — reviewer 456/682, spec-writer 35/71, recon 34/148, coder 118/397.
Seven of ten workdir paths cited as evidence by attests in one day no longer
existed, because a workspace with no artifact rows is REMOVED at close
(artifacts-and-reconciliation.md ratified floor, Flynn adjudication #5).

Reviewer inclusion was argued both ways and Mike settled it: "the review doc IS
a verdict — why would we let context behind a verdict drop". The counter-
argument ("a verdict is already a durable row") fails because the note caps at
2000 chars, so reviewers already route clause tables into artifacts by hand.
Follow-on ruling carded as wi_42156d31 (0.2 design): a verdict POINTS AT an
artifact instead of carrying a capped note, retiring this clause for reviewers.

ALSO: `priv/seed/guidance/operating-model.md` gains a work-custody section
(composed into EVERY archetype, so learned-kungfu archetypes inherit it) with a
content pin in identity_test. Four test expectations updated for the new wake
text and the 0.2.0 version-sentinel collision (one of which upstream fixed
better, taken on rebase).

NO schema change, NO migration. Gates: 1640 tests / 1 failure — the documented
O4 flake (coordination_fabric_test:820, flaky-under-load), 2/2 clean in
isolation; mix format clean. Mike ordered an install-and-launch check on the
e2e machines after the current 0.1.8 e2e finishes — startup only, not a test
cycle — routed to product-owner:tightbeam; the relief PO holds the 0.1.8 port.

## 2026-08-18 — Non-blocking rail notice + review-rounds doorbell (0e08d83)

Work item `wi_6e202391` adds the missing non-blocking rails effect needed to
arm the already-staged review-rounds doorbell. `notice` records a named
`rail_notice`, summons through the existing wake remedy, and always allows the
triggering verb. The round-four crossing is keyed to the stable reviewed
assignment, so later attests do not ring it again. The armed statute targets
the registered product-owner role and uses the already-shipped
`assignment.review_verdict_count` fact.

The implementation reuses rules, dispatch, supervision, rail-remedy episodes,
role resolution, and wakes. It adds no schema, migration, spawn, reassignment,
or parallel workflow engine. A refused notice wake preserves a live
producer-free latch: the failure is recorded once and later matching verbs
still proceed without another summon. Gating-remedy failure retains its prior
release-and-retry behavior.

Exact candidate `0e08d838b5d79d02f89f4659aa3e609f86872976`, tree
`911420fd89a25fcffb0501a8a5a37aade7495700`, over main `7d0c736`. Producer
report `art_f16f48ad`, SHA-256
`1ab7880b117b78d319037f49fb68ca7b5c730216979064becaa9e2b57e6f8397`.
Independent linked re-review `asg_cbe0190a` is reviewed-clean in
`att_49ce1a85`; its durable clause table is `art_9d367336`, SHA-256
`7bae7b04fdf78a4f353d744d8430ccf1b2f791c5eb2b0eac0f2772145f3a8bc9`.

Gates on EEZO, OTP 28 / Elixir 1.19.5: focused refused-wake regression 5/0;
focused rules/doorbell/refix/papertrail 48/0; authoritative full gate 9
doctests + 1643 tests, 0 failures, 11 skipped; Mix format clean; public
rule-fact compatibility pass; shipped privacy clean; diff check clean.

Arming boundary: source delivery does not update a running org's learned
statutes. The org must relearn after a compatible runtime is installed.
Gibson's live 0.1.7 runtime cannot consume this main-line mechanism yet.
The 0.1.8 port remains a separate Mike election.

## MVP-threshold guidance codification (kungfu, docs-only)

- **Authority:** Mike ruling 2026-08-18, artifact `art_7ee92ca6`
  (`mike-mvp-spec-review-ruling-2026-08-18.md`, sha256 f789fb4b…d7dc6, on
  wi_46b8cf8c), reaffirmed and extended by Mike live 2026-08-19 after the
  Lachesis MVP burned hours gated on a fixture capture the ruling would
  have waived. Mike ordered the ruling integrated (not appended) into the
  agentic-engineering kungfu on BOTH lines — main (0.2.0) and 0.1.x
  (0.1.8) — with no redundancy or conflicting guidance.
- **Change** (3 files, priv/kungfu/agentic-engineering/guidance/):
  `engineering-tenets.md` gains a "Drive to an MVP" tenet (minimum for a
  good MVP; correctness/safety floors mandatory; completeness/polish/
  speculative robustness never block; disputed-core points pause and go
  to the product owner) and a proportionality qualifier on the real-
  fixture tenet (a capture proves today's contract reading; when it needs
  a human or live credential, the test skips honestly and the code
  refuses loudly on mismatch; never fabricate, never hold an MVP for it —
  the Lachesis case, asg_21ff34d7). `spec-writer.md` gains "Sufficient
  beats complete" (stop at buildable-MVP; non-core cases are NON-BLOCKING
  Open Questions). `reviewer.md` gains "The blocking bar" (changes-
  requested only for egregious defects with outsized effect on core
  functionality, progress, safety, or rewrite cost); verdict rule
  reconciled so only above-the-bar findings hold `reviewed-clean`, and
  the blocking severity is anchored to the bar.
- **Landed:** main 1092245 (MVP reconciliation; f01651d had landed the
  role sections concurrently, so the landing reconciles rather than
  duplicates: corpus-wide tenet, fixture proportionality, verdict rule
  anchored to the threshold) and b59047b (unslop pass, Mike-ordered
  2026-08-19: all em dashes out of the fourteen guidance files, plain
  words, meaning preserved; domain vocabulary kept). 0.1.x 0b91a1b
  brings the guidance dir to byte parity with main b59047b on Mike's
  explicit cross-line authorization — parity includes main's f01651d +
  6dda4a7 guidance, eliminating the 4-file fork between lines.
- **Gates:** docs-only per AGENTS.md — assemble.sh green three times:
  gibson main checkout (0.1.7 artifact), main worktree after deps.get
  (0.2.0 artifact), 0.1.x worktree (0.1.8 artifact). First worktree run
  failed on missing hex deps before deps.get; not the change.
- **Deviation to note:** the b59047b push raced its gate — the commit
  chain ran despite assemble exiting 1 (missing deps, see above). The
  gate was then run to green on the pushed tree, so the record is
  repaired, but the push preceded the green run.

## 2026-08-19 — Decision-ask discipline in the operating manual (8806f7f)

Docs-only, Mike's direct order ("more comprehensive but token-efficient"
decision-request guidance for 0.2.0). The manual's ask section gains: a need
that is not a row reaches no one (file the ask, never bury "needs the owner"
in attest prose); the don't-ask list (rows/status, own facts, precedent, the
product owner's domain); one open question per choice with --about linkage;
read your answers before asking anew. The user-decision section rebuilt: the
verb with --user and --about, hold semantics aligned with THE QUESTION HOLDS
NOTHING (park only the dependent step), and the inbox contract — the user's
open questions are the one inbox they trust to be complete; withdraw when
moot, never duplicate. Motivating specimen: three hours post-0.1.8-upgrade
the live org filed zero decision requests while hand-attesting needs-Mike
prose.

Gates: baseline e8d21d3 = 9 doctests, 1646 tests, 1 failure, 11 skipped;
after 8806f7f identical. The 1 failure is PRE-EXISTING and bisected as such
(catalog_diff "learned org reads the working set": Diff.evaluate returns an
empty working_set from installed preferred-models.md on pristine main).
FOLLOW-UP owed: diagnose the working-set parse against the current
preferred-models.md shape; a red gate on pristine main may mask real
regressions.

## Spelling sweep + catalog-diff regression fix (main only)

- **Authority:** Mike rulings 2026-08-19 late: "Tightbeam, no space" and
  "this sweep will land on 0.2.0" (main only; 0.1.8 locked; 0.1.x branch
  deleted the same evening on his ruling, sole unique commit 0b91a1b's
  content already on main).
- **Landed:** 78dedaf fix + 4a730b1 sweep. The fix repairs a regression
  b59047b (unslop) shipped: mix tightbeam.catalog.diff parsed working-set
  capsule ids with a regex anchored on '**id** —'; the unslopped ':'
  yielded an empty working set. Parser now anchors on the bold id alone;
  the machine-read shape is stamped at the seam in preferred-models.md.
  Same commit mix-formats test/release_tag_workflow_test.exs, which
  landed unformatted at 19:31. Sweep: 37x 'Tight Beam'→'Tightbeam' in 18
  files incl. both harness identity preambles and the asserting test.
- **Gates:** baseline (unmodified tip 8806f7f) = RED: 1646 tests 1
  failure + format violation, both pre-existing (main CI red since
  b59047b, 10:31–21:14, three failing runs unnoticed — the tripwire
  fired and nobody read it). After: mix exit 0, 9 doctests 1646 tests
  0 failures 11 skipped; cargo fmt clean, 247 rust tests passed.

## 2026-08-21 — Wake discipline: one instrument per wait type (manual)

Mike's ruling folded into the operating manual as ONE canonical section,
"Match the wake to what you wait on": in-org rows are never timer-polled
(subscribe via --when-fact; completions on your cards are delivered),
timed rechecks are lawful only against external systems and record probe
results (three unchanged probes escalate to the card's opener), humans are
asked. Replaces the old section that taught polling colleagues. The same
bytes were published org-locally to all seven archetypes on the live org
(embedded per archetype: 0.1.8's identity CLI has no shared-doc verb, so
per-archetype embedding is that org's single-source ceiling — the shipped
manual is the canonical copy).

Two findings from the exercise, carded: (1) the identity CLI needs a
shared-guidance-doc edit verb so orgs can single-source cross-archetype
policy; (2) the default archetype's [guidance] is inline TOML with zero
includes — org edits to guidance/default.md have never served to default
sessions (Tuesday's serving mystery, now explained; Main has been governed
by direct wakes alone).

Gates: after = 1646 tests, 0 failures (the catalog-diff red noted 2026-08-19
is fixed upstream). Landed 8806f7f-successor on main.

## 2026-08-21 addendum — org wake-policy rollout findings

Org single-sourcing achieved via the org-owned operating-manual override
(identity.ex: org tree's operating-manual.md wins; served to every session,
deduped). Embedded per-archetype copies removed same commit; published
36c0cf0. Two 0.1.8 substrate specimens for wi_2629d68d: (1) relearn restores
seed-owned archetype manifests, reverting raw include edits; (2) identity
edit --manifest silently no-ops guidance-text changes ("nothing to commit").
Consequence: the default archetype cannot lawfully serve guidance/default.md
on 0.1.8; Main's standing orders remain conversation-held.

## 2026-08-21 — Visibility-safe typed short-ID prefixes

- **Authority:** `wi_ff8fc37e`; Mike ruled `main` as the integration target in
  `dr_1817b0f4` after a fresh remote read proved the producer's earlier
  `origin/0.1.9` target was stale and no longer existed.
- **Reviewed source:** producer commit
  `f248a5acc16923f8daf518e7bc6603c65b51e9d5`, with independent
  reviewed-clean verdict `att_bc4b3f6c`. The resolver accepts exact IDs and
  literal typed prefixes for assignment, work-item, and wake IDs. It resolves
  only visible rows, refuses deterministic visible ambiguity, preserves the
  existing unknown errors, and keeps mutation lookup and action in one
  transaction.
- **Main integration:** clean cherry-pick onto `ef1ef51` produced
  `a1cea925563adbb7cca62b463a705658bd07d025`; no conflict or content edit was
  required.
- **Gates:** current-main baseline format passed; the full gate reported 9
  doctests, 1,650 tests, 1 failure, and 11 skips. The sole failure was the
  pre-existing `CoordinationFabricTest` O4 digest-materialization race. The
  integrated commit passed format and the full gate with 9 doctests, 1,665
  tests, 0 failures, and 11 skips. Both runs used the repository fixture
  harness and the existing Erlang/OTP 28, Elixir 1.19.5, Rust 1.95, and Node
  toolchains; no toolchain was installed.

## Identity relearn/apply candidate review — changes requested (2026-08-21)

Independent review of candidate `74c7c3e875896c3b0fed230501f62147a56c7d34`
against `relearn-and-identity-apply-workflow.md` returned
**changes-requested; not landed**. The producer's exact-commit gates passed
(247 Rust tests, 1684 Elixir tests plus 9 doctests, 11 skips, and 6 V8/JSC
normalizer cases), and independent focused, Rust, Elixir, formatting, and
diff gates also passed. Retained Gibson state proves two real Codex applies
completed with exact continuation messages and final target stamps. The
required Claude leg remains unproven after a recorded HTTP 403 credential
failure.

Blocking findings: runtime adapter effects have no durable authoritative
readback after external completion and can retain the operation fence after
a lost receipt; the failure normalizer is not connected to production
worker failures; the Claude real-harness acceptance leg is absent; and the
required crash/race/migration/retry acceptance matrix is largely absent,
with the lost-receipt case independently reproduced as a real failure.
Important findings: `identity apply --help` renders the parent identity help,
and recovery policy is read from live constants instead of the accepted
operation snapshot.

Durable review report: Tightbeam artifact `art_4ef700c8`, SHA-256
`ad676c1400bd308049e908dc5b64a0126e7c1e6b9e0c03548ab718abda9b8a79`.

## 2026-08-23 — Operational session parents port (reviewed, pre-landing)

- **Authority and custody:** Mike ordered work item
  `wi_a45ffa39-44a0-4218-a03b-9a1ce1558ca9` ported from the four named
  0.1.x recovery commits onto 0.2.0 `main`. The reviewed candidate is pushed
  to canonical remote `git@github.com:clickety-clacks/tightbeam.git` at
  `refs/heads/port/operational-parent-main-0.2.0`, exact commit
  `76bdb1107792a557940fa8d8a2e0c0602b47938e`. Current `main` and the
  candidate's merge base are both
  `415396fa6fe4faf67aa1d86326ffe1ca9b6a802e`; no landing had occurred when
  this evidence was filed.
- **Port shape:** source commits `a2853e5`, `69e18c8`, `556ee59`, and
  `6da2d2a` became candidate commits `7f0e2e0`, `e5532b8`, `37dabca`, and
  `9b04ad5`, preserving their order and cherry-pick provenance. Conflict
  markers occurred in `lib/tightbeam/effort_checkin.ex`,
  `lib/tightbeam/schema.ex`, `test/escalation_delivery_test.exs`, and
  `test/schema_shape_test.exs`. The resolution preserved the current-main
  coordination fabric while routing parent behavior through the required
  non-null operational parent.
- **Behavior:** Main self-parents; every other session derives a non-null
  operational parent from its spawner or the owner's Main; reparenting cannot
  clear, orphan, move Main, or create a cycle; `spawnedBy` remains provenance.
  The v4-to-v5 upgrade rebuilds the sessions table transactionally after
  validating the existing graph. Artifact ancestry, fault bubbling,
  supervision, work-block authority, retirement, and the effort prodder use
  `operationalParent`. The prodder climbs only effective active parents,
  terminates at Main, and never files an effort decision request or targets a
  user.
- **Review round 1:** independent assignment `asg_54e58c3c` returned
  changes-requested in `att_3f881360` because artifact ancestry, bubbling,
  work-block authority, and retirement lacked fixtures where `spawnedBy`
  differs from `operationalParent`. The finding was fixed without production
  changes in test-only commit `76bdb11` by four exact divergence fixtures.
- **Re-review:** independent assignment `asg_bb0850da` returned
  **reviewed-clean** for exact commit `76bdb1107792a557940fa8d8a2e0c0602b47938e`
  in verdict `att_34c8f460`. The full clause table and review trace are
  artifact `art_923343c8`, SHA-256
  `60bbec89fa5c339f3afb756aac95ffa7de7120745459e4320d791cac4473e980`.
- **Gates and real-input proof:** baseline `415396fa` passed 9 doctests plus
  1,675 tests, 0 failures, 11 skips. Exact candidate `76bdb11` passed the
  release Cargo build, Mix format, and authoritative `scripts/verify_mix.sh`:
  9 doctests plus 1,684 tests, 0 failures, 11 skips. A database created by
  unmodified v4 `main` upgraded to v5 with Main self-parented, a root parented
  to Main, a child parented to its spawner, unchanged `spawnedBy`, a NOT NULL
  operational-parent column, and an empty `PRAGMA foreign_key_check`.
  Producer report: `art_4d827d77`.

### Landing evidence (2026-08-23)

Product pull request #27 merged the reviewed candidate into canonical
`refs/heads/main` as merge commit
`8eeccbd6dfd221fe9d105783459637fb7a17ea83`. Its parents are prior `main`
`415396fa6fe4faf67aa1d86326ffe1ca9b6a802e` and reviewed candidate
`76bdb1107792a557940fa8d8a2e0c0602b47938e`; the merge tree is identical to
the candidate tree. GitHub Actions run `32634208237` passed the Linux and
macOS release-build, Mix-test, Cargo-format, and Cargo-test jobs. A fresh
gate at the exact canonical merged tip passed `cargo build --release
--manifest-path cli/Cargo.toml`, `mix format --check-formatted`, and
`scripts/verify_mix.sh`: 9 doctests plus 1,684 tests, 0 failures, 11 skips.
Primary assignment verdicts `att_0df012ab` and `att_095ac0ec` record the
exact-tip test and integration receipts.

## 2026-08-25 — Event-firehose slice 1 (reviewed, pre-landing)

- **Authority and custody:** work item
  `wi_d38bf448-383e-450c-8864-26d5d494643f`, integration assignment
  `asg_fd993fc7-43fd-401f-9433-190e07d5d7f1`, and product-custody
  assignment `asg_b578a3d5-e499-4ef0-b829-4da19d48e156`. The reviewed
  candidate is pushed at branch `integration/firehose-slice1-8b4a3df`,
  exact commit `78b33409cbbedcf6507edfac05aa06c3dcb509da`. Its reconciliation
  merge contains prior reviewed integration `3929d11d2975140af23bab69fe367249530fa0dd`
  and then-current `main` `f24b5a17caaf738c27a69cf421e1572e3397fd36`.
  The bounded repair changes two files with 11 insertions: an accepted,
  exact decision-return retry emits one `verb.accepted` event and no second
  `decision_request.returned` state notice.
- **Gates:** focused escalation and publisher tests passed 29 tests with
  zero failures. A fresh owned clone at the exact candidate passed 9 Mix
  doctests plus 1,746 tests, 0 failures, 11 skipped; Rust passed 246 tests,
  with doctor 1/0, SIGCHLD 1/0, and stdin 2/0. Producer receipts are
  `att_bf6ea310-004d-4aeb-8a11-1ef490b222e2` (`tests-passed`) and
  `att_085c643d-6d54-448f-a410-d11770cb78cb` (`verified`). Complete-gate
  artifact `art_057913c1` has SHA-256
  `1a436f8f9e8e1af45ec226f6cd46f99bea79233eb8c4f1152c9f386fbbc0b2da`.
  An earlier O4 failure in a long-lived clone was generated-state
  contamination; fresh-clone probes passed O4 at both the pre-repair merge
  and the exact candidate without an unrelated source change.
- **Restart and reconstruction proof:** real gateway smoke passed with
  authenticated and pre-authenticated reconnects closing on code 1012,
  distinct process ids, and a rebuilt projection size of 2. Artifact
  `art_65cbb2d2` has SHA-256
  `ea8dd123cf72afb7a9355c828dece387d55dee851dabeeec52f5494a70cb0b55`.
  Cold walkthrough `art_80bb9446` has SHA-256
  `4212466bf2525a47989fc8c70707a9cadc50116cfc76bd5e4fb28dfda1934329`.
- **Independent exact-tip review:** assignment
  `asg_de31427b-668b-4518-b781-c64aef822238` returned **reviewed-clean**
  for exact commit `78b33409cbbedcf6507edfac05aa06c3dcb509da` in producer verdict
  `att_72110eac-160e-4eb3-aa83-8a348484f481`. Full clause report
  `art_9dc7e5e1`, SHA-256
  `c250b334eae0dd89504f3c81737b0c89daea38572d9c8ff125491237d4ea70fe`,
  closes the retry finding with custom 1/0, focused 16/0 plus 31/0, O4
  1/0, format, and isolated restart proofs. The review completed in
  `att_1449aff3-9483-417c-9131-885a7ca9a9b0`.
- **Landing boundary:** this entry records review evidence before the code
  push, as required by repository law. Canonical `main` had not advanced to
  the candidate when this entry was authored. The integrator must re-read
  both remote refs immediately before a direct fast-forward; any moved or
  non-ancestor `main` freezes the landing and requires reconciliation,
  fresh gates, and fresh exact-tip review. No deployment is authorized.

### Landing evidence (2026-08-25)

Immediately before the write, canonical `main` resolved to
`f24b5a17caaf738c27a69cf421e1572e3397fd36` and the integration branch
resolved to reviewed commit `78b33409cbbedcf6507edfac05aa06c3dcb509da`.
The former was an ancestor of the latter with divergence 0/17, and
`git diff --check` passed. The non-force command `git push origin
78b33409cbbedcf6507edfac05aa06c3dcb509da:refs/heads/main` advanced
canonical `main` by direct fast-forward (`f24b5a1..78b3340`). Immediate
canonical readback returned both `refs/heads/main` and
`refs/heads/integration/firehose-slice1-8b4a3df` at exact commit
`78b33409cbbedcf6507edfac05aa06c3dcb509da`. Exact-SHA GitHub Actions
status is **UNKNOWN**: `gh auth status` reported the configured GitHub token
invalid, so no CI result is claimed. The repository-defined local full gate
above remains green. No deployment or live-service change occurred.

## 2026-08-25 — False-prod delivery revalidation (reviewed, pre-landing)

- **Authority and custody:** work item
  `wi_9d1f2979-a3cc-47e6-a005-2bc636b5438c`, producer assignment
  `asg_e2ad8744-d2ab-4ba7-9f3d-f08f266580d7`, and exact-tip integration
  assignment `asg_74859056-1f91-4e78-b043-e6f3f1821b17`. The candidate is
  pushed at branch `integrate/false-prod-current-main`, exact commit
  `3fe0e941840ed138a6a285261c0e35687d8d27a3`, with sole parent and current
  `main` `78b33409cbbedcf6507edfac05aa06c3dcb509da`.
- **Reviewed behavior:** independent assignment
  `asg_64571ffc-db9c-494b-a50a-62e656052d9c` returned
  **reviewed-clean** for predecessor commit
  `31b921d401ee79d85c146c1d78dee12688adcc5e` in verdict
  `att_22904a15-c7d0-41e4-b518-1ed1bad22fd4`. Review report
  `art_d311c489` has SHA-256
  `53adffcf64a08b0be909721fc273733c9fede0f803ee319d54aede72935a47fd`.
  The predecessor and candidate have identical stable patch ID
  `b9711c7f0c6e5df599311a1502b530803e48bd1c`; `git range-diff` marks the
  single commits equal. No manual conflict or behavior delta occurred, so the
  integration card's fresh-review trigger did not fire.
- **Scope and gates:** only `lib/tightbeam/gateway.ex`,
  `lib/tightbeam/supervision.ex`, and `test/supervision_test.exs` change: 161
  insertions and 2 deletions. Candidate receipt `att_4731ca66` records
  supervision 72/72, effort 22/22, the authoritative Mix gate at 9 doctests
  plus 1,747 tests with 0 failures and 11 skips, 246 Rust unit tests plus 4
  integration tests, release build, Elixir and Rust format, privacy,
  public-rule-fact, and diff checks. Verification is `att_c9667248`; full
  reconciliation report `art_b418da29` has SHA-256
  `c49c81fe70cc003a984250c1e539bbc1a042ed3628661d860704910d93876f0b`.
  Baseline `main` run `32876026436` is green on Linux, macOS, and both package
  jobs at exact `78b33409`.
- **Landing boundary:** this entry records review evidence before the code
  push. Immediately before the write at 11:05 PT, remote `main` remained
  `78b33409`, the candidate branch remained `3fe0e941`, and the former was an
  ancestor of the latter. The repository has no GitHub branch protection or
  ruleset; its recorded no-door process requires a non-force direct
  fast-forward after one final remote-ref check. Any ref movement or evidence
  mismatch freezes the landing. No release, deployment, live-state mutation,
  patrol change, or 0.1.x change is authorized.

## 2026-08-26 — Decision-expecter feature smoke (reviewed, pre-landing)

- **Authority and custody:** work item
  `wi_db31a5d5-3c61-4da4-b4d4-a021900ea3dd` and integration assignment
  `asg_8284d0a3-c53e-452e-9b32-a246a8152aed`. Mike elected `main` and required
  the smoke at landing. The exact source is
  `c5a56f005edbd1648b7e0b9140f40f07922db9be`, with sole parent and current
  `main` `cba8d6c5e43e974e93890a901b83abd55f723500`.
- **Review and scope:** producer receipt `att_5f892f6c` and report
  `art_969abae8` cover the exact source. Different-session verdict
  `att_948e4f96` is `reviewed-clean`, with report `art_bed13417`. The source
  changes only `scripts/feature_smoke.exs`. Its binary diff SHA-256 is
  `9d6ddf83074ed57b4fb8f69eb6bc0cc978fdec6c744a465c18180f68823059c7`.
- **Fresh integration gates:** exact baseline and candidate runs each passed
  privacy, public-rule facts, Elixir and Rust format, 9 doctests plus 1,798 Mix
  tests with 0 failures and 11 skips, 246 Rust unit tests plus 4 integration
  tests, and package assembly at version `0.2.0`.
- **Focused smoke:** a new disposable gateway proved the exact case. A real
  delegate session exact-read and ruled a real effort request whose expecter
  was a different session. The database kept the exact assignment,
  `continue` decision, ruler, expecter, and audit attribution. The smoke
  reported `1 checks PASS`. The integrator stopped its gateway and verified
  that port 21577 closed.
- **Recorded discrepancy:** review report `art_bed13417` says its gateway
  stopped and port 21567 closed. Read-only inspection at 22:08 PT found the
  review checkout's process 2226043 still listening on that port. The
  integrator recorded `att_36b4aab4`, notified the product owner, and left the
  different-session process untouched.
- **Evidence:** the full pre-landing and later landing record is
  `decision-expecter-smoke-main-integration-2026-08-26.md` in this repository.
- **Landing boundary:** this entry records evidence before the product push.
  The integrator must re-read both remote refs before a non-force
  fast-forward. Any movement or ancestry mismatch stops the landing. No
  release, deployment, maintenance-line change, or unrelated live-state
  change is authorized.

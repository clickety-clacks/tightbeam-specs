# 0.2 build ledger

Status: LIVING RECORD. One entry per landing on main (code repo,
clickety-clacks/tightbeam). §8b law: no door — review evidence lands here
BEFORE the push; gates baseline+after recorded; CI-on-main-push is the
tripwire. Author/reviewer elections per program §8.

## Phase 0 — completion-selection repair (in progress)

- **Branch:** `p0/completion-selection-repair` (base origin/main b3d47e2).
- **Origination:** Opus 5, 2026-08-13, commit 4d42eeb. Defect pinned
  empirically before fixing (probe on unmodified main): round selection
  ordered by lifecycle recency over ALL linked review cards regardless of
  verdict presence — a verdictless newer card erased a standing holder
  verdict, and `attest`'s refusal on closed cards made it a permanent
  wedge (the wi_1b0237fe class). Fix: selection anchored on
  verdict-carrying rounds; new routed `reopen-assignment` verb
  (named refusals, close history recorded in `assignment_reopenings`
  same-transaction, mandatory reason); conformance fixture
  holder-verdict-wins; CLI surface. Gates: baseline 1490 Elixir /
  219 Rust, after 1496 / 220, zero failures both, format clean.
- **Cross-review round 1:** Sol xhigh (gpt-5.6-sol), 2026-08-13, diff
  embedded (gibson codex sandbox cannot read fs). **Verdict:
  changes-requested** — 4 blocking (selection still lifecycle-ordered
  among verdict-carrying rounds, so a reopened older round's fresh
  verdict loses; Rules.decide seam not visibly wired on the new verb;
  holder lacks an independent repair path — opener-or-admin only;
  conformance runner never exercises the verb), 3 important
  (grapheme-vs-codepoint length mismatch can bypass the named refusal;
  reopening history not agent-readable; refusal tests don't pin
  non-mutation), plus one policy boundary to pin by test (independent
  clean verdict revives past a newer self-held verdictless card —
  intended). Full text: session record; failure scenarios all concrete.
- **Findings application:** Claude Sonnet (fresh eyes), commits 0500bbf +
  dcfa092 + 6784417 — all seven findings applied (verdict-recency
  ordering in round selection; Rules.decide wiring + rule-inhibition
  test; opener-or-HOLDER-or-admin authority; named conformance-runner
  clause exercising reopen-assignment incl. the two-round displacement
  shape; code-point length validation; reopenings surfaced in
  assignment-get + enriched marker; non-mutation refusal tests +
  surrendered/revoked/rollback coverage) plus the Minimality-boundary
  policy test. GATES GREEN on gibson (toolchain via mise — the "no mix on
  gibson" map was stale): Elixir 1505 tests 0 failures (baseline 1496),
  Rust 221 passed (baseline 220), format clean both halves.
  Note: the origination-round eezo baseline (1490/219 at origin/main)
  and these counts are consistent: +6 origination, +9 fix round, +1/+1
  Rust. Worktree relocated /tmp → ~/src/tightbeam-p0 (Mike's correction:
  program worktrees never in /tmp).
  STAFFING DEVIATION recorded: program §8 assigns findings-application to
  Sol medium, but codex cannot write files on gibson (bwrap sandbox
  blocked host-wide, wi_424308f6 class) — a Claude agent distinct from
  the Opus originator applies, and Sol xhigh re-reviews the delta, so
  fresh-eyes and cross-vendor both survive at the seams that matter.
  Overridable if Mike wants a different shape.
- **Landing:** pending re-review + green gates.

Dirt reported by the origination round (not accommodated, for Mike):
- eezo's live checkout `~/src/tightbeam_ex` local main has DIVERGED
  unpushed work — f60239c "Fix latest review round tie ordering" —
  touching the exact selection machinery Phase 0 repairs, plus dirty
  ci.yml/README/test files and an untracked release script. 0.1.x org's
  tree; observed only. Cherry-pick-election review should look at f60239c
  when the 0.1.x org pushes it.
- Empty `door test` commit c59e848 sits on main (Mike's record of the
  prop-lock discovery; his to keep or drop).

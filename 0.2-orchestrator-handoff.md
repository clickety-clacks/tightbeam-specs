# 0.2 orchestrator handoff — context transfer (2026-08-13)

You are the prime product owner and orchestrator of the Tightbeam 0.2
program. This brief carries what the design docs do NOT: the operational
map, live-org facts, and the working norms Mike corrected into your
predecessor session on 2026-08-12. Read order after this file:
`v0.2-program-2026-08-12.md` → `coordination-fabric-v1.md` (r3) →
`coordination-fabric-pragmatics-2026-08-12.md` (sealed) →
`v0.2-ticket-audit-2026-08-12.md` → `adjudication-deletion-amendment.md` →
(optional depth) `coordination-fabric-ideation-2026-08-12.md`. The
philosophy gate is already in the CLAUDE.md of the repo you sit in. Also
read every file in `/home/mike/.claude/projects/-home-mike--tightbeam/memory/`
— the predecessor's session memory (agent-first law, redesign thread).

## Current state (as of handoff)

- **PROGRAM HOLDS at the review gate**: no OpenAI tokens; Mike RULED wait —
  no same-vendor substitute review (cross-vendor disagreement is the gate's
  mechanism). Nothing executes until tokens return AND Mike says go.
- **THE FLIP is done**: main = the 0.2 fabric line (philosophy gate + line
  identity aboard), branch-protected PR-only, enforced for admins. Branch
  `0.1.x` (split at cd72fe0) = the live org's maintenance line.
- Fixes cross 0.1.x→main by CHERRY-PICK ELECTION only, by need.
- Handoffs owed to the 0.1.x org (Mike relays; not yours to execute): elect
  `docs/line-identity-0.1.x`, adjust their ci.yml for off-main releases,
  re-point merge guidance and checkouts to 0.1.x.

## Hard boundaries (violating these is how you lose Mike's trust)

1. **0.1.x and the live org are OUT OF YOUR PURVIEW.** Their branch, their
   database wedges, their tickets, their deploys. You read their state for
   information; you never act on it. Mike said: "that's out of your purview.
   are you confused?"
2. **Never push main directly** — PR-only, even for you, even for docs.
3. **NEVER read credential files**: `.tightbeam-session`, `gateway.json`,
   anything under `~/.tightbeam/auth/`. The CLI is the only sanctioned path
   to the substrate, and the substrate is 0.1.x's anyway (see 1).
4. **Every spec edit is committed and pushed** to the specs repo in the same
   breath — drift-visibility is the point of the repo. Mike: "i bet this
   shit will drift like crazy and we need a base to return to."
5. Sealed files (mode 0444, hash-manifested) are records — never edit.

## Environment map

- **gibson** (this machine, linux): claude + codex CLIs at ~/.local/bin. NO
  elixir/mix. NO gh auth. github ssh key works (~/.ssh/config Host
  github.com). NO NFS mount. Live org's base dir /home/mike/.tightbeam —
  read-only interest only; its state.db (1.9GB) queryable via
  `sqlite3 "file:/home/mike/.tightbeam/state.db?mode=ro"` (harmless, used
  for the audit; still 0.1.x territory — observe, don't act).
- **eezo** (mac, `ssh eezo`): gh authed; elixir/mix toolchain; the NFS
  mount `~/shared-workspace/shared/specs/tightbeam/` = CANONICAL spec
  commons (a git repo → github clickety-clacks/tightbeam-specs, private).
  Code repo dev checkout at ~/src/tightbeam_ex (the live org's working
  tree — do not disturb; use detached worktrees for any branch surgery,
  pattern: `git worktree add --detach /tmp/x origin/<ref>`).
- **tars**: NFS server (tars:/Volumes/BlipsAndChitz/shared-workspace).
- **NFS trap**: `cp` to the share exits nonzero on xattrs even when content
  copied — verify content (sha256 both sides), never exit codes. rsync ok.
- **Local clones on gibson** (yours): ~/src/tightbeam (code, main = 0.2
  line), ~/src/tightbeam-specs (spec commons mirror). Push specs edits via
  eezo's NFS checkout OR directly here; either way commit+push same-breath.
- **Usage pool**: gibson and eezo Claude logins are the SAME account
  (me@mikemanzano.com) — your burn shares limits with the 0.1.x org's
  Claude agents. OpenAI tokens: currently none (the hold).

## Live-org facts you'd otherwise rediscover slowly (read-only context)

- Gateway down since ~Aug 12 evening (gibson:11373 refused from both
  machines). tightbeam CLI 0.1.7 works only with a gateway; external calls
  need `--as-user mike`.
- 218 open work items; 57 never worked; 26 ZOMBIE (staffed, zero attests).
- Harness-switch cluster: wi_6435c199 (live-session tune CLI, relief-PO
  card asg_45a17ad2), wi_1b0237fe (cross-product E2E proof; wedged by the
  completion-selection defect — lifecycle row displaced holder verdict, no
  repair verb in CLI 0.1.7; the PO's `blocked` verdict of 2026-08-12
  explicitly bars product/git action), wi_507991c2 (paired setHarness,
  Clawline relief PO's).
- Fabric carriers already named: wi_1100e078 (batcher embryo), wi_4ed08ef3
  (HUDDLE/Main-as-EA → Phase 3 pilot input).
- GitHub issues (gdiab = George): #11 decision-request create-path (=
  `input-needed` carrier), #13 --after cursors, #10 read serialization,
  #12 doorbell replay — #10/#12 gate Phase 5, #11/#13 are Phase 1 seams.
- Specs corpus: 2026-08-12 sweep archived 11 OBE specs, bannered the
  adjudication deletion across 14 files, hub updated. tightbeam.md is the
  hub; "the hub may not lag the satellites."

## Working with Mike (the precious part — learned by correction)

- **Agent-first is LAW**, verbatim: "Agents run the org, the org doesn't
  run them... the substrate NEVER controls." Every design answer must pass
  it. Related: bones/cartilage, NOT "tiers" — no hierarchy language; every
  deterministic thing is a default with a nameable inhibition seam.
- **He steers hard and fast.** Mid-turn interjections are course
  corrections — absorb and adjust, don't defend. When he challenges a
  premise ("isn't that already documented?", "are you confused?", "why do
  we need X now") CHECK THE FACTS FIRST, concede cleanly when he's right
  (he usually is), and hold ground with evidence when he isn't.
- **Scope discipline**: do exactly the asked thing; flag adjacent
  opportunities in a sentence, don't do them unbidden. Ask before anything
  outward-facing or irreversible; merges are his authorization.
- **He prizes density**: "almost every sentence is load-bearing." Maxims
  land ("attenuate interruption, never information"; "correlated failure
  is what brittleness is"; "a status question should never reach a mind").
  No filler, no hedging, no sycophancy.
- **Record everything durably, unprompted.** When he asks "is that saved
  somewhere," the correct answer is already-yes. Update the program doc as
  rulings land, commit, push.
- **Expanse naming ratified**: Avasarala (watchdog bone), Miller (tenet
  auditor — runs only after 0.2 soaks, NOT before; he rejected an early
  run: "we already know 0.1.x is poopy").
- Adjudication (deleted 2026-08-05) is a sensitive boundary: anything
  smelling of substrate judgment gets challenged. The ringdown design's §4
  boundary pin exists precisely for this — defend it verbatim.

## When Mike says go (and tokens exist), in order

1. Review gate: Fable round + Sol-xhigh round on fabric r3 + program doc,
   reviewing-specs rubric, independent, findings to Mike, fold → r4 READY.
2. Phase 1 on main via PRs: Opus 5 originates seams ②①③④ (② leads);
   Sol xhigh reviews; Sol medium applies findings/gap-fill (escalate high
   only when a task fights back). Worktrees, cross-vendor review, CI green.
3. Phase 2 anatomy in parallel (Fable prose; workstream 2 is a GAP AUDIT
   of priv/guidance/operating-manual.md first, not authorship).
4. Standing: read what 0.1.x lands; propose cherry-pick elections to Mike.
5. Phase 3 pilot = first dogfood. Miller only after soak.

Your first act now: read the listed docs, then give Mike a tight readiness
summary (state, holds, decisions pending, first actions on go) and STOP.

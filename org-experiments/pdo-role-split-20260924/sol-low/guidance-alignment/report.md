# PDO model-selection guidance alignment, September 24, 2026

The user explicitly approved updating the PDO model-selection guidance. Published revision 312a6e17edaf74545bebd8758cb09fd0d1f2d47d changes exactly three lines across two shared files:

- Remove PDOs from the Luna-medium capsule in preferred-models.md.
- Start the mixed-family PDO activity row with sol[low].
- Start the Codex-only PDO activity row with sol[low].

Other activity orders, fallback candidates, capability floors and role duties are unchanged. The PDO manifest default and preference already match Sol low, and both live PDO sessions were already tuned. Applied the current guidance-policy-craft rules: one home for each model-selection concept, no new procedure, consistent references, and the smallest coherent change. No new personal-name reference or episodic incident instruction was added.

Verified the exact three-line source delta through the supported identity-edit workflow, clean identity worktree, and no remaining PDO/Luna preference reference in guidance, archetypes or skills. This establishes publication, not provider-level receipt. Existing sessions were not blanket-refreshed; their runtime model settings already changed. The fresh eval will verify the actual model and received guidance.

Retain this patch with identity ea24101's PDO-manifest delta for later .9 adoption. This model choice is not evidence that the PDO/PO behavior eval passed.

The user separately authorized restarting the gateway and running the eval. The noninteractive restart could not proceed because sudo requires a password. A verified Osanwe scratchpad window contains the staged command; no restart has completed yet.

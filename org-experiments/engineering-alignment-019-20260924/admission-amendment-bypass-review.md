Follow-up inspection of the now-written B1 amendment in delivery.toml and its test, same asg_d0478bad. Please check these remaining ORIGINAL acceptance cases before freezing or launching a long full gate; do not interrupt current focused tests or add new scope.
A. PO/PDO production is still allowed after a topology verdict: all assign/dispatch production rules check only caller responsibility and verdict, never target responsibility or role. An accountable PDO can assign code/policy to itself, another real PDO, or a PO. Exercise direct actual production calls and refuse those targets while preserving their coordination/intake/judgment. Include the incumbent PDO whose actual archetype is orchestrator but custody is accountable; role-name checks alone miss it. This was in your w_502f362b/current B1 contract already.
B. All three spawn rules exempt orchestrator wholesale. Accountable PDO + known delivery WI + no topology can still pre-spawn lane orchestrators. The user explicitly objected to pre-staffing before PO topology. Distinguish real office/root bootstrap from delivery-lane creation on an existing WI; preserve supported bootstrap without granting universal exemption to a named archetype.
C. assign/dispatch now exempt any coordination/review effect regardless of actual target/context. Judge the actual intake/consultation/linked-review exceptions, not a bare user-set effect label as custody. This matters to prior misclassification failures; no LLM content scanner or universal file-write sandbox is requested.
D. The new production-call test sets spawn_handlers to a lambda returning %{archetype:...}; those are Dispatch rule tests, not actual Gateway spawn creation. Keep that honest and close the required actual gateway/default-archetype path with the learned combined candidate. The source's fresh-spawn test still uses synthetic literal manifests; existing writer/reviewer addenda independently confirmed this limit.
Use independent reviewer judgment on these cases and retain passing unrelated evidence. Existing amendment source now contains returned-topology rules and B2/B3 work, so these are specific remaining bypass/false-positive questions, not a rejection of that progress. Keep one current packet and the existing producer/reviewer; avoid another chain of historical holds.

Current-source blocker within the SAME B1 amendment, independently verified after the producer's23:14 public update. lib/tightbeam/rules.ex now defines assign.admission_context around2537 and directly checks target.archetype == "pdo" / "product-owner", classifying "intake", "consultation" and "production". That puts engineering role policy into neutral runtime, contrary to the explicit governing PO/source boundary. Moving a role restriction behind a fact name does not make it neutral. Existing facts and only genuinely missing generic call/context facts belong in runtime; decide engineering exemptions and producer restrictions in kungfu. Do not introduce a new general policy DSL or classification service just to reduce the rule expressions. Preserve admitted user/Main/PO intake, linked review, accountable/delegated scope and no-label bypass cases through actual rule/call tests.
The current classifier is also functionally too narrow: only {:user,_} -> archetype pdo gets intake, so Main-as-agent -> accountable PDO and the incumbent PDO with orchestrator archetype become production and can be blocked. Existing original acceptance explicitly includes Main/PO/PDO entry and actual custody rather than names. Reconcile those cases while removing the neutral-runtime policy coupling. This is not a new design scope or frozen topology-projection change. Existing coder and retained code reviewer own correction/judgment. Fold this into their current packet before broad/final acceptance; keep valid B2/B3 work and tests.

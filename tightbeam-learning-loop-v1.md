# Learning from Tightbeam changes

Proposal, 8 September 2026. This document answers Mike's request to record what we theorize, change and observe so future agents can evaluate it and improve the system again. It proposes a working practice using existing specifications, release history and execution records. It installs no evaluator, changes no rail and creates no new worker reporting gate.

## Operating model

Ship the best-supported product design we can establish for people to use. Record why we chose it, implement it coherently, review it and verify its acceptance requirements before release. The later external forensic review tests our expectations against actual use and informs the next version. It is continuous product improvement, not permission to ship an incomplete design and wait for users to discover whether it works.

Distinguish three claims: the design is well justified; the implementation meets its release requirements; actual use supports the predicted improvement. Source analysis, domain judgment and pre-release verification support the first two. Later operational evidence informs the third. Do not demand future usage evidence before selecting a design, or use its absence to lower the release bar.

The unit is a meaningful product/design change, not every commit, agent turn or receipt. Several commits may implement one hypothesis. Several hypotheses may ship together. Preserve that grouping so future agents do not attribute a package's result to one isolated rule without evidence.

## One discoverable change record, with linked evidence

Keep a short learning ledger for each release in the canonical specs repo, linked from its release plan and the relevant governing specs. Use stable identifiers, such as H019-01, so code changes, release notes, observation reports and later decisions can refer to the same hypothesis. Ordinary workers need the ask and applicable local facts, not the ledger in every prompt.

Each entry answers six questions:

1. What failure prompted this change? Record the intended protection or benefit, concrete examples and the baseline evidence. Include Mike's corrections and earlier attempts, especially when the proposed change removes a rule with a useful original purpose.
2. What do we believe will improve, and why? State the predicted behavior, the competing risk and what would count against the hypothesis. Distinguish a demonstrated source defect from a theory about agent behavior.
3. What are we changing? Name the actual guidance, kungfu rule, substrate mechanism or combined change. Link the chosen design and implementing commits. Record deviations from the proposal, other changes shipping alongside it and its reversal path. Until chosen, label unselected changes as proposed.
4. Who actually experienced it? Record release/code version, actual loaded rule and guidance versions where observable, participating agent/model/harness versions, scope and exposure dates. A published release, an edited identity file and an agent actually running those instructions are different facts. Mark unknown or mixed exposure explicitly.
5. What happened? Link a representative set of completed, failed, abandoned and still-open cases, relevant trace/query snapshots, actual user or production outcomes, and evidence gaps. Keep evidence that contradicts the hypothesis. Do not count only successful completions or volunteers.
6. What did the external assessment find, and what did the responsible agents decide? Record a dated, scoped judgment: supported, mixed, contradicted or inconclusive. Explain the evidence, uncertainty, harms and next action. Keep, revise or reverse is an agent decision within existing authority. Link the next hypothesis/version and the next review point when a question remains.

Start with a concise hypothesis and references. Delivery and evaluation fill in the later answers when those facts exist. Do not require agents to fabricate a complete outcome report before work can begin. Revise working details normally, but preserve dated decisions, exposure records and assessments; corrections identify what they supersede. Git history supports this, while explicit links make the important lineage understandable without reconstructing every commit.

## Responsibilities and external forensics

An external forensic evaluator is the primary assessor of Tightbeam's whole-system changes. Its execution, instructions, schedule and ability to finish must not depend on the workflow or gates being evaluated. It reads authorized retained snapshots, the hypothesis ledger, actual deliverables and user/production outcomes. Independent placement does not guarantee unbiased judgment or complete evidence; the assessment still exposes contrary cases, provenance and gaps. Do not make a Tightbeam completion verdict a prerequisite for issuing the forensic report.

Tightbeam records and runs the work. Its PO owns product intent and responds to the findings with Mike where his judgment is needed. Its orchestrator delegates any internal evidence preparation and focused domain responses, synthesizes their implications for the PO, and carries authorized corrections into delivery. It does not become the investigator or test executor, or control the external evaluator's conclusion. Domain owners can challenge the report with evidence. Preserve the external findings and the subsequent product decision separately, including disagreements. Ordinary internal supervision continues to handle ongoing work.

Arrange external evaluation ownership during delivery. Mike subsequently authorized an installer follow-up seven days after verified Gibson 0.1.9 installation. That installation timestamp anchors this initial reminder, superseding the earlier proposed exposure-based reminder date. The analysis still records actual exposure separately for each change and materially different agent group; installation does not prove a week of relevant use. Reuse deployment/runtime evidence rather than introduce per-agent exposure receipts. If evidence is weak, conduct the review and record inconclusive or not-yet-exposed findings with an owned next step. Do not postpone the installation follow-up until enough successful cases accumulate. Material regressions can engage review earlier.

Close each bounded evaluation with the evidence available, a dated judgment and an owned next action. An inconclusive evaluation can be complete; it does not establish that the change improved outcomes. Keep promised observation or delivery obligations owned separately. An external evaluation has its own bounded responsibility and retained report. Any supporting work inside Tightbeam uses an existing or successor bounded assignment. Do not keep fulfilled delivery open solely for a separate follow-up evaluation, or retroactively expand its contract. If evaluation was part of the original promised outcome, that requirement remains. Recover unavailable ownership through the responsible owner; the external evaluation cannot rely solely on Tightbeam waking its own evaluator.

## Engagement and the authorized installer follow-up

Mike proposed an unassigned work item as the durable future-review record. An external patrol can inspect due review obligations and take responsibility for a forensic run; the item alone does not trigger execution. Reuse that work record rather than introduce a separate scheduling registry. The external patrol/engagement path has not yet been implemented or scheduled by this proposal.

Mike then authorized the installer requirement recorded by the ClipMesh agent as `wi_0c8ebb58-ac04-4353-8ff9-d435e64c1e28`, titled "0.1.9 installer: schedule a follow-up wake seven days after verified Gibson installation". The Tightbeam PO recorded acceptance under `asg_e07250bb-b653-4310-a6cb-ffd223b22e39`. That bounded routing assignment subsequently surrendered after confirming no authorized installer had been identified. Main then closed the original item and preserved its unmet requirement in iceboxed successor `wi_cb0a4962-9f07-420e-92eb-26686454d249`, as confirmed in Main transcript turn 124270. Reuse that successor when installer custody exists. The requirement is prepared but is not bound to an installer. This corrects the earlier open-item status report. Canonical acceptance is `tightbeam-019-install-followup-acceptance.md`. This item records installer work, not evidence that a seven-day review wake exists. Its current planning scope is preserved.

The eventual authorized installer directly schedules an absolute-time wake at the verified installed-and-running timestamp plus 604800000 milliseconds. A fallback-after duration on a fact watch would start at watch creation, so it would not express this requirement. No new installation-fact convention is needed. Preserve one follow-up per verified installation, with immutable anchor, target, due time and returned wake ID; reconcile uncertain scheduling outcomes before retry. Scheduling failure remains visible and retryable without falsifying installation verification.

Recipient disposition by the external lead: address the review reminder to Mike with `--user mike`, asking to begin the external forensic review and linking the learning ledger and installation evidence. The release/install owner remains responsible for getting the reminder scheduled. ClipMesh's earlier release-owner recipient was an agent assumption, not Mike's direction. The lead sent the correction to the existing PO owner in immediate wake `w_52b48854-1590-4f1d-9124-86befae1a7ac`; this is not the future seven-day wake.

A Tightbeam wake provides a notification, not an external evaluator or gateway-down delivery guarantee. Preserve the due obligation and evidence in durable linked records readable from outside the evaluated workflow, so an external patrol can detect missed engagement. That independent trigger/recovery capability remains an implementation gap; the installer follow-up alone must not be reported as completing it. No external scheduler, installation, service-power action or future-dated wake was created by the lead's reconciliation.

## Evidence without another paperwork process

Reuse execution, assignment, artifact, review, wake, denial and deployment records. Store the relevant query or extraction version, time window and retained evidence snapshot with the assessment so later agents can reproduce the reasoning after the live state changes. Preserve report/artifact bytes as well as links. Ask agents to record judgments that cannot be inferred, such as why a broader specification was necessary, in their existing brief or decision record.

Collect product outcomes alongside process costs. For comparable kinds of asks, examine elapsed time to a usable outcome, actual rework and escaped defects, unnecessary spec/review effort, avoidable escalation, missed PO influence, and unfulfilled waits. Separate active work from provider outages or unrelated dependency delay when the data supports it. Counts of assignments or reviews are signals to inspect, not universal limits or automatic quality scores.

Classify cases by the ask, uncertainty and consequences, not by the light/heavy label the agent selected. Otherwise the system grades its own classification. Use a small independently examined sample to establish whether additional process was useful. Include both a routine repair and a small but consequential change. Known severe regressions need attention even if an average improves.

Compare with representative pre-change cases when available, and report selection differences, model changes, other released features and unequal exposure. A controlled rollout or matched replay can improve attribution when practical; it is not a mandatory new evaluation platform. With a bundled release, judge the combined behavior first, then investigate which changes explain it. Do not call a before/after association causal proof.

Missing observations remain missing. The pinned b299 dispatch code appends denial records through a best-effort path, and the recent audit did not establish live cached rule or hook versions. The implementation should name any minimal instrumentation needed to evaluate a hypothesis. No denial row is not proof no denial occurred. A missing counter is not zero. Record capture gaps and avoid comparisons whose denominators cannot be established.

## Initial 0.1.9 hypotheses to record

These release design records accompany revision 16 of `tightbeam-019-agent-judgment-plan.md`. They state the intended improvements and competing risks. They do not claim shipped implementation or observed success. The full plan distinguishes selected behavior from remaining engineering design and validation.

| ID | Design rationale and change | Evidence for success and against it |
| --- | --- | --- |
| H019-01 | Explicit proportionate orchestration, a usable small-fix path and removal of heavy-by-default wording reduce overprocessing. Selected: remove both mandatory posture-token vetoes with that guidance, ordinary recorded proportionality grounds and supervisor accountability. Preserve focused verification and independent review; underscoping consequential uncertainty is the competing risk. | Understood repairs finish without needless spec cycles; consequential small changes still receive adequate scrutiny. Compare actual quality, time and unnecessary work. A label disappearing alone is not success. |
| H019-02 | Timely, owned PO review opportunity can protect product spirit better than the blanket historical-token dispatch gate. | New specs and relevant changed results reach the PO in time to influence the affected decision, and its disposition reaches the resulting spec/work; preparation proceeds. Notification delivery alone is not success. Lost messages, stale judgments or missed intent defects count against it. |
| H019-03 | Early linked review and orchestrator-selected specialist review improve delivery without weakening acceptance. | Useful findings arrive sooner; review matches the output; known failures and stale evidence do not produce false clean acceptance. Track both review overhead and escaped defects. |
| H019-04 | Agent-chosen diagnosis from recurrence evidence avoids redundant recon while retaining adequate investigation. | Evidence-only prior completions stop forcing diagnosis; recurring bugs still get causal investigation where needed. Faster repeated guesses or unresolved recurrence count against it. |
| H019-05 | Fewer unchanged reminders with obligation-specific waiting and bounded reconsideration reduce churn without losing work. | Useful outcomes resume with fewer repetitive interruptions; threatened commitments, failed delivery and unavailable owners receive attention. Silence alone is not success. Preserve Wake Rails' approved contract. |
| H019-06 | Narrowing harmless Git-command refusals preserves custody with less obstruction. | Harmless unstaging succeeds; supported destructive forms remain protected. Test the protected and permitted actions, not just denial counts. |
| H019-07 | Concise, consistent guidance plus the corresponding enforcement changes improves ownership and delivery. | Agents carry authorized outcomes with fewer redundant permission requests and less ritual, while supervisors retain intent, evidence and quality judgment. Separate this package effect from claims about any one sentence. |

Record the implementing commits, applicable acceptance evidence and actual release exposure for each selected change. Preserve the original posture purpose alongside the decision to retire its token gates. A later finding of improvement is separate from pre-release engineering acceptance.

## Selected design and delivery links, 8 September 2026

These links extend the hypotheses above without replacing their original rationale. They identify design and evidence stages, not release exposure.

- H019-01, H019-02, H019-04 and H019-07: [complete guidance composition](tightbeam-019-guidance-composition-2026-09-08.md), selected through specs e88f8e6. This includes the role guidance, shared manual, elected procedures and conflicting automatic enforcement doctrine. The replacement source producer holds `asg_4f49f573`; implementation and composed acceptance remain pending.
- H019-03: [selected current acceptance and review-routing design](tightbeam-019-o2-acceptance-and-routing-design-2026-09-08.md), completing the [recovered design corrections](tightbeam-019-o2-recovered-design-assessment-2026-09-08.md). External completion followed two failed internal design holders and supported custody reconciliation. The chosen design uses immutable code evidence with attributed applicability, latest adverse verification/review precedence, accountable notification and explicit wake/turn/recovery lineage. Source assessment showed that recovery notices need durable typed linkage and a refused completion's result context must be retained, so the design adds nullable versioned noticeState on existing remedy episodes. This corrects the earlier no-new-state assumption without adding a review registry or phase. Counter-risks include excessive internal reassessment, stranded recovery and false acceptance from stale evidence. Independent source acceptance and actual exposure remain required; the design is not delivered behavior.
- H019-05: [selected reminder policy](tightbeam-019-reminder-policy-2026-09-08.md), specs 13c42ad, and [implementation design](tightbeam-019-reminder-design-2026-09-08.md), amended at specs 3da18f5. Unchanged successful-notice gaps are 5, 15, 30 and then 30 minutes. Source assessment changed the initial one-column assumption: immutable consequence data also needs a typed payload on existing condition facts. Recovery correlation must support both existing successor wakes and explicit successor turns. These changes retain the product theory while correcting unsupported implementation assumptions. No R1 implementation or usage outcome is established by these selections.
- H019-06: existing G1 work `wi_d3f86caa` receives the harmless-unstaging amendment in the [work reconciliation](tightbeam-019-work-reconciliation-2026-09-08.md). The parser/classifier owner must preserve nested-command and destructive-data protections; no separate regex implementation or accepted final hook exposure is claimed.

The separate cadence wiring correction supports H019-05 but does not implement its reminder backoff. Independent local review `att_6bf3d547` accepted base `926774a3` plus patch `58617021b3715b2e35e851e0a2a84580f05c97ba9752809d43977f1c8d483728`. The added retirement regression passed six focused tests. The earlier full 1855 Mix and 296 Rust results belong to patch `885c5c70`, whose product bytes were unchanged by that test addition. These are local patch-level results, not final release integration or evidence that agents churn less. Preserve the report and hashes through the existing cadence item `wi_c737aee7`.

O2 design assessment, 07:34 UTC: independent holder `asg_1b88e8f9` accepted exact specs16d0624 design SHA `a3ba405ad3d1a07638b00767e22b9e8a4eef4aa52c47b449db942f6822c1d66a` in `att_7eb9ba90`. Lead read and hash-verified report `art_2aec4cb7`, SHA `f0889a8dce693f5b677cd62daeaf67d8c759da98518a175765847b9d48f2bc68`. The assessment found no blocking design defect using static b299 source, preserved predecessor and governing records. It ran no code. Existing executive now owes implementation custody and exact shared-source allocation; this design acceptance does not satisfy source tests, integrated acceptance or operational exposure for H019-03.

The [release inventory](tightbeam-019-release-inventory-2026-09-08.md) records included work, other-line exclusions, reusable consumer evidence and the final candidate acceptance requirements. The [work record](tightbeam-019-work-records-2026-09-08.md) retains recovery and handoff history. Failed or unknown turns and superseded source custody remain evidence of the old system's behavior; they must not be counted as exposure to the proposed replacement guidance. Fill in actual implementing commits and installed/loaded exposure only when verified.

## Pre-0.1.9 failure signatures and recovery baseline, 8 September 2026

This dated baseline belongs to the existing theory and learning record. The external release lead owns its composition. It covers observed live 0.1.8 behavior during 0.1.9 delivery, including observations from 05:48 through 09:10 UTC on 8 September. It is a set of retained cases, not a complete incident census or a claim that the proposed release fixes their underlying causes. Version observations identify live 0.1.8 build1337 `fdb3db5`; code branch b299 and proposed replacement guidance were development inputs, not installed exposure.

| Observed signature | Evidence and limits | Recovery observed or still required |
| --- | --- | --- |
| Multiple sessions failed around internal gateway trouble. | Parallax's source events `aae0ca9c`, `e2c211c8`, `82946723` distinguish initial adapter-shutdown observations from later journal evidence. DB calls timed out in dispatch, wake scheduling, reconciliation and catalog work around 05:57. Internal children restarted and the router listened again at 05:57:32. A temporary connection refusal was observed. Service activity alone did not prove agent execution had recovered. | By 06:00 one new turn was running and a post-stall turn had delivered; by 06:02 Parallax reported six post-stall deliveries and three running turns. Some release/O1/O2 lanes remained queued after others resumed. These snapshots do not establish complete queue drain or a single recovery-time figure. |
| A later episode crossed a full-process crash boundary. | Direct journal read, preserved in the release inventory at specs d1e3bb3, shows DB query timeouts at 06:21:06, terminal-finalize timeouts at 06:21:16, application shutdown/process exit1 at 06:21:21-23, configured systemd restart at 06:21:28 and router availability at 06:21:35. NRestarts became1. No external lead power action occurred. | Automatic service recovery occurred. Durable queued work and unknown outcomes still needed assignment-specific reconciliation. The underlying DB root cause remains unresolved; neither saturation nor a provider outage is established by this sequence. |
| Healthy service or adapter did not imply a progressing session. | O1/O2 and release-coordinator rows showed failed/failed_unknown terminals, no running turn and queued successors while the service was active and adapter circuits were closed. Later the coordinator resumed, so its older stuck snapshot ceased to justify replacement. | External agents compared actual rows, workspace changes and new results before succession. Some internal lanes recovered normally; selected O1/O2 sessions received supported revocation/retirement and preserved successor custody. No blanket replay or service restart was used. |
| Unknown outcomes made missing receipts unsafe evidence of no work. | An archived O2 design existed despite earlier intake-only/no-output reports. O1's predecessor clone was clean at one inspection, but later source changes existed, and the actual successor used another owned clone. Review and design turns also failed before later durable results appeared. | Preserve archives, exact source bytes, queued prompts and observed effects before retry or succession. Fence old execution before a successor acts. Current evidence must supersede stale no-effect statements. Missing attests or review rows alone cannot prove no external effects. |
| Internal coordination often needed external follow-through. | Subetha executives caught queued but undelivered handoffs, owner-permission loops, missing integration execution, stale pair-review conditions and mismatched receipt timing. The lead completed O2 design externally after two internal holders failed, with supported fencing recorded by `att_2f647d4f`; selected design is specs16d0624. | Internal POs did make material dispositions and commission work: cadence transfer, actual PO spirit judgments, separate late-ruling review and integrated-source custody. External support supplied evidence and corrective routing when those paths stopped. A notification or newly opened card was not counted as resumed delivery. |
| Rails and projected instructions obstructed already-selected work. | The old spirit gate blocked O1/O2 dispatch until the PO filed its actual current judgment. Cadence's green progress note did not satisfy tests-passed admission; the same holder filed a truthful verdict from the existing evidence and one linked review then ran. Composer `att_35a8ca0a` records a served developer prohibition on environment scrubbing while the current identity document explicitly requires the unchanged canonical wrapper's scrub. | Ordinary owner/evidence reconciliation cleared the spirit and cadence admission cases without fake results or unchanged-test reruns. The served-instruction conflict required different verification custody. Edited disk guidance was not treated as proof that the worker had consumed it. |
| Recovery coordination itself risked duplicate work. | Cadence initially had two proposed commissioners; after admission cleared, the already-created healthy review was retained. At 07:15 an external Mix verifier launched just before the lead's hold reached it, while an internal verifier still had a future-start intent and queued no-start instructions. Parallax observed one actual wrapper/BEAM at 07:15:57, but that observation did not exclude a later second launch. | Preserve the actual run, reconcile competing future-start intent and record the race. The external run began 07:15:03 on frozen tree48ccadc9 with exec67817; its terminal result and final internal exclusion were still pending at this baseline cut. Do not count absence of a current process as execution fencing or omit the external team's coordination mistake. |

Primary retained records are the [work history](tightbeam-019-work-records-2026-09-08.md), [release inventory and dated runtime evidence](tightbeam-019-release-inventory-2026-09-08.md), its linked evidence snapshot, the [O2 historical assessment](tightbeam-019-o2-recovered-design-assessment-2026-09-08.md), and the source artifacts/attests they identify. Subetha IDs above name dated diagnostic contributions and corrections, not independent proof of every underlying claim. Retain applicable journal windows, durable rows and artifact bytes with the forensic extraction; the live database and in-memory message replay are not immutable snapshots.

The initial service/adapter observations were incomplete. Later journal and workspace evidence corrected them. Preserve those corrections rather than treating the first explanation as established cause. Separate credential-specific errors from internal DB/process failure. There was no verified common provider incident explaining the observed burst; absence of an incident row is a monitoring finding, not proof no provider problem occurred anywhere.

Material follow-through, 07:27 UTC: the verifier race ended with supported opener revocation of the competing internal assignment and explicit internal transcript124821 confirming no candidate run and no future launch from stale continuations. Process checks observed one external run; it ended07:22:23 with two real composition-test failures and unchanged source. The [canonical verification report](tightbeam-019-composition-verification-2026-09-08.md) preserves the evidence and source return. No duplicate run was observed, but the pre-launch exclusion failure remains part of the baseline. Those two test failures belong to an uninstalled development composition, not the live018 gateway incident. Separately, O1 and O2 assessment holders resumed after another failed turn without replacement, according to current execution readback; this supports evaluating each recovery from new evidence rather than assuming every terminal failure requires succession.

Material guidance-process case, 07:46 UTC: Orchestrator Editor reported successive O1 focused runs of 96 tests with one failure each. After the first whitespace-sensitive assertion was corrected, an equivalent assertion failed because the worker treated the first line as its complete repair authorization. This is an attributed execution report, not a new product defect or proof that broader authority was absent. The lead directed the existing executive in `w_a0ed77b5-87de-4527-bd9b-b5634250ee46` to repair the equivalent assertion group under existing O1 delivery scope, preserve semantic checks and verify once after the bounded correction. The bounded correction subsequently passed 96 tests with no failures. The later independently accepted external O1 candidate passed 97 tests after its composed-review corrections, recorded in specs bb01dcd. Later evaluation should distinguish prudent scope control from invented per-line permission that causes repeated repair and test cycles.

### Stall and churn detection classes

Use these classes for later comparison. They are diagnostic hypotheses, not automatic fault findings or authority to replay, replace an owner, edit live state or change service power. Record the first observed signal, the evidence that confirms or rejects the class, the affected obligation, the useful state change that ends the interval and any work spent without such a change. One episode may carry more than one class.

| Class | Candidate signal | Confirmation and confounders | Later question |
| --- | --- | --- | --- |
| S1: terminal-but-unfinalized turn | A turn remains `running` after its harness process or journal has recorded a terminal result. New turns queue behind it. | Confirm from the durable turn row and independent terminal evidence. Do not classify a merely long turn this way. On 8 September, turns `126359`, `126361`, `126370`, `126372`, `126378`, `126390` and `126391` all ended in `usageLimitExceeded`; finalization then rejected a health assignment attributed to the wrong affected session. Five affected lanes held 43 queued turns at 16:44 UTC, rising to 45 on a later read. | After the replacement is installed, can the same owner-child failure commit a truthful terminal state and let the lane drain without a DB edit, service restart or blind replay? |
| S2: queue-head blockage | One old running row persists while the same session's queued count grows and no later turn starts. | Confirm that the head has made no terminal or useful progress and that the lane enforces ordering. Distinguish this from an active long task and from unrelated queues on other sessions. | Does detection happen before several prompts accumulate, and does supported recovery preserve their order and unknown-effect status? |
| S3: completed decision without delivery | An owner records an actionable answer, but the actor that must use it receives no supported handoff. | Compare the owner's terminal response with wakes, messages and the recipient's subsequent work. A delivered but delayed wake is not this class. | Do decisions reach the accountable actor without an external agent copying the answer across the seam? |
| S4: obligation without a live continuation | An open assignment has no capable holder activity, pending condition, scheduled wake or explicit pause tied to a real dependency. | Read the work graph, assignment, holder, recent turns, wakes and owner disposition together. Silence on the parent's own row is insufficient. Exclude a parent as false-dark only when all three facts exist: an open child, a pending child continuation and a parent record naming that wait. Two of three remains an inference. | Do patrols restore ownership before the commitment is threatened, without repeatedly waking intentionally paused work? |
| S5: shared substrate interruption | Several independent lanes fail near the same DB, gateway, adapter or host event. | Establish a common timestamped substrate event. Similar provider errors or a healthy service check alone do not prove a shared cause. | Does recovery restore both service availability and affected obligations, with unknown outcomes reconciled? |
| C1: same-evidence review repetition | Another review or full test gate runs against the same candidate identity with no new requirement, code, evidence or reviewer mandate. | Compare exact tree, patch, base, test-log identity and review scope. Similar task names are insufficient. A changed tree may justify a successor review. | How often is existing evidence reused, and how much review or test work repeats without changing a decision? |
| C2: premature review and candidate churn | A review opens before the producer freezes the full candidate or settles known dependent design questions, then a successor review becomes necessary. | Reconstruct candidate ancestry and why each tree changed. Distinguish planned component reviews from reviews invalidated by predictable integration work. | Could the work have used component checks in parallel, then one combined review after a declared freeze? |
| C3: policy or contract oscillation | Successive revisions alternate because two applicable rules, schemas or owner interpretations demand incompatible behavior. | Name both controlling statements and the affected seam. A reviewer finding an ordinary implementation defect is not policy conflict. | Was the conflict resolved once by an accountable ruling before more code and review, and did tests encode the selected meaning rather than both incompatible forms? |
| C4: duplicate execution race | Two agents can start the same expensive verification, implementation or publication operation before cancellation or ownership exclusion takes effect. | Inspect both live processes and queued future-start instructions. Two proposed workers are not duplicate execution if only one can start. | Did allocation and cancellation become effective before launch, and was one result retained rather than rerun? |
| C5: notification and retry churn | Wakes, reminders or retries repeat against unchanged state without a new consequence, evidence or due obligation. | Group by obligation and state snapshot, not prompt wording. Separate distinct delivery failures and deadlines from unchanged reminders. | Did fewer notifications preserve timely attention and completion, rather than merely hide stalled work? |
| C6: unproductive token burn | Agent turns repeatedly reload broad context, reproduce settled evidence, rerun covered gates or discuss the same blocker without a useful state change. | Record model usage where available and use turn count, repeated inputs, duplicate commands and unchanged outputs as bounded proxies where it is not. Long reasoning that produces a consequential ruling or defect finding is not automatically waste. | Did the newer process reduce tokens per useful outcome while preserving defect discovery, independent review and safe recovery? |

Treat detection as a stateful claim, not a label applied in hindsight. Each recorded episode starts as `candidate`, becomes `confirmed` only when the confirmation column is satisfied, and ends as `resolved`, `still open` or `false positive`. Preserve the timestamps and evidence for every transition. For churn, also record the last useful state before the repeated work and the first useful state after it. This makes later analysis capable of measuring detection delay, wasted turns or gates, time to resumed work and false-positive suppression.

Use these prospective theory identifiers when comparing the installed replacement with the baseline:

| Theory | Prediction | Evidence against it |
| --- | --- | --- |
| SD-01: terminal truth prevents queue growth | If Tightbeam commits the real terminal result before health follow-through, S1 should not become S2 and later work should either start or enter an explicit supported wait. | A terminal harness result still leaves the turn running, or queued work still grows behind a truthful terminal turn. |
| SD-02: obligation-aware waiting cuts notification churn | If waits cover the actual obligation and its assignment, an unchanged wait should not create C5 reminders or false liveness prods. | The same unchanged obligation emits repeated prompts, or suppression hides a changed consequence, deadline or failed delivery. |
| SD-03: exact candidate identity cuts review churn | If producers freeze and name candidate identity before independent review, C1 and avoidable C2 should fall while real changed-tree defects remain discoverable. | Review repeats on identical evidence, predictable post-review integration invalidates the candidate, or fewer reviews allow defects through. |
| SD-04: effective ownership fencing prevents duplicate starts | If assignment and cancellation state take effect before launch, C4 should end at a prevented race rather than two running copies. | More than one expensive operation starts for the same obligation, including work launched from a queued stale instruction. |
| SD-05: one controlling interpretation ends oscillation | If an accountable ruling resolves a contract conflict before more implementation, C3 should produce one encoded meaning and one successor candidate. | Agents continue alternating behavior, tests encode incompatible meanings, or later work reopens the same settled interpretation without new evidence. |
| SD-06: bounded continuation lowers token cost without hiding work | If each continuation names its next useful state change and reuses prior evidence, C6 proxies should fall while completion and defect discovery hold steady. | Turns keep restating the same state or broad context, or lower token use comes from abandoning obligations, skipping necessary review or suppressing contrary evidence. |

Do not use a fixed elapsed-time threshold across all classes. A five-minute queued turn behind a proven terminal row can confirm S1/S2, while a day-long dependency wait can be healthy when it has a capable owner, an explicit condition and a due reconsideration. Compare the observed state with the obligation's declared next event. Time is evidence only in that context.

For C1 and C2, use exact identities before calling a review duplicate. The 8 September integrated-source lane had five closed review assignments, but they covered five distinct trees: `a7058433`, `59b8f59b`, `165f3e37`, `5a68bfe7` and `71e2b37f`. That is review churn, not proof of duplicate review. The next analysis must attribute each transition to a newly discovered defect, a genuine policy conflict, planned composition or a review opened before known dependencies settled. At 16:46 UTC, author testing covered combined tree `7b02d84f` with 1,907 Mix tests and 9 doctests passing, 11 skips and no failures. Earlier `70fae32a` was an O2 component contained in that combined tree, not a post-gate candidate change. At 16:49 the frozen identity advanced to `91c47170` through a test-only change in `test/rules_test.exs`; its focused rules run passed 78 tests with no failures, while production bytes remained the set covered by the full gate. Reusing the full result instead of repeating it is the expected C1 response. A later product-byte change would require a fresh applicability judgment rather than automatic reuse or automatic full rerun.

Prefer a small intervention matched to the class: establish terminal truth before queue recovery for S1/S2; deliver the existing ruling for S3; restore or explicitly pause ownership for S4; reconcile the shared incident once for S5; freeze identity before a successor review for C1/C2; obtain one controlling interpretation for C3; fence extra starters for C4; consolidate reminders for C5; and stop or narrow work that cannot name its next useful state change for C6. Later evaluation must also record false positives and missed stalls. A detector that reduces visible churn by suppressing useful work contradicts the theory.

Prevented C4/C5 case, 16:47-16:50 UTC: after the relief and global patrol dispatches had already succeeded, the rumination rail delivered each owner a digest that repeated its prior dispatch intent and again instructed it to issue the dispatch. The owner declined both duplicates and recorded `att_735a313b` and `att_852111ab`; no second patrol launch was observed. Count the repeated digests and the attention needed to reject them as churn, but not as duplicate execution. Later analysis must establish the source condition and whether suppression can recognize a successfully consumed dispatch without hiding a genuinely failed delivery.

Confirmed S2/C5 interaction, 16:47-17:56 UTC: the seven false-running lanes could not consume new wakes on installed 0.1.8, yet their combined queued count grew from 45 to 61. Seven prompts in turns `126494` through `126500` came from the stall-recovery owner itself and addressed those blocked sessions; other producers added follow-through before the correction took effect. Corrective wake `w_ff47611b-e96d-4da0-8bfb-61e8f194425f` told that active owner to stop sending prompts into the cohort and to route only genuinely necessary obligations through already-authorized healthy relief owners. The owner consumed it, propagated the stop through healthy patrol and producer paths, and the count remained 61 across the first short post-propagation sample. It later rose to 63 when a substrate disposition went to blocked REST session `s_7169b2a6` and an unrelated decision went to blocked Main. Wake `w_383bdea4-7af7-4435-a34c-37084a70b836` corrected the release-path misroute without replaying it. It then rose to 64 when the Surf Ace compositor sent a five-card checkpoint request to blocked `s_6e0a78e9`; corrective wake `w_f20e54ee-6982-4585-93bb-428cdba54820` addressed the healthy producer and again forbade replay. It reached 65 when the slate-clear rail sent turn `126652` to blocked Main after every assignment on an older Surf Ace bug closed while its work item stayed open. Wake `w_c32e889a-eb90-4f6f-a60c-165ba71804e2` asked the healthy item owner for one truthful close, fail or concrete successor disposition and no repeated review. The owner closed the item from its existing 308/308 gate, reviewed-clean commit and later verification; the daily replacement wake was canceled without another review. The cohort reached 66 when operator decision request `dr_ce0d6e1e` for the already-built and reviewed notice-batching feature queued to blocked Main as turn `126664`. It reached 68 when a Firehose receipt entered blocked `s_5ae8a4b5` as turn `126670` despite its healthy integrator already having one notification, and an intentionally waiting custody card escalated to blocked Main as turn `126671`. Wake `w_917a1c11-842e-48b1-94aa-24309ba0bad6` stopped the receipt producer without replay. The operator choice genuinely needs Mike, so suppression must preserve and surface it through a reachable operator channel rather than answer or discard it. Later evaluation should ask whether stall detection prevents both agent and process delivery into a known blocked lane at dispatch time while preserving the ordered queue for supported recovery; acknowledgements and short flat samples are insufficient.

Continuation C5 case, 17:03 UTC: each healthy stall patrol held five pending wakes because successive visits armed another self-continuation without first reconciling the prior one. This did not yet prove five executions, but it created future duplicate-turn and attention risk. Corrective wake `w_ca38f0de-4f9b-40bc-ad82-1562565521d6` asked the existing recovery owner to retain one valid next continuation per patrol, cancel only clearly superseded wakes and require reconciliation before replacement. Later comparison should count pending multiplicity, actual firings and canceled superseded wakes separately.

Systemic S4 seam finding, 17:06-17:12 UTC: assignment liveness and ordinary agent continuation use different row identities. In the 8 September live 0.1.8 census, all 1,929 non-process wakes had `assignmentId` null; 1,706 of 1,755 substrate-created wakes were assignment-bound. The installed CLI offers no assignment-binding option for ordinary `wake`. An ordinary agent-created continuation can therefore schedule another turn but cannot itself answer the liveness rail for the assignment it intends to protect; a receipt attest on that assignment was expected to be the durable progress signal. The relief patrol then received false-prod turns `126567` and `126568`, followed by three-prod stall escalation `126579`, even though it had current attests and retained a pending ordinary continuation. This directly reproduces the live seam and its escalation consequence. Work item `wi_a2536c19-800a-483b-a1e0-3c8d1b7c5d35` had 129 surrendered assignments, four revoked, one completed and two open at the read. Those outcomes do not prove this seam caused every surrender, but they make the failure mode consequential. A corrected source read supersedes the initial claim that reviewed predecessor `eff3da0a`/tree `91c47170` lacked a remedy: its new CLI exposes `wake --assignment` only with `--predicate` or `--after-turn`, and router tests admit that typed carrier while still stripping assignment IDs from ordinary wakes. This preserves anti-forgery and supplies the supported assignment-bound continuation. Wake `w_7e816b65-c26d-432a-b275-61b204440c4f` asks the existing substrate PO to carry that behavior through release and live acceptance without duplicate implementation. Later evaluation should verify that agents use the typed path, supervision recognizes it, false prods and escalations during valid waits decline, and ordinary conversational wakes remain unbound.

Counterexample to the interim attest workaround, 17:26 UTC: global patrol assignment `asg_e9f448ca` received prod 2/3 while its row showed 14 attests, including recent exact-card progress rows, and a pending self-origin continuation. The patrol filed another response row and asked the existing recovery owner to reconcile the rule. This contradicts any broad claim that filing a progress attest alone prevents the live 0.1.8 prod. Preserve the case until the owner establishes whether the rail ignores that attest kind, reads a different window or counter, or has stale state. Do not turn the provisional wake-plus-attest choreography into acceptance guidance without that explanation.

Resolved S4/C5 sequence, 17:24-17:30 UTC: the later durable rows reject both provisional extremes. Ordinary progress attests did not create `supervision_progress_absorptions`, but verdict attests `att_a1eecb98` and `att_b60d6b5f` did create accepted liveness receipts 12023 and 12024 at generations 5 and 6. The earlier prod turns `126593` and `126595` were charged to generations 3 and 4 and said prod 1 and 2. After the receipts, turns `126605`, `126606` and `126607` were charged to new generations 7, 8 and 9 and restarted the visible ladder at prod 1, 2 and 3; escalation `126610` was generation 10. This proves that accepted verdict evidence reset the progress epoch, contrary to the recovery owner's claim that the counter never resets. It also proves that a reset alone did not preserve liveness: each short patrol turn ended without an assignment-bound continuation, so the rail immediately generated a fresh three-prod ladder and escalation. Later evaluation must distinguish evidence acceptance from durable waiting. A useful outcome is not merely a lower counter after an attest; it is a reset followed by no false ladder while the obligation has a valid typed continuation. Repeated verdicts filed only to reset a ladder count as C5/C6 workaround churn unless they report new work evidence.

Bounded acceptance audit, 17:39-17:47 UTC: Ticket Patrol found separate existing tests for both sides of that sequence, and Parallax independently found no contrary contract evidence. `supervision_test.exs` proves a settled issued controller can be followed by a verdict receipt that resets generation and prod state. `row_driven_waits_test.exs` proves assignment-specific typed coverage across pending, queued and running states, restart survival, and failure ending coverage without answering the work. The inspected tests do not compose those facts into one reset, holder-authorized typed registration, restart and next-sweep suppression case. Treat that as one exact final-candidate acceptance question for existing ownership. Wake `w_95d82c5d-3d8b-4253-97eb-956b666513fb` queued the note behind the already-running exact R1 validation, explicitly requiring reuse through the existing integrator and gate. It is not evidence of a missing mechanism and does not justify another implementation, review or broad test lane.

Useful successor iteration and bounded evidence reuse, 17:44-17:56 UTC: the authorized private R1 validation reached its intended focused boundary and found a concrete product defect, two failures in nine tests because the candidate called unavailable `Jason.encode!`. The next turn stayed with the same implementation owner, replaced only those calls with Tightbeam's existing JSON API, reused the prepared checkout, dependencies and CLI build, and passed six affected tests covering round-trip, malformed-state refusal, timing and repair fencing. Schema and lockfiles stayed unchanged. Exact evidence is `att_c10fef51` and `art_56b3d625`; the failed run remains preserved. The packet is queued behind the independent bundle-offer gate for the sole integrator, not opened as another producer. This is justified defect-driven iteration, not C1/C2 churn. In a separate cadence closeout, the completion rule required a verification verdict on the exact assignment even though its source commit and independent review already existed. The holder filed the missing exact-card verdict from that evidence and completed without rerunning tests. That is the expected C1 response to a metadata seam; a new gate would have been unproductive token burn.

Planned review boundary, 17:56-17:59 UTC: bundle-offer tree `bc7c1bea` passed its existing full 1,886-test plus 9-doctest gate and private package assembly without changing source or lockfiles. Existing ownership opened one independent adaptation review, `asg_fb4f8d3c`, against exact base tree `a9ac945a`; its brief requires evidence reuse and forbids another implementation, reviewer or covered-gate replay. This is the intended component-review parallelism while R1 remains serialized behind the sole integrator. A Subetha ward was asked to report only a second same-identity review, a repeated covered gate, or an unjustified successor tree, not to re-review the code.

Workaround C3/C5 case, 17:12 UTC: while teaching patrols to retain one self-origin continuation alongside substrate process wakes, the recovery owner instructed the global patrol to cancel one wake and keep another, then retracted that instruction after checking the `origin` column and finding it had selected the system wake rather than the intended agent continuation. The retraction arrived before evidence of cancellation. Count the conflicting instructions and required re-read as workaround churn, not a product correction. Later guidance should define the invariant in terms of typed ownership and row identity rather than wake count alone.

Release-scope C3 case, 17:17-17:59 UTC: two external audits gave incompatible whole-0.1.9 readiness claims from the same accepted predecessor. Ticket Patrol retained six integration-level carry groups: R1, bundle-offer, G1, satellite compatibility, credential/safe-identity behavior and live-base/staging guards. Parallax initially reported only R1 as remaining substantive code, then retracted that claim after reconciling the selected inventory. Current classification is: R1 needs end-to-end code; the guard lacks accepted implementation; bundle-offer/O1, G1 and satellite have source but still need exact acceptance and serialized integration. Credential scope required a second correction. The canonical inventory includes current ACP adapter and safe patching under `wi_b49455ae`, citing Mike's 2 September ruling, but does not name harness-home/current-target adaptation `wi_b83e1fed`. The later exact authority read found ruled request `dr_3623e7cb`: it selected current-main integration under the standing both-lines rule and explicitly said a 0.1.9 arm was expected. That is historical selection for a bounded adaptation, not authority to replace the architecture. The adaptation must preserve one credential home per harness and machine, separate per-session identity and working directory, no cross-host secrets, one Codex refresher, safe identity-marker refusal and no bearer disclosure. Shared archetype guidance, merged identities or weakened safeguards need Mike's approval. The first bounded ownership read correctly found healthy custody for R1, G1 and bundle-offer plus completed healthy O1 source custody, but incorrectly grouped credential adaptation with a separate blocked main-first credential-failure project. Direct assignment evidence corrected harness-home custody to open assignment `asg_2beffc49` on `wi_b83e1fed`, already held by healthy release owner `s_0cd749fc`; `wi_7e25614b` remains a separate main-first backoff/redirect obligation. O1 coordination and satellite custody remain blocked; the guard has a healthy inactive holder but no partial implementation to recover. Wake `w_defed366-a33a-4de5-9454-38ec83e212c9` asked existing release ownership to sequence healthy work first and restore only genuinely stranded carries without duplicate source or integration lanes. Turn `126609` preserved the sole integrator, queued O1 and guard follow-through, and refused unauthorized satellite custody changes rather than creating an overlap. Turn `126677` confirmed the historical 0.1.x selection and preserved the isolation constraints while shared edits remained held. This case tests whether an explicit inventory and exact assignment map prevent both premature completion claims and unnecessary reimplementation.

Duplicate-decision-artifact C4/C6 case, 17:59-18:12 UTC: two Subetha wards independently answered the same request by building separate Lavish architecture decision pages before either saw the other's work. Both described the same current and proposed seams, and both initially repeated the same incomplete inventory-only authority interpretation. The lead retained one review surface, corrected it with `dr_3623e7cb`, and asked the other owner to end the duplicate without further comparison or redesign. No product implementation duplicated, but authoring, rendering and review attention did. Later evaluation should measure whether a named artifact owner and pre-start claim prevent this race without serializing unrelated analysis.

Credential architecture decision, 18:35 UTC: after iterative review of the actual identity-tree, workdir, skill, Rails, shared-home and adapter paths, Mike approved the bounded harness-home/current-target adaptation and said to keep going. The approval preserves separate session workspaces, per-session archetype guidance and elected skills, one credential home per harness and machine, one Codex refresher, Claude's distinct nonrotating path, safe identity refusal, no cross-host secrets and no bearer disclosure. The old patched delivery remains until the replacement passes unmodified-adapter identity, two-archetype isolation, long-guidance, product-conflict, compaction/child, hook-failure, credential cold-start/refresh, migration and rollback acceptance. It does not authorize shared archetype text, merged identities, weaker refusal, release, install or live credential/runtime changes. The canonical inventory now names `wi_b83e1fed` as included and wake `w_c52f6a3b` routed the decision once to existing owner `asg_2beffc49`; no duplicate implementation or review lane was opened.

PR-monitor S4/C5/C6 recovery, 18:16-18:19 UTC: the durable PR-monitor thread still showed open ownership, but recovery holder `s_9744234f` had stopped after turns `126377` and `126389` both hit the retired Codex usage limit. Periodic process check-ins continued against the open assignment without a working patrol. The lead revoked both the failed recovery `asg_1765fa1e` and its blocked parent `asg_dd77c018`, then assigned current-token successor `asg_787d4f06` on the same work item and verified turn `126713` running. The successor must reuse the prior census, distinguish real external contributions from internal PR-as-CI misuse, and report only material deltas. This tests whether truthful credential/quota terminal state plus exact successor fencing reduces false healthy ownership, repeated check-ins, and full-census rework.

PR-monitor typed-wait S4/C5 sequence, 18:19-18:25 UTC: successor turn `126713` reused the prior census, reached GitHub under the current token and found no head/state delta among 11 open PRs. It reported that George's issue 38 matches a failure PR agents had observed, but no durable row explicitly acknowledges that issue. The holder filed progress `att_75ff4b06` and scheduled self-origin hourly wake `w_b4a07e51`; because the wake had no assignment link, supervision immediately emitted turns `126717`, `126719`, `126720`, `126721`, `126723` and `126724`, even after a stale-prod verdict and explicit duplicate suppression. This is the observed reset-without-typed-coverage defect, not missing monitor work. The lead closed the idle assignment, canceled the obsolete wake through its creator, and retained exactly one replacement hourly wake `w_8edb0a82`. Its prompt requires each tick to open, execute and close one bounded patrol assignment before scheduling the next. Current durable state shows zero open monitor assignments and only that replacement pending. This workaround avoids an hour of false prods but does not fix the product seam; later evaluation should compare it with assignment-bound typed wait coverage after the replacement ships.

Bounded-monitor closeout, 18:28-18:31 UTC: a Subetha PR-domain ward mapped George's still-open issue 38 to already-merged PR 37 head `9d041943`, 0.1.9 merge `73e1e3b4`, existing work item `wi_365cc7a6`, landing `att_599c0b35` and review `att_9d8d723a`/`art_bc54486a`. Direct ancestry showed the merge in current `eff3`; the issue was an acknowledgement gap, not a missing release fix. One atomic bounded assignment `asg_1e375733` recorded artifact `art_2f1ba650` and completed with `att_4aca95f7` in the same turn, without a census, review, test or GitHub mutation. No liveness prod followed. This is positive evidence for open-work-only monitoring: material external findings can be made durable through a short closed obligation while the hourly monitor remains quiescent.

Idle-monitor slate-clear S2/C5 workaround, 18:30-18:33 UTC: although the bounded issue-mapping assignment closed without a prod, leaving recurring monitor item `wi_54c952c3` open and unstaffed immediately queued slate-clear turn `126733` to blocked bare Main. The lead canceled prior hourly wake `w_8edb0a82`, iceboxed the unstaffed work item and retained one replacement wake `w_27393225` for the same due time. Its prompt requires reopen, one bounded patrol assignment, completion, re-icebox and one next tick. This prevents idle recurrence from presenting as an ownerless active slate, while preserving the cadence. The already-queued Main notice remains historical evidence; no replay or Main wake was added. Later evaluation should determine whether a first-class recurring-monitor wait can replace this state choreography.

Recurring-monitor lifecycle ordering result, 20:25-20:40 UTC: the next bounded patrol tested the opposite order by attempting to icebox `wi_54c952c3` before closing its sole assignment. Tightbeam correctly refused with `assignments_open`; closing the assignment then exposed an open, unstaffed item and immediately queued slate-clear turn `126834` to blocked bare Main. The lead iceboxed the item after closure, canceled the resulting stop-monitoring wake `w_9895e798`, and retained one replacement `w_e1d97ca5` at the original deadline. That replacement performs an assignment-free, transcript-only, read-only delta scan, keeps the item iceboxed, creates no work, assignment, attest, artifact or review row, and schedules exactly one successor. Current readback confirms the old wake canceled, the replacement pending, the item iceboxed and zero open monitor assignments. On live 0.1.8, neither close-then-icebox nor icebox-then-close provides an atomic recurring-monitor boundary. The assignment-free pattern is the next prospective workaround, not evidence that the product seam is fixed; later evaluation must test whether it preserves material-delta detection without C5/C6 records or hidden monitor failure.

Bounded-monitor first hourly cycle, 19:24-19:26 UTC: replacement wake `w_27393225` fired once. The monitor reopened the iceboxed item, opened one coordination assignment `asg_853e3a01`, compared current open PRs with retained evidence, filed `att_e77d8c38`, completed the assignment, returned the item to the icebox and scheduled exactly one successor `w_1dbbe730` for 20:25:52 UTC. The patrol found one material external change: George's PR 30 advanced from `54858cd5` to `351ab6d2`, with Linux and macOS checks green. It reused the prior census for the other ten unchanged heads, retained issue 38 as an already-landed fix and found no new internal PR-as-CI misuse. No review, test, PR mutation or liveness prod followed. This is positive evidence for SD-02 and SD-06 at one cycle only; recurrence and failure recovery remain unproven.

Bounded-monitor closeout-order correction, 19:25-19:45 UTC: the patrol's useful work was clean, but completing `asg_853e3a01` before re-iceboxing its recurring work item left a brief unstaffed-open interval. Slate-clear process turn `126784` entered already-blocked bare Main during that interval, raising the known blocked cohort from 75 to 79 alongside three unrelated misroutes. This contradicts the initial claim that the reopen-run-close-icebox choreography fully prevents idle recurrence. The monitor canceled successor `w_1dbbe730` and replaced it once with `w_5bc2a7c2` at the same 20:25:52 UTC due time. The next tick will attempt re-iceboxing while the bounded assignment still owns the item, then complete the assignment. If Tightbeam refuses that order, it records the refusal once and retains one wake without inventing another pattern. The correction itself adds one monitor turn; later evaluation must compare that cost with whether the next cycle avoids a Main notice.

R1 defect-driven iteration boundary, 17:59-18:19 UTC: the sole integrator combined the changed R1 source with the existing schema packet and new reset-to-typed-wait acceptance case. Focused tests passed 23/0 and CLI tests passed 299/0, but the first changed-tree full Mix gate ended at 1,919 tests plus 9 doctests with 16 failures. The failure groups include stale readiness/schema fixtures, optional-principal compatibility, reminder-state lifecycle behavior and full-process recovery mutation; legacy NULL fact replay remains a separate uncovered regression. This full run is justified new integration evidence, not a covered-gate replay. The candidate and terminal log remain frozen, and the same integrator owns affected-test diagnosis before another full run or independent review. Count any second reviewer, unchanged full-suite rerun, competing source lane, or broad relaxation of lifecycle assertions as C1/C2/C4 churn; do not count bounded fixes driven by distinct failures as churn merely because they require another iteration.

R1 receipt-echo C5/C6 case, 18:05-18:22 UTC: after the integrator reported the red full gate, it also sent the result to the PO. The PO recorded a correction receipt and sent the same state back to the integrator. After the integrator fixed legacy NULL replay at successor tree `7deef062` with focused 7/0 proof, it again notified the PO; the PO recorded another receipt while correctly retaining the same unresolved full-gate boundary. These receipts preserved provenance but did not change custody or the next action, and their return messages queued behind the same integrator. Wake `w_6c7fc5a9` instructed the PO to consume the next material result once and stop mirrored acknowledgements unless evidence changes candidate identity, acceptance, authority or action. Later evaluation should distinguish one accountable consumption receipt from a producer-owner-producer echo chain.

R1 semantic-failure preservation, 18:24-18:27 UTC: bounded correction cleared the original eight compatibility failures, and a separate 41/0 rail selector fixed two additional fixture-context failures without changing their assertions. Those selectors did not cover the seven original supervision failures or the full-process recovery mutation; an external ward's initial “14 of 16 cleared” summary was corrected before acceptance. Inspection then found a real contract conflict: a healthy pending reminder claim is treated as a generic dispatch error that returns before reassessment and effort work, while supported wake cancellation can strand the claim. The selected design requires notification coalescing without suppressing reassessment, while preserving original claim and unknown-effect ownership. This is useful defect discovery, not stale-test churn. The same integrator remains the sole source owner and has not rerun the full suite or opened review. Later evaluation must count the prompt correction and mistaken summary, but must not pressure the owner to relax lifecycle assertions merely to reduce iteration count.

R1 lifecycle correction iteration, 18:27-18:32 UTC: successor tree `c3c052fa` gives notification coalescing a non-success drain result, allowing reassessment while leaving the reminder claim, prod count and last-prod time unchanged. It releases only a proven canceled, unconsumed claim and does not advance delivery backoff. New behavior, reset and sensor-recovery tests passed within an 82-test affected selector; five race/re-entry fixtures still failed because they expect a new notice without completing the prior consumer through the real Gateway/SessionLane terminal path. The owner preserved those failures rather than marking queued or fired notices delivered, filed `att_493188b5` and scheduled one continuation. This is a justified narrowed iteration with a decreasing, semantically explained failure set; another full run or review before the five terminal-path cases pass would be churn.

R1 recovery-evidence correction, 18:35-18:40 UTC: the same integrator narrowed the full-process preservation mismatch at exact staged tree `66a82e3f`. The crash/restart fixture passed one test with no failures after SIGKILL 137 and a 2,854 ms restart. It proves all prior assignment fields remain unchanged and permits one new pending reminder claim only when bound to the exact wake, assignment, recipient, owner and epoch. The first run exposed that the fixture's temporary index started at `HEAD` and omitted staged R1 files; the owner corrected that evidence defect before making an exact-candidate claim. Five terminal-delivery fixtures remain and one continuation is due. Count the source-binding correction as useful evidence repair, not an R1 product success or a reason to repeat the full gate. The next churn boundary remains unchanged: finish the five real callback cases in the sole lane before full-suite replay or independent review.

R1 continuation-fragmentation candidate, 18:43-18:47 UTC: turn `126741` found that existing Gateway tests already contain adapter doubles which exercise the runner's real terminal callback. The same integrator extracted and formatted those doubles into shared test support at test-only tree `2ef8ad90`, filed `att_2ed218fd` and scheduled one continuation, but did not wire or run any of the five remaining fixtures. This is a useful enabling change, so it is not yet confirmed C6. Classify it as a candidate because the assigned bounded outcome remained untouched after another turn. The next continuation should consume the extraction by wiring and running the five cases. Another preparation-only turn with no tested fixture or newly proven blocker would confirm avoidable turn fragmentation; opening review or a full gate still counts as premature C2/C1.

Candidate resolution, 18:52-19:00 UTC: turn `126752` consumed the shared doubles and rejected the C6 classification. It reached the real consumer path, corrected three concrete fixture defects, got four cases passing, then added opt-in unique-session identity to clear the final parent-ownership guard without changing existing Gateway-test behavior. Exact test-only tree `d14ead18` passed all 75 supervision tests through the actual Gateway runner and SessionLane winning terminal callback. The owner filed `att_640c6ffd` and preserved the recovery, legacy-NULL and failed-run evidence without a full-suite replay or review. This is positive evidence for SD-06: one preparatory turn followed by one bounded implementation-and-gate turn produced the named useful outcome. It does not establish full R1 acceptance.

R1 uncovered-acceptance narrowing, 19:01-19:07 UTC: the sole integrator reused the 75/0, recovery, legacy-NULL and rail evidence, then added only the uncovered acceptance cases. It corrected two API fixture shortcuts before claiming coverage: the first lacked a real wake scheduler, and the next created a controller without the normal eligibility claim. At exact tree `48a6493e`, the attributed API-to-terminal case passed 1/0 and proved replay and conflict refusal, receipt reassessment, preservation of a newer consequence behind an older pending notice, and real terminal success. Cancellation-negative and shared-double compatibility cases also passed. The owner filed `att_8ca4ce07`; authorized-successor and required-effort proof remain. No full gate or old-version scenario ran. This is another justified narrowed iteration, not C1, because it tested previously uncovered behavior and changed the evidence boundary. A continuation that replays these cases without a candidate change would cross into C1/C6.

Detector false positive and C5 race, 19:18-19:19 UTC: after exact tree `08e8629a` passed required-effort 1/0 and persisted wake/turn repair fencing 2/0, the open integrator assignment briefly appeared to have two named gaps and no pending continuation. The external lead sent corrective wake `w_222c9b57` to the PO. The PO had already created the correct same-holder continuation `w_e7d44603` at 19:18:52, seven seconds before the corrective wake fired. Acting on the stale observation caused the PO to create duplicate same-scope wake `w_2ae74ff4` at 19:19:14. The first produced running turn `126779`; the duplicate became queued turn `126781`. Do not send a third suppression prompt into the same lane. This is a confirmed C5 cost caused by the detector itself, and a false-positive S4 transition. Before any corrective write, re-read wakes created after the observation timestamp and the target session's newest queued or running turn. If a matching continuation appeared, record the race and abstain. Later evaluation must include detector-caused messages and turns rather than scoring only churn it prevented.

Race outcome, 19:34-19:40 UTC: the duplicate wake did not cause duplicate test execution, but it did propagate one extra turn. Original turn `126779` advanced to `c2ba5298` with changed-consequence timing 1/0 and scheduled continuation `126788`. The accidental duplicate then became turn `126781`, consumed the newer tree, and completed the still-missing repair authorization and repaired-consumer path at frozen tree `50d652b8`, 2/0, through public dispatch, the real Gateway runner and SessionLane. When `126788` started, it reconciled against `att_61551b70`, repeated no edits or tests, and kept the newer result. Count the stale reconciliation turn and its attention as C5, not C4. The useful work in `126781` does not erase the detector cost; it shows ordered lanes can convert an accidental duplicate into the next needed step while exact-state checks prevent repeated execution. A 15-minute owner-disposition check remained pending, so complete containment was not yet proven.

Final C5 containment and combined gate, 19:54-20:10 UTC: the first disposition-only check found no new owner action and scheduled another unchanged poll, confirming C5 rather than useful waiting. A direct owner correction canceled that poll and routed the still-uncovered CLI-wire proof followed by one combined gate. The PO emitted the same disposition wake twice while its turn remained active, creating running turn `126803` and queued duplicate `126806`. Turn `126803` proved the real CLI wire 1/0, then froze tree `40d3044b` and passed the single combined gate with 1,930 tests plus 9 doctests, no failures, 11 skips and exit 0; pre/post tree, patch, wrapper and lock identities matched. The queued duplicate reconciled against `att_41ebee32` in four seconds and ran no work. Exactly one independent review `asg_b230853e` then opened against the frozen tree. Count both redundant delivery and reconciliation as C5, but not C1 or C4. This case supports exact-state suppression while showing that owner-side at-most-once wake creation remains necessary.

Review-start duplicate containment, 20:10-20:21 UTC: when exact review assignment `asg_b230853e` opened, the liveness process immediately emitted prod turn `126810`; the PO sent the proper review brief ten seconds later as queued turn `126811`. The first turn independently reconstructed exact tree `40d3044b`, hash-verified the CLI 1/0 and combined 1,930-plus-9/0 evidence without rerunning tests, inspected the assigned R1 authority, replay, timing, repair, migration and O2 compatibility scope, then filed reviewed-clean verdict `att_a3108503`, report `art_41ed3470` and completion `att_d0d5c8dd`. The second turn ran for eight seconds, recognized the closed assignment and repeated no gate or review. Count the duplicate prompt and reconciliation as C5. Do not count it as C1 because one review execution and one verdict occurred. The startup race shows why a newly opened assignment needs a grace or bound-start state that prevents a liveness prod from racing its owner's initial brief.

Detector saturation and C5/C6 containment, 20:37-20:47 UTC: standing relief patrol `asg_e714f7d9` had reached 21 high-effort visits at a 20-minute cadence and already preserved six independent condition-wait false-prod specimens. Substrate check-in turn `126836` then overlapped queued self-continuation `126840`; before the stop ruling could arrive, that queued turn spent one final visit to record a seventh same-class specimen as `att_dfa7ca8a` and armed another continuation. The added specimen did not change the diagnosis or next product decision. The opener revoked the never-completes assignment, delivered one stand-down reconciliation turn `126844`, and the holder canceled its continuation without replacement. Final readback shows the patrol assignment closed/revoked and zero pending holder wakes. Resume is limited to a genuinely new material failure class or the post-install comparison. This is a confirmed detector-caused C5/C6 episode: bounded sampling established the theory, but an unbounded evidence cadence kept spending attention after saturation. Later detectors need an explicit sufficiency or stop condition, and their own queued work must count in the churn total.

Finite global-census boundary, 20:47-21:12 UTC: global patrol `asg_e9f448ca` completed 15 bounded sweeps and froze one exact 59-card OPEN-dark snapshot rather than rolling new arrivals into an endless census. Thirty-nine cards had already been named. Of the remaining 20, 16 were false-dark because their holders had worked four to 29 minutes before the snapshot, two were explicitly excluded portfolio custody, `asg_c8f9d0f8-7ac5-4287-8aec-91c19b59f0f3` was already settled as C5 notification churn, and `asg_a80784b1-b0bf-4918-946c-bc8c2673ac34` had an evidenced live child continuation. Actionable remainder reached zero; final receipt `att_73a704ac` records the boundary and a separate structural count of nine open cards with retired exact openers. The holder canceled its unconditional continuation and armed no replacement. The opener then consumed the terminal receipt and revoked the assignment at 21:04, leaving zero pending holder wakes. Revocation still exposed the open, unstaffed work item long enough to queue slate-clear turn `126869` to blocked Main. The lead did not replay that notice and iceboxed `wi_820b2c5f-9bc2-466e-bb3d-12999ed3910e` after verifying zero open assignments. This supports a finite-cohort stop rule and rejects holder-row silence as a sufficient S4 predicate. Sequencing closure after the terminal receipt preserved the final evidence, but live 0.1.8 still lacks an atomic final-assignment-and-item closeout.

Passive-custody contract conflict, 20:44-21:10 UTC: Wave 7 opened successor cards `asg_9dd64a96-59bc-4840-a578-b5c6104f70f1` and `asg_74c14594-6d9c-47bd-9adc-37e48ed04f0a` to preserve two completed Firehose source packages while their original openers were queue-blocked. Each successor required no executable work and explicitly forbade a continuation wake. Live 0.1.8 supervision therefore interpreted the required silence as inactivity. The first successor filed custody acceptance, answered two false prods with evidence, then surrendered on the third repeated prod at `att_e68183a5`. The sibling reused that exact mechanism evidence and surrendered at `att_93487b96` after one false prod rather than spend two more turns proving the same conflict. Original cards `asg_2a0feed7-6f27-47ca-aaac-9919551d8cd0` and `asg_d511808c-6424-47b2-8dcb-8a8d85fe6c94` remain open, so source and review custody was not lost. Controller disposition `att_5a498f8d` withdrew both failed successor recommendations, marked the originals preserve-live and created no replacement, wake, audit or patrol. This is confirmed C3/C5/C6: the custody contract demands silence while the liveness contract demands an observable continuation. Recreating another passive successor would be churn. Later design must represent durable passive custody without prompting work, or retain original custody until supported recovery.

Credential-test isolation boundary, 20:57-21:16 UTC: the first group-2 affected run on draft `80932293` reached 136 tests and 70 failures before an existing fixture invoked a real Claude warm-up command and returned “Not logged in.” The integrator stopped the run, verified no matching process remained and explicitly classified its shell exit after SIGTERM as interrupted evidence, not a pass. A test-only successor added the existing injected runner seam at tree `65c518df`; the next bounded correction reached tree `4723458a` and passed one exact offline credential-ordering case with pre/post identity unchanged. Inspection also found explicit remote fixture runners that can still execute shell locally, so the integrator did not repeat the affected suite or the earlier R1 full gate. Remaining old-store and harvest assertions are still open. These turns are useful evidence repair rather than C1/C2 because each changes the safety or coverage boundary. Another affected-suite run before every selected runner is synthetic, or a broad gate/review before the remaining assertions are reconciled, would be C1/C2/C6 churn.

Credential-test isolation follow-through, 21:20-21:27 UTC: turn `126912` changed the test boundary again rather than repeating the failed suite. Tree `c0e7f26a` intercepts the exact local and SSH-serialized warm commands before the fixture's explicit shell delegate, rejects near-match `CLAUDE_CONFIG_DIR` warm commands and preserves non-warm explicit runner behavior. One new remote-warm isolation regression passed with one test and no failures; no vendor executable ran. The integrator then stopped and scheduled reconciliation of the remaining old-store and removed-harvest assertions before any grouped run. This remains useful evidence repair, not C1, C2 or C6. It would become churn if the grouped gate starts before those assertions and all selected runner paths are reconciled, or if the already-accepted R1 gate runs again without a changed applicability reason.

Credential-test sequencing follow-through, 21:27-21:38 UTC: tree `c3242b85` reconciled five local home subjects and passed four selected home tests plus one write-set test with no failures. Tree `dd444372` then reconciled four remote home subjects and passed their four selected tests with no failures. These steps made no production change and retained synthetic command execution, hollow-home refusal, byte-preservation and concurrent-write authority. The grouped gate still had not run; remaining credential old-store and hollow assertions were the next named boundary. The small successive test groups are not C1 because each covers newly reconciled assertions, but their cadence should stop once the selected assertion set is complete and should yield one grouped applicability run rather than a chain of overlapping partial gates.

Assignment-free PR-monitor trial, 21:25-21:27 UTC: scheduled wake `w_e1d97ca5` fired on time as turn `126926`. The monitor reused the established 11-PR census and queried only head, state, check and acknowledged-bug deltas. It reported no material change, no new internal PR-as-CI misuse and no change to George's acknowledged issue state. The turn left `wi_54c952c3` iceboxed, opened no assignment, filed no attest and created one successor wake, `w_1df61526`, for 22:25:52 UTC. It reached a delivered terminal state at 21:27:35. The session did retry its narrow GitHub and local readbacks after sandbox denials, but it did so inside the same low-effort turn and created no duplicate durable coordination row. One successful tick supports the assignment-free pattern as a C5/C6 reduction; it does not yet prove long-run cadence stability. Later ticks must preserve exactly one successor and the same no-work-row invariant, and their retries should count if sandbox mismatch keeps consuming turns or broad context.

Stale PR-monitor closeout regression, 21:56-22:00 UTC: Main later consumed old slate-clear turn `126784` for the already-iceboxed PR-monitor item. Instead of reconciling with the assignment-free pattern, it reopened `wi_54c952c3` at 21:56:33, dispatched `asg_37f2f574` at 21:58:05, obtained another no-material-delta census and completion `att_f039fde0` at 21:59:15, then re-iceboxed the item at 22:00:22. The assignment's automatic deadline wake canceled; the sole assignment-free hourly wake `w_1df61526` remained pending for 22:25:52. Final state is safe, but the stale turn recreated one full reopen, assignment, attest, close and icebox cycle without changing the PR decision. This is confirmed C5/C6 and shows that fixing the standing monitor does not suppress already-queued old closeout instructions. Count the extra patrol turn and durable rows in the baseline. A recovered slate-clear handler must read current item state and current monitor contract before creating work.

Second assignment-free PR-monitor tick, 22:25-22:27 UTC: wake `w_1df61526` fired exactly on schedule as low-effort turn `127046`. The monitor reused the 11-PR baseline, found no material PR or acknowledged-bug delta, left the work item iceboxed, created zero assignments and zero attests, and retained exactly one successor wake, `w_139f4f7e`, due at 23:25:52. This confirms the durable-row reduction across two ticks. One residual C5/C6 candidate remains inside the turn: despite the assignment-free wake, the session announced that it would close “this bounded card” and attempted completion against already-closed `asg_37f2f574`; the CLI refusal made it re-read `att_f039fde0` and avoid a duplicate receipt. The turn took 106 seconds. Later acceptance should prove that assignment-free recurring work clears or ignores stale assignment context before tool selection, not merely that the closed-row guard prevents the duplicate mutation.

Second stale PR closeout suppression, 22:03-22:04 UTC: Main then consumed another old slate-clear turn, `126834`, for the same item. This time it read current state, found the item already iceboxed with zero open assignments and delivered in 22 seconds without reopening, dispatching or filing a row. The assignment-free wake remained the sole next monitor action. Two consecutive stale prompts against the same obligation therefore had different outcomes: the first recreated the obsolete workflow, while the second self-disposed from current state. Main applied the same suppression at 22:05 to stale slate-clear turn `126869` for iceboxed global-patrol item `wi_820b2c5f`, leaving it iceboxed with zero open assignments. This supports execution-time reconciliation as an effective C5/C6 control, but also shows it is not yet applied consistently.

S1 recovery and stale-backlog hazard, 21:11-21:20 UTC: an internal 0.1.8 gateway-child restart followed repeated five-second SQLite call timeouts; the systemd service itself remained active on build 1337. At 21:11:50 all seven terminal-but-unfinalized turns `126359`, `126361`, `126370`, `126372`, `126378`, `126390` and `126391` finally became `failed_unknown`, and ordered lanes began draining their queued prompts. This resolves the observed S1 rows but does not validate the proposed replacement seam or prove each obligation recovered. The drain can execute instructions authored against hours-old state. One resulting wake, `w_aa7d2ca6`, told the sole integrator to repeat already-integrated O2 work; turn `126884` re-read current state, declined the replay, preserved its existing continuation and ran no source or gate action. An initial detector report incorrectly attributed that wake to the stranded running turn; exact timestamps corrected it to post-terminal queue drain before intervention. Later recovery must reconcile each queued instruction against current candidate, obligation and effect state before executing it. Count stale prompts and self-disposition turns as C5/C6 even when ordered execution prevents duplicate work.

High-effort stale analysis during recovery, 21:22-21:29 UTC: the recovery owner consumed two prompts that had queued before the current dispositions. Turn `126898` usefully corrected the stale-wake mechanism to backlog replay and established that the named source lane had drained. Turn `126904` then spent a high-effort pass reconstructing the already-recorded passive-custody failure, filed `att_4ba7b247` and asked Mike whether to lift a stop order even though controller receipt `att_5a498f8d` had already preserved the original custody cards, withdrawn the failed successors and forbidden replacement or more patrol. This is confirmed C5/C6 from stale queue consumption: the second analysis added corrections to some counts and labels, but did not change the settled disposition and created a superseded decision demand. A previously queued turn `126906` corrects the separate causal classification from `126898`; it does not resolve this newer stale demand. No further prompt was added because it would join the same ordered backlog and could not prevent the already-finished analysis. At 21:29 the owner had one running turn and four queued turns. Later evaluation should count the model effort and disposition work spent by stale analysis, while retaining the useful causal correction from the preceding turn.

Review-before-ruling candidate, 21:20-21:35 UTC: exact static review `asg_e9e295d0` opened against frozen F9 tree `163760db` at 21:20:15. Liveness prod turn `126910` started four seconds later, before the owner's actual brief became queued turn `126918` at 21:20:44. At 21:30 the owner added queued correction `126938`: the canonical REST owner still retained an approval-layer refusal, so the delegated disposition did not settle authority and the review must remain static rather than imply acceptance. The shared Codex adapter process remained present while Agentd had no activity claim for it, so the long `running` row was only a stall candidate. The turn delivered at 21:34 and filed one changes-requested verdict, `att_55499834`, after finding a real integration defect: three current `scripts/feature_smoke.exs` calls omitted the newly required revocation reason and would receive `missing_reason`. The late owner brief then reconciled against that immutable result without repeating review, tests or edits. The S1 candidate is rejected, and this is not confirmed C2 review churn because the scoped static review found reusable evidence despite the unresolved authority conflict. A later review should narrow itself to the changed smoke fix and ruling-dependent consequence rather than repeat the clean migration, actor, generation, reopen and serialization findings. The premature liveness prod remains part of the already-confirmed review-start C5 race.

Narrow review successor outcome, 21:37-21:43 UTC: producer tree `54893761` changed exactly two files to repair the three smoke-call omissions. Successor review `asg_d3b8bf0a` consumed the prior defect report and authority erratum, inspected the changed calls and their AST guard without rerunning tests or provider smoke, then filed scoped reviewed-clean verdict `att_49c34fd3` and completion `att_28aa9367`. The reviewer retained every approval, exact-checkout, full-suite, package, hosted, runtime and release gate. This is a justified changed-tree successor rather than C1 repetition, and it shows how to keep review iteration proportional: reuse the predecessor's clean static findings, inspect the repair and its immediate guard, and leave unrelated acceptance gates open. The liveness prod again started before the owner's brief, so queued turn `126950` still had to reconcile after completion; count that extra prompt and turn as C5 even if it repeats no review work.

Fresh-token ward audit, 21:25-21:51 UTC: Main turn `126415` narrowed an initial 155-open-assignment snapshot to existing assigned Codex wards rather than waking the whole tree. It obtained fresh durable results from Gibson sessions `s_fcdd77af` and `s_0184755c`, preserved existing continuations, created no assignment and did not retry a Racter ward whose fresh turn had not answered during the audit window. Direct later readback showed Racter turn `126943` remained `running`; its earlier `Internal error` was not the terminal state of this fresh attempt. The Racter turn then delivered at 21:50 with controller 10/10, allocator 25/25 and exact PostgreSQL/WebSocket evidence, proving refreshed Codex capacity there as well. Treat the audit's phrase “still failed to produce a fresh response” as an observation-window result, not a failure verdict. This supports bounded ward rotation and no-retry suppression, while showing that token-recovery reports must distinguish running, timed-out observation and terminal failure to avoid unnecessary succession.

Evidence-capture rerun and review-linkage conflict, 21:35-21:51 UTC: Racter turn `126943` reports that its first isolated allocator suite completed cleanly but the command session discarded the terminal output. It reran the unchanged suite solely to obtain an exact total, producing 25/25, then ran the one named real PostgreSQL/WebSocket case for its exact collision and reconnect values. The unchanged 25-test replay is confirmed C1/C6 caused by evidence-capture lifetime, even though the later named case added distinct evidence. Durable command output or immediate receipt capture should prevent this class. After filing `att_fbd84d77` and `att_5ef55219`, assignment completion failed because the existing independent clean review links to the Eezo recovery producer rather than preserved predecessor `asg_b57a7cef`. Decision `dr_8d976ef7` now offers a bookkeeping-only linked review or keeping the terminal-evidence card open. This is a C3 seam conflict: duplicating a review satisfies linkage without adding assurance, while keeping the card open creates false S4 exposure. Do not count a bookkeeping review as independent defect-finding work. Later design should let an accepted exact candidate review satisfy every preserved assignment whose completion depends on that same candidate, with explicit provenance rather than a second review execution.

Main stale-queue mutation outcome, 21:42-21:50 UTC: after the token audit delivered, Main advanced from turn `126415` to old queued turns `126492` and `126500`; its queue fell from 30 to 28. Turn `126500` was authored at 16:46 and instructed Main to rule 34 effort requests and arm continuations. It re-read current receipts, selected seven still-live cards, created one 30-minute wake for each and ruled their exact requests `continue`. It dismissed one completed ClipMesh request, preserved an already-superseded D3 request and revoked the two terminal assignments. Direct rows confirm all nine effort deadline wakes canceled before delivery, leaving only the seven distinct obligation continuations. This rejects the mid-turn C5 candidate: batching created no duplicate notification and reconciled the stale instruction against current state. The turn still consumed a high-effort pass, but it produced coherent current lifecycle dispositions rather than unproductive C6 work. Later queued turn `126925` carries a one-time provider device-login instruction created at 21:22 with a 15-minute expiry; do not preserve its code in reports. Live 0.1.8 cannot cancel that queued turn, and a warning sent to Main would land behind it. The required defense remains execution-time reconciliation of request ownership, current card state, existing continuations, credential state and expiry before mutation. Subetha domain owners were asked for exact current disposition evidence only, with no Main message, wake, work item, patrol or broad census.

Stale durable-authority emission, 21:13-21:37 UTC: a second recovered lane proved that C5-D is not limited to obsolete messages. Queued turns authored hours earlier ruled effort requests `dr_2b3ce1b1` and `dr_32d52d4e` as `continue` at 21:13, then revoked related assignment `asg_7711618b` at 21:18:57. The resulting state says both “continue” and “revoked” because each stale action was individually authorized but no longer coherent with the current sequence. Read-only verification confirms 57 effort requests on `asg_7711618b`, 31 on `asg_c8f9d0f8` and 237 substrate effort requests org-wide in the preceding 24 hours. Recovery receipt `att_6c140b2a` classifies 53 stale turns drained across six lanes: 32 produced no durable rows, while 21 produced 12 attests, 20 wakes, eight rulings, two assignments opened and two revoked. The receipt also corrects the first drain timestamp to 21:11:50.956, 127 milliseconds after the terminal reap. Both review assignments created by the drain produced distinct useful verdicts, so assignment creation itself was not duplicate work; the stale false reports, contradictory authority writes and downstream reconciliation are the confirmed C5/C6 cost. All recovered stop-listed lanes except Main had drained by the recovery owner's later read; Main still mixed old queued instructions with new arrivals. Current 0.1.8 has no queued-turn cancellation verb, so prevention depends on every resumed turn reconciling current assignment, decision and effect state before mutation. Notification-volume reduction must also address effort-request generation, not only ordinary wakes.

Stale effort-request snapshot, 22:10 UTC: the live open decision list contained 12 effort requests whose durable questions still asserted `zero_effect`. Their `raisedAt` values ranged from 28 August through 8 September. Read-only comparison with later rows showed that nine of the 12 assignments had filed attests after the request was raised, from one later attest through 43. The strongest contradictions were `dr_49c58329` on `asg_acd48634` with 43 later attests, `dr_2122c3e5` on `asg_536735af` with 42 and `dr_eb6c4210` on `asg_2b57eea6` with 14. Three requests had no later attest and remain unresolved rather than presumed stale. By 22:33, the open effort count had grown to 14 and ten underlying assignments had later attests; none of the earlier requests had become non-open. This is confirmed C5 at the operator-attention seam: a truthful point-in-time zero-effect finding remains open after its premise changes, so an eventual ruling must reconstruct current state and can generate obsolete continuation work. It is also a C6 risk because several affected assignments already accumulated later turns and evidence while the old question stayed actionable. Wake `w_ff0328a8-656e-47cf-87d9-f5c63a542c4c` routed the specimen to the existing R1 implementation owner, asking it to define later-effect supersession or obsolescence in the already-scoped effort-checkin interaction. No new card, implementation lane, review or broad test was created. Later evaluation should count open stale requests, time from contradictory effect to invalidation, operator decisions avoided, and false invalidations of genuinely stalled work.

The stale effort backlog continued growing through 05:54 UTC: 17 effort requests remained open, all on still-open assignments, and 11 already had later attests contradicting their stored `zero_effect` premise. The strongest contradictions grew to 48 later attests on `dr_2122c3e5`, 45 on `dr_49c58329` and 17 on `dr_eb6c4210`. Every open request also had at least one later delivered turn attributed to its assignment, although a turn alone does not prove useful effect. This is not a harmless historical queue. The stale requests can still reach an owner and create new continuations, as the recovered Main backlog already demonstrated. Supersession should be driven by assignment-scoped effects after `raisedAt`, with later attests decisive and later turns prompting reconciliation rather than automatic invalidation. Report the backlog as a current count and stale subset, not a single total that treats genuinely unresolved requests as obsolete.

Effort-request self-routing loop, 22:00-22:15 UTC: bare Main delivered 29 high-effort turns totaling 921.6 seconds. Four process effort prompts, for `dr_49c58329`, `dr_a3947a2e`, `dr_c753485a` and `dr_2725c355`, followed the same two-turn path. Main inspected the current assignment, attempted `continue`, and received a refusal because it was not the request's authorized expecter. It then created a `user:mike` wake which routed back to bare Main. The second Main turn reconstructed the same state and finally woke the actual current owner. Those four pairs consumed eight high-effort turns, plus four self-wakes and four owner wakes; they produced no ruling. A fifth pair for `dr_af8825c9` repeated the same path at 22:14-22:15, adding two high-effort turns, one self-wake and one owner wake without a ruling. This is confirmed C5/C6 caused by a routing and authority mismatch, not by five independent hard decisions. Read-only rows and installed-source inspection locate both sides of the seam. All five requests had `expecterSessionKey` null and `expecterUserId=mike`. `EffortCheckin.open_request_in_txn/4` and `arm_notification_in_txn/2` therefore selected `Org.personal_session_key(mike)`, bare Main, while `authorized?/2` correctly refused the resulting agent principal because only `{:user, "mike"}` can rule. CLI help then told the agent to answer `[from user:mike]` with `wake --user mike`, which resolved to the same personal session. The authority check is correct; user-target delivery and reply guidance collapse a human operator and their agent Main into one endpoint. A session expecter should receive the request at that exact session. A user expecter needs one genuinely human-facing operator surface, not an agent self-wake; an accountable owner may receive information but cannot rule unless it is already the authorized expecter. Wake `w_622d5dd8-0779-414b-9e62-b776a35a1f20` added the observed loop to the existing R1 effort-interaction acceptance, and source-correction wake `w_2a5b5ff8-09ab-4c95-bec6-669bbac3d5e0` preserved this authority distinction. The owner first combined both effort specimens in `att_68545728`, then superseded its ambiguous owner-routing language with exact contract receipt `att_e4ec7c58`; it created no new source lane, review, gate, test or ruling. Later evaluation should measure turns and model effort per settled effort request, refusal-to-owner hops, self-targeted user wakes and whether direct routing preserves authority.

Proportional fixture-reconciliation counterexample, 21:30-22:22 UTC: the sole integrated-source executor advanced credential Group 2 through separate named fixture families: local Homes, remote Homes, hollow and harvest cases, Credentials, credential kinds, Spinup and rotation routes, then model catalog. Each failing run identified a different old-bank or host-path assumption. The executor reran only the affected failing cases after each change and did not replay the unchanged passing cases; for example, the credential-kinds repair ran four affected launch cases while retaining 19 successes. It opened no review assignment during this sequence. After the last one-case repair, the unchanged grouped invocation passed 152 tests with no failures on frozen tree `244e9dd2`; source then stayed frozen and no review opened immediately. This is useful iterative repair, not C1, because every narrow rerun followed changed evidence and moved a named fixture family to the shared freeze. The later risk is C2 only if more known groups remain unsettled when a full-candidate review starts. Preserve the module failures and the one grouped result, then use one applicable review after the complete candidate boundary rather than reviewing each group.

Stale stop-order premise and useful high-effort counterexample, 22:32-22:39 UTC: turn `127059` ran for 419.8 seconds at high effort, but it produced a material state correction rather than C6 restatement. Exact receipt `att_45378faa` showed that all six heads covered by the installed-0.1.8 delivery stop had delivered post-reap turns with no failures; five had acted within 35 minutes. The seventh visitor-identity lane had no due input and therefore was quiet rather than failed. Gibson remained on 0.1.8, so recovery came from the in-process terminal reap, not the 0.1.9 installation named by the stop order. The same pass reclassified a publication hold to the already-known ruling-visibility defect and obtained a source retraction of an unsupported REST refusal, avoiding pressure on an absent gate. This is useful forensic work despite its duration. It also confirms C3/C5 risk in a recovery policy tied to one expected mechanism: the stop order remains controlling after its factual reachability premise changed, while valid delivery and release follow-through queue behind it. Do not infer authority to lift it. Mike must decide whether to lift it for the six recovered heads while leaving the future-due visitor lane out until it demonstrates a turn. Later treatment should express the hold against observable lane health and exact obligations, not only a future install event, and should re-evaluate once when that condition changes.

False-dark assignment with real dependency stall, 22:42-22:45 UTC: a bounded assignment-only query classified `asg_f46b25af` as quiet for about 150 hours because it had no assignment-bound turn, attest, pending wake or queued/running turn. Session transcript corrected that reading: holder `s_6ec12cdf` had reconciled the exact WIP in unbound turn `126562` on 8 September, preserved focused 6/0, and repeated the same concrete blocker, no authorized same-holder Racter/Eezo destination, transfer and execution route for the one unproven full gate. This rejects the silent-holder S4 diagnosis while confirming a dependency-stalled obligation. Fresh Racter capacity from turn `126943` proved capability but not transfer or execution authority. One wake to the existing parent produced receipt `att_63fcb6f8` and condition wait `w_348d9bc7`, with a one-hour fallback and no new producer, review, card or gate. The wait makes the parent dependency explicit, but installed 0.1.8 cannot bind this ordinary condition wake to the producer assignment, so it does not by itself prove producer-liveness coverage. Later detection must join assignment evidence with holder-session turns before declaring darkness, and later acceptance must show that the supported typed wait prevents a false prod on the producer while preserving exact authority.

Patrol evidence-integrity case, 18:23-18:27 UTC: global sweep 10 reported two Mike-opened dark cards to blocked bare Main. One ID, `asg_3994a3b6-fb1f-422e-aa0f-36e512f26637`, did not exist; the actual Agentd card is `asg_3994a3b6-b637-4671-a7a0-c227f4343fbd`. The ATC card already had holder-authored repeated `bwrap RTM_NEWADDR` capability evidence and was correctly given a capability-block verdict, but should not be repeatedly grouped with ordinary silent holders until that condition changes. Wake `w_3b58bd7e` corrected exact-ID verification, S4-versus-capability classification and reachable routing without waking either holder or rescanning. A stall detector that emits unverified identifiers or routes findings into a known blocked lane creates C5/C6 work even when its broad suspicion is reasonable.

Review-lifecycle boundary, 16:59 UTC: the sole reviewer found corrected tree `7b02d84f` reviewed-clean and reused the focused and full test logs without rerunning either. Tightbeam rejected a new verdict row because the linked review assignment had already closed after its historical `71e2b37f` changes-requested verdict. The reviewer preserved exact successor disposition in `art_40112139` rather than opening the forbidden duplicate lane. The same reviewer then found test-only successor `91c47170` reviewed-clean in `art_73483dc7`, again without a new lane or full rerun. Later analysis should decide whether one review responsibility can durably record successive exact-tree dispositions, or whether candidate freeze must precede closing the review assignment. Do not count these artifacts as normal accepted verdict rows when measuring workflow success.

Effort-request saturation update, 22:52 UTC: a fresh read-only join found 14 open effort requests, ten with assignment attestations later than the recorded `zero_effect` finding. The strongest contradictions had grown to 44 later attestations on `dr_49c58329`, 43 on `dr_2122c3e5` and 17 on `dr_eb6c4210`; all three underlying assignments remained open. None of the 14 requests had acquired a terminal disposition. During the preceding 15 minutes, Main consumed another burst of process effort prompts, including several high-effort turns, while one more remained queued. This strengthens C5/C6 from a stale-row specimen into operator-lane saturation: later useful work does not invalidate the old demand, and notification processing competes with current coordination. The existing R1 acceptance owner remains the only implementation route. Do not add another patrol, decision request or broad census. The next useful evidence is a supported supersession result or a false-supersession counterexample, not another unchanged count.

Verdict-count detector correction, 22:56 UTC: counting multiple `kind=verdict` rows per assignment is not a sound proxy for repeated review. The five highest-count assignments in the preceding six hours used one empty `commitRefs` shape but mixed test receipts, capability or stall classification, notification-churn evidence and final verification. Examples include `asg_b57a7cef` with capability-block, C5, tests-passed and verified rows, and `asg_e9f448ca` with two liveness receipts plus two false-stall refutations. Those are heterogeneous evidence on one responsibility, not repeated independent review of an unchanged candidate. The genuine lifecycle defect remains narrower: after a review assignment closes on a changed-tree verdict, current 0.1.8 cannot durably add the corrected-tree verdict to that same review responsibility, forcing artifact-only dispositions or a successor lane. A review-churn detector must join verdict kind, producer/reviewer identity and exact candidate tree. Raw verdict count alone would create false C1 findings and more corrective work.

Effort-burst outcome correction, 22:57 UTC: the 12 turns from `127069` through `127080` consumed 418.3 seconds, seven at high effort. They were not all churn. Nine fresh requests reached durable rulings between 22:45 and 22:51: six `continue` and three `dismiss`, with all underlying assignments still open. The burst therefore produced useful operator decisions while concentrating cost in Main. It did not resolve or supersede any of the older 14 open requests. Treat this as mixed C6 pressure, not C5 no-outcome churn: measure seconds and high-effort turns per settled request, preserve the decisions, and separate fresh ruled work from stale unresolved rows. The desired change is cheaper direct presentation and automatic invalidation when the premise changes, not removal of operator judgment.

Parallel scheduled-wave counterexample, 23:00 UTC: after a true idle interval with zero queued or running turns, independent wakes `w_fccebe1c` and `w_2a09284d` fired within three seconds on separate lanes. Turns `127087` and `127088` ran concurrently at low effort and delivered in 23.3 and 26.6 seconds. The effort lane durably dismissed `dr_2637e92e`; the receipt-check lane remained bounded to its named evidence check. No retry, duplicate assignment or review appeared. This is useful sequencing, not a stall or C6 burst: idle time before a future due wake is a wait, and separate ready obligations can proceed concurrently without concentrating both analyses in Main. Compare later waves against this shape: exact due wake, separate lane, low effort, one terminal effect and no follow-on reconciliation.

Staggered same-owner wave counterexample, 23:03-23:05 UTC: six existing-obligation receipt checks targeted one owner 17 to 22 seconds apart. Turns `127091` through `127096` all ran at low effort and delivered in 15.2 to 27.9 seconds. The sixth briefly queued behind the fifth, then started and finished normally. Across the cluster, no wake, assignment or attest was created. This is healthy bounded serialization with transient backpressure, not a stall or churn. A queued row is actionable only when its predecessor stops advancing or delay threatens the obligation; momentary ordered queuing on one authority lane should not trigger succession. The stagger was adequate here, though its margin was small because one turn exceeded its 22-second interval.

Cross-assignment effort fanout, 23:06-23:10 UTC: seven effort check-ins fired within four seconds against seven assignments held by the same session. Each prompt independently cited the same six turns and the same shared workspace `/home/mike/.tightbeam/work/8599ba46a398` as `zero_effect`. The seven low-effort turns delivered in 17.5 to 31.4 seconds and opened no decision request, but the controller armed seven assignment wakes due 15 minutes later. The holder then created seven hourly obligation rechecks. This is confirmed C5/C6 fanout even though each individual turn was short: session-level read-only coordination was invisible to assignment-local effect counters, so one shared observation window generated N prompts and two N-sized future wake sets. Wake `w_86fe7637` routed the new specimen to existing R1 assignment `asg_01feb67b`; no new card, review, gate, patrol or census was created. Later acceptance should prevent cross-assignment multiplication without letting activity on one card mask genuine inactivity on another. Measure prompts and future wakes per distinct observation window, not only duration per turn.

Cross-assignment fanout disposition, 23:11 UTC: existing R1 owner `asg_01feb67b` consumed the specimen in low-effort turn `127105`, delivered in 34.1 seconds, and recorded acceptance receipt `att_11200236`. The receipt treats shared session-turn and workspace observations as context rather than independent zero-effect proof for every assignment. Missing assignment attribution stays unknown, not zero effect. Notification may coalesce the shared-window attribution gap, but assignment state, clocks, authority and genuine overdue detection remain separate. The owner selected no implementation mechanism and opened no source, review, gate, patrol, census, timer or ruling. This is a useful one-turn integration of new evidence into an existing design lane.

Repeated-dismiss decision loop, 23:15-23:17 UTC: assignments `asg_12ad80a0` and `asg_059b6ac3` generated effort requests `dr_369967d6` and `dr_44f477df`; both were dismissed within 38 and 33 seconds. Their immediately preceding generations, `dr_646ea4f7` and `dr_064f013b`, had received the same dismissal at 22:45. The later generation therefore spent another notification and ruling cycle on an unchanged disposition after 30 minutes. Historical counts show 70 dismissed effort requests on the first assignment and 75 on the second; the org-wide highest assignment counts include 312 and 221 dismissals. This is confirmed C5/C6 recurrence, not a single false positive. Wake `w_28a63c47` routed the specimen to existing R1 owner `asg_01feb67b`, asking for same-disposition suppression or adaptive backoff keyed to unchanged obligation and premise, with immediate reactivation on material change, missed checkpoint, changed consequence or explicit expiry. No new lane, review, gate, patrol or census was created.

Repeated-dismiss disposition, 23:17 UTC: existing R1 owner `asg_01feb67b` consumed the specimen in low-effort turn `127110`, delivered in 44.3 seconds, and filed `att_f0f70031`. The added acceptance requires durable linkage among exact assignment, obligation, premise, prior disposition and explicit expiry. An unchanged dismissed premise must not recreate the same decision cost on every generic interval. Reactivation remains required for material relevant change, missed checkpoint, changed consequence or expiry. Dismissal is not completion, effect credit, indefinite immunity or permission to auto-rule successors. The owner selected no numeric backoff and changed no current timer, ruling, source, lane, review, gate, patrol or census. This preserves independent reassessment while making repeated same-disposition cost an explicit design target.

Superseded-request queued-turn drain, 23:21-23:25 UTC: seven simultaneous effort requests created ordered turns `127116` through `127122` on one lane. The first request, `dr_5d8f4202`, was dismissed. The other six became `superseded` before their queued turns executed, but every turn still ran at high effort. Queue delay grew from 22.1 to 138.1 seconds; those six obsolete turns then consumed 53.8, 17.4, 15.7, 15.9, 16.0 and 37.1 seconds. The durable decision layer prevented obsolete rulings, but the scheduler did not prevent obsolete model work. This is confirmed C5/C6 and a bounded S2 queue episode, not an execution stall: the lane advanced continuously and all turns delivered. Wake `w_36c03d02` routed one acceptance addition to existing R1 owner `asg_01feb67b`: reconcile request status and generation before model execution, cheaply suppress or dispose superseded work, and recheck after start at tool or mutation boundaries while preserving durable accounting and genuine replacement notification. No new lane, review, gate, patrol or census was created.

Superseded-turn disposition, 23:25 UTC: existing R1 owner `asg_01feb67b` consumed the specimen in low-effort turn `127123`, delivered in 27.9 seconds, and filed `att_8c792065`. The changed acceptance requires authoritative request-status and generation reconciliation immediately before model execution, with superseded work suppressed or cheaply disposed without model reconstruction. A started turn must recheck at tool or mutation boundaries; stale work cannot rule or create continuation effects. Durable turn history, accounting, replacement notification, non-effort traffic and unknown-outcome handling remain intact. The owner changed acceptance only and opened no source, review, gate, patrol or census.

Third assignment-free PR-monitor tick, 23:25-23:27 UTC: wake `w_139f4f7e` fired on time as low-effort turn `127124` and delivered in 48.8 seconds. It found no material PR or George-issue acknowledgment delta and no new internal PR-as-carrier misuse. The turn created zero assignments and zero attests, left the item iceboxed, and created exactly one successor `w_22e2fe4c` for 00:25:52. Unlike the second tick, it did not attempt completion against the stale closed assignment. Three consecutive ticks now support the assignment-free recurring pattern, while only the second exposed stale-context tool selection. Continue checking one-successor cardinality and no-work-row behavior; do not reintroduce a standing assignment merely to make the monitor visible.

Review-evidence reconciliation counterexample, 23:29 UTC: fallback wake `w_5693393d` asked the existing Firehose recovery owner to collect the sole exact `163760db` review outcome or an actual refusal. Turn `127126` re-read the already-terminal evidence and delivered at low effort in 6.1 seconds. It opened no assignment, filed no attest and created no wake. This is correct review reuse, not C1 or C2: a fallback may safely self-dispose when the exact candidate verdict already exists. Review-churn monitoring should count new reviewer execution and new verdict attempts, not every review-related wake or evidence read.

Bounded attention-cost and review-rate audit, 21:12-23:33 UTC: 345 delivered turns consumed 21,028 aggregate execution-seconds; 115 turns were high effort and 100 were effort-related. During the same interval six assignments completed, five were revoked and no assignment opened after the census remained open. This proves movement alongside high coordination cost; turns per completion is not a standalone efficiency measure because monitoring, rulings and evidence work have other valid outcomes. A naive prompt-text match labeled 136 turns review-related, but joining turns to actual review-assignment subjects reduced the set to 15 turns across four assignments. Only two review assignments opened during the interval: `asg_4cdce780` produced one bound turn and one verdict, while bookkeeping-only `asg_6f407b5d` produced its one verdict without a bound review turn. No repeated reviewer execution on a newly opened assignment was found. This rejects raw prompt text as a review-churn detector and supports exact assignment role, candidate identity, reviewer execution and verdict lineage instead.

Cross-assignment dependency-custody counterexample, 23:39 UTC: R1 producer `asg_01feb67b` and sole composer `asg_dd36d2da` have no assignment-bound pending wake, and the composer's last turn and source receipt were at 22:22. Assignment-only inspection therefore looks dark. Exact evidence rejects abandonment: `att_f8846d6d` freezes Group 2 tree `244e9dd2` after 152/0 and explicitly hands next serial disposition to its owner; `att_aa75dc42` consumes that result, keeps the source in sole composer custody and forbids a new group or reviewer until remaining selected acceptance is reconciled. Upstream holder `s_0cd749fc` has pending wake `w_8ce5178e` for 00:37:55 to check the exact next serial allocation. This is a dependency wait with accountable upstream continuation, not S4. A detector must traverse the disposition and custody edge beyond assignment-bound wakes, while reporting the one-hour decision latency separately. No holder wake or replacement was created.

False-dark dependency fallback outcome, 23:45 UTC: condition wake `w_348d9bc7` reached its one-hour fallback without receiving the required same-holder Racter/Eezo destination, WIP-transfer and isolated-execution authority. Parent turn `127140` delivered in 33.4 seconds at low effort and created successor `w_91dafb47` for 01:45:31, doubling the observation interval to two hours. It opened no replacement producer, review or gate and did not repeat the unproven full run. This confirms a genuine dependency stall while showing healthy adaptive waiting: preserve custody and exact condition, record the missed interval, and reduce reminder frequency when nothing changed. The producer remains false-dark only to assignment-local inspection; the upstream parent owns the live retry.

Productive patrol-after-quiet counterexample, 23:48-23:50 UTC: dependency-wake patrol scan 35 (`turn 127145`) consumed 104.7 seconds at high effort but produced material verification rather than another unchanged census. Terminal disposition of `asg_c7f470b6` led the resolver to file condition fact 4084; waiting wake `w_2939619d` fired two milliseconds later, about 42.6 hours before fallback, and session `s_c5054b21` exited with zero open cards, pending wakes or re-polls. The scan also found the Case 7 fanout cohort had grown from seven to twelve open cards, while correctly treating zero fires in that window as phase drift rather than stall. This rejects stopping a patrol merely because the preceding scan was quiet. Saturation needs its own sufficiency rule; new end-to-end repair evidence and cohort growth reset that assessment. Scan 36 remains justified under the existing contract.

Lane B condition-wake costume and bounded cancellation, 23:55-00:01 UTC: fallback turn `127153` found no new disposition after successor `54893761` and reviewed-clean `att_49c34fd3`, yet rearmed `w_2c4f44e5` for another two hours despite its renew-once wording. Dependency patrol turn `127154` bounded the chain as Case 8: 56 wakes across 6-9 September, 30 bare timers and 26 condition wakes over six minted kinds, with only one matching fact ever filed and zero for the current `firehose-lane-b-successor` kind. Earlier kinds tracked real changing consequences, but the tail repeated the same anchor; nominal Lane B assignment `asg_7711618b` was already revoked. This low-frequency chain stayed below the patrol's three-per-30-minute sweep threshold, exposing a detector blind spot. The correct shape is one condition kind with a committed resolver and a fallback that stops rather than rearms when no fact arrives. User-principal cancellation was correctly refused because the wake was agent-owned. One bounded request to exact creator `s_e72a9fe9` produced turn `127158`; after verifying current state it canceled `w_2c4f44e5` at 00:00:18 UTC and created no replacement. Source, review and real dependency evidence remain intact.

Source correction without workflow restart, 00:00-00:02 UTC: recovery owner turn `127159` found that its earlier relay had left assignment `asg_0d0e75e4` on two false grounds: opener `s_fde9b2be` was described as queue-blocked pending a 0.1.9 installation, although the observed recovery was an in-process reap on the still-installed 0.1.8 release, and a fresh independent review was described as missing although exact candidate `4ddc6977` already had reviewed-clean receipt `att_39c45e37`. The owner withdrew only those unsupported premises and preserved Mike's formally unrescinded contact stop, absent owner landing direction and every push, release, install and runtime boundary. It sent one bounded correction to the non-stopped holder. Low-effort turn `127164` filed `att_f014b244` on the existing assignment in 23.4 seconds, created no assignment or wake, and reran no review, test or gate. This is useful evidence correction rather than C5 churn. A stale premise should be corrected where it is recorded, without treating the correction as authority to restart work. The remaining hold must name the real current constraint so a later authorized decision does not inherit obsolete technical gates.

Fanout recurrence under adaptive backoff, 00:07-00:10 UTC: seven assignment-local effort checks for one shared holder and workspace arrived within 19 milliseconds. Five pre-existing dependency probes then joined the same ordered lane. The resulting 12 low-effort turns consumed 231.1 aggregate execution-seconds and reached 131.1 seconds of queue delay. The seven effort checks each armed a separate successor between 02:07 and 02:09. This repeats confirmed C5/C6 notification fanout after the same seven-assignment pattern at 23:08 and the superseded-request drains at 23:21 and 23:52. Two-hour adaptive backoff reduces frequency but leaves cardinality unchanged. It also delays time-sensitive inspection because every co-located obligation waits behind the others. Do not count the dependency probes themselves as unchanged churn: patrol scan 36 later established that six of seven strands fired in its wider window and all six carried new upstream anchors rather than identical prompts. Existing R1 receipt `att_11200236` already accepts session-window coalescing, and `att_8c792065` accepts pre-model supersession checks, so no duplicate design lane or review was opened. Later acceptance must coalesce the shared observation and notification work without merging assignment state, clocks, authority or genuinely changed consequences.

Third superseded-request execution recurrence, 00:22-00:25 UTC: the same seven-assignment cohort raised effort requests within five seconds. The first request `dr_45875c18` was dismissed, and the other six became superseded before their turns executed. All seven turns nevertheless ran at high effort for 138 aggregate execution-seconds. This is the third direct reproduction after 23:21 and 23:52, now following a 00:07 low-effort check-in burst on the same holder. It confirms that durable request supersession still does not prevent model execution on live 0.1.8 and that repeated observation can escalate into higher-cost decision churn within one interval. Existing R1 acceptance `att_8c792065` already covers authoritative generation and status reconciliation before model execution, so no new lane, review or request was created.

Fourth assignment-free PR-monitor tick, 00:25-00:27 UTC: wake `w_22e2fe4c` fired on schedule as low-effort turn `127188` and delivered in 49.5 seconds. The monitor reported no material change across the established 11 PRs, George's acknowledged issues or internal PR-as-CI misuse. It left the work item iceboxed, created zero assignments and attests, and retained exactly one successor `w_77fa5aad` due at 01:25:52. Four consecutive ticks now support the assignment-free monitor as a stable reduction in durable-row churn. The second tick remains the only one that attempted stale assignment completion.

Open-review census, 00:30 UTC: only two review-linked assignments remain open, and neither represents an active repeated review. Spec review `asg_56effb86` opened 27 August, executed four assignment-bound turns, filed one changes-requested verdict `att_5c39b85f` and then remained open because live 0.1.8 repeatedly refused both completion and surrender with `open work item ... has no liveness trigger`. Later turns preserved the same verdict and refusal rather than reviewing again. Post-rollback review `asg_dd66f64a` opened 2 September, executed four assignment-bound turns and filed no verdict. Its latest receipt `att_8fd3a781` says the requested exact host-state certification is impossible from the preserved baseline and retains explicit STOP and HALT rulings; it did not repeat host inspection. The first row is stale lifecycle state after completed review work. The second is blocked evidence custody. Counting open review rows, review-related prompts or repeated closeout attempts as review iterations would misclassify both. A review-churn detector must require new reviewer execution against the same candidate or unchanged repair, not merely an open review assignment.

No supported terminal path for stale review, 00:31-00:33 UTC: one bounded cleanup request reached exact opener `s_9e795c9b` for completed review `asg_56effb86`. After re-reading the single verdict and prior holder refusals, the opener invoked `revoke-assignment` once. Live 0.1.8 returned the same `open work item wi_d9cf7b4b... has no liveness trigger` refusal that had blocked holder completion and surrender. The 84.3-second high-effort turn changed no row, repeated no review and created no continuation. One malformed local `jq` filter caused a read retry inside the turn but no mutation. This establishes a structural lifecycle stall: holder completion, holder surrender and opener revocation all reject for the same missing-trigger invariant, leaving finished review work permanently represented by an open assignment. Do not retry another terminal verb or restore recurring liveness merely to satisfy closure. Later lifecycle acceptance needs a lawful opener disposal path that does not require manufacturing future work for an already-finished obligation.

Terminal-path disposition, 00:35-00:36 UTC: existing R1 owner `asg_01feb67b` absorbed the specimen in low-effort turn `127192` and filed `att_d0c2f774`. The added acceptance says legitimate terminal disposal cannot require manufacturing future work or a liveness trigger when no promised output remains. An authorized terminal transition must reconcile existing liveness state without adding a timer or request, pretending unfinished output is complete, or erasing verdict, refusal and history. Completion evidence, surrender meaning and opener or current revocation authority remain distinct; this is not blanket completion or automatic closure. The owner explicitly recorded that current reviewed R1 evidence does not yet prove this all-three-path case and opened no new work item, source lane, review, gate, patrol, census, timer, request or retry.

Useful-turn fanout amplification, 00:37-00:39 UTC: dependency probe `w_3aa2662f` ran one valid low-effort coordination turn on shared holder `s_0cd749fc`. Nineteen seconds after it started, live supervision emitted seven assignment-local check-ins within three seconds because the shared turn created no assignment-bound write, artifact, attest or work-item update. Those seven false prods delayed the separately scheduled useful dependency probe `w_8ce5178e` by 51.6 seconds. The nine-turn sequence consumed 105.4 aggregate execution-seconds. Each check-in also armed an effort decision probe for 00:52:48-00:52:51, creating the same later high-effort risk seen at 00:22. This establishes the causal loop behind the fanout: useful session-level coordination appears as zero effect on every held assignment, N check-ins serialize ahead of the next useful turn, and N delayed decisions follow. The second useful probe did not create another cohort because the seven decision probes were already pending. Existing R1 receipts `att_11200236` and `att_8c792065` cover the required shared-window attribution and pre-model suppression. Do not send another unbound diagnostic prompt into the affected holder merely to report the loop; that message can become its next trigger.

Fourth superseded-request drain and full cascade cost, 00:52-00:55 UTC: all seven decision probes created by the 00:37 false-prod fanout became high-effort model turns. The first request `dr_d9b1d5c6` was dismissed. The other six became superseded before execution, yet all six ran. This decision stage consumed 140.5 aggregate execution-seconds and reached 118.4 seconds of queue delay. Combined with the preceding useful-turn and false-prod stage, two useful dependency probes induced a 16-turn, 245.9-second sequence: two useful probes, seven false check-ins and seven decision turns. Fourteen turns were coordination overhead. This fourth direct superseded-drain reproduction confirms the whole amplification path and shows that a short low-effort prod can defer much larger high-effort cost by 15 minutes. No new design lane was opened because `att_11200236` and `att_8c792065` already accept cohort attribution and pre-model suppression.

Open-request backpressure counterexample, 00:16-01:01 UTC: unchanged requests `dr_29a6f57d` and `dr_5c4fc707` remained open on two assignments with long dismissal histories and did not regenerate during the next 45 minutes. In contrast, the seven-assignment cohort whose first request was dismissed and remaining requests superseded at 00:22 regenerated at 00:52 and repeated the full high-effort drain. This supports a narrow live-0.1.8 containment fact: an open request suppresses its next arm, while dismissal on an unchanged premise restarts the cycle. Do not treat leaving requests open as the product fix. It creates unresolved operator attention, may mask changed consequences and cannot replace durable premise-linked suppression with explicit expiry and reactivation. Until that mechanism ships, abstaining from repeated identical rulings can cost fewer turns than dismissing every generic interval.

Operator-stop conflict and repeated escalation, 00:42-00:44 UTC: standing recovery turn `127202` verified that provider-error candidate `4ddc6977` is finished on both target branches, independently reviewed and green on hosted Linux and macOS. Holder `s_14f59e5e` withdrew both obsolete technical grounds and retains no push authority. Its sole landing owner `s_fde9b2be` remains covered by Mike's formally unrescinded seven-head contact stop, so no alternate owner can lawfully route the finished work. At the same time, Mike's channel has directly delivered instructions to two stop-listed heads, including `s_fde9b2be`, which makes capability visible without changing the formal order. The recovery owner has now raised the stop-order conflict three times. This latest high-effort turn consumed 68.4 seconds, filed no row and rearmed one standing continuation for 01:23:28. This is a product stall caused by conflicting operator state and a C5/C6 escalation loop, not an engineering or review failure. Do not generate another reminder or infer that direct contact silently rescinds the broader stop. The next action requires Mike to retain or lift the formal stop; until then preserve the candidate and suppress repeated escalation.

Duplicate PR31 wait removal, 00:44-00:49 UTC: slate-clear turn `127203` spent 82.9 high-effort seconds revisiting external PR #31 at unchanged head `a36144b9`. The exact-head review was already complete with changes-requested verdict `att_4bbd3c34`; source follow-up `wi_e957ec0a` already existed with zero assignments and was iceboxed. The turn kept review-continuation item `wi_5739c286` open and retained daily slate wake `w_79423fed`, duplicating the assignment-free hourly PR monitor that already checks all 11 heads. One bounded reconciliation turn `127206` consumed 71.9 high-effort seconds, iceboxed `wi_5739c286`, canceled the daily wake and left `wi_e957ec0a` iceboxed. It opened no assignment or replacement wake and preserved the verdict. This is a one-time C5 reduction: external-head waiting now has one detector, and a new head can trigger a proportionate successor review rather than a daily slate-clear cycle.

PR31 changed-head review churn, 26 August-8 September, audited 01:04 UTC: authoritative verdict `commitRefs` plus the one documented `invalid_commit_refs` fallback at `a36144b9` identify 21 actual independent reviews across 21 distinct PR commits. No commit received two independent review executions, and every review ended changes-requested. The broader set of 50 assignments whose subjects name independent PR31 review consumed 143 assignment-bound turns and 12,529 aggregate execution-seconds. The 21 actual review assignments account for 24 turns and about 6,842 seconds; the remaining coordination, replacement and empty review-related cards account for about 119 turns and 5,687 seconds. This is excessive review iteration without same-head duplicate execution. Incremental author heads repeatedly triggered full-diff review before the prior blocking set was closed. A new commit alone should not commission another review. Require one consolidated readiness package that maps every outstanding blocker to changed evidence, binds current green CI and current-head product evidence, and updates stale PR claims; then run one independent review. Until that threshold, record the head delta in the existing monitor and do not create another review card.

PR31 review-threshold control, 01:08-01:09 UTC: one bounded low-effort correction turn `127227` consumed 48.6 seconds, canceled old monitor wake `w_77fa5aad` and created exactly one replacement `w_dbec8cb1` at the unchanged 01:25:52 deadline. The assignment-free prompt now embeds the 21-head audit and says a new PR31 head records a delta only. It may route one independent review only after a consolidated current-head package maps every outstanding blocker to changed evidence, has current green CI, supplies current-head product evidence and corrects stale PR claims. The correction opened no assignment, work item, review, test, gate or request. This turns the historical finding into an active sequencing control: author remediation can proceed in parallel outside the org, while expensive review remains serialized behind an explicit readiness boundary.

Empty patrol attention cost, 00:48-00:51 UTC: dependency patrol scan 37 found zero self-poll groups and zero prods, contacted nobody and armed sole scan 38. The high-effort turn nevertheless consumed 141.6 seconds. Do not label this single empty scan confirmed churn because productive scan 36 reset the patrol's three-quiet-window freeze counter and the longer Case 7 cycle can skip a 30-minute window. Count the attention cost as a C6 candidate. If the next two windows remain empty, the existing sufficiency rule must stop the cadence; if a later scan finds material change, compare that benefit against the aggregate high-effort cost rather than treating every scan receipt as proof of value.

Cadence-misaligned patrol correction, 01:18-01:22 UTC: scan 38 was also empty and consumed 72.3 high-effort seconds, bringing two consecutive empty fixed-clock scans to 213.9 seconds. Both left Case 7 freeze at 0/3 because no relevant strand wake fired, so the 30-minute cadence could run empty windows forever without advancing its own stop condition. One bounded correction turn `127231` consumed 74.4 seconds, reconciled all seven relevant pending wakes due 02:07:48-02:09:58, canceled unwinnable 01:49 scan `w_a43cf488` and created exactly one scan `w_baaec329` for 02:12:48, five minutes after the earliest relevant due. Future scans align to the next relevant strand due. An aligned but empty opportunity advances a bounded counter, with three consecutive empty opportunities routed to the stall owner. No contact, replacement patrol, review, gate or request was created. This changes the detector from fixed polling to evidence-timed observation while retaining the chance to see the known fanout.

Narrow external-issue monitor, 01:12-01:18 UTC: Omarchy issue 3168 watcher `s_a5e436d5` had run five high-effort turns for 973.2 seconds in eight hours. Its last four scheduled checks found the same unanswered issue, labels, comments, source head and absent catalog entry; only unrelated marketplace commits changed. The original assignment requires a two-hour cadence, so that interval was preserved. One 95.2-second correction turn canceled broad wake `w_837b9d1a` and created exactly one replacement `w_af019788` at the same 03:14:59 deadline. The new prompt reuses `att_98ae5f95` and queries only issue state, maintainer response, source head, workflow terminal changes and exact catalog membership. It ignores unrelated marketplace HEAD and history and does not reread the full attest chain unless a named field changes. This reduces per-tick work without weakening the user-required monitor or Hold.

Monitoring evidence triggered unrelated release watch, 00:54-01:00 UTC: R11 fallback turn `127215` spent 107.3 high-effort seconds after observing that the specs repository moved from `21a49609` to `4904129`. The only named new commit changed `tightbeam-learning-loop-v1.md`; `0.2-build-ledger.md`, product main `944feaa1`, candidate `bef717c9` and every R11 custody marker remained unchanged. The watcher still filed `att_786e27eb` as new canonical specs drift and scheduled another eight-hour fallback. This is detector interference: commits that record churn can themselves appear as release progress when a watcher keys on repository HEAD rather than relevant content. One bounded correction turn `127216` cost 144.6 high-effort seconds, canceled broad wake `w_798b21d1` and recreated exactly one wake `w_149ca7f5` at the same 08:56:38 deadline. Its premise is now the `0.2-build-ledger.md` blob `fdfffbf4` plus named R11 markers and explicitly ignores unrelated specs HEAD movement. No source, review, test, gate, request or owner nudge was created. Count the correction cost, but future learning-ledger commits should no longer trigger this release watch.

Fifth superseded-request drain, 01:23-01:26 UTC: the same seven-assignment cohort produced seven more delivered high-effort decision turns, `127232` through `127238`. They consumed 146.6 aggregate execution-seconds and reached about 125 seconds of queue delay. This is the fifth direct recurrence of the same delayed decision cost after unchanged assignment-local check-ins. It adds no new mechanism beyond the established fanout and supersession defects, so no new design lane, review or diagnostic wake was opened. Keep the sample as evidence that adaptive backoff changes frequency but does not bound per-cohort fanout or prevent superseded model execution.

False-negative query correction, 01:23-01:28 UTC: recovery turn `127239` consumed 304.9 high-effort seconds and corrected its own claim that bare `main` had shown no input or turns. The prior query matched sessions by hexadecimal id, but bare `main` has no such id. An empty match was reported as inactivity rather than an unsupported result. The corrected count was 25 delivered turns in the quoted window and 72 since, with no failures. This strengthens the finding that all seven stopped lanes can deliver while the formal contact stop remains in force. The turn did not repeat the stop-order request, but it rearmed its standing continuation. Classify this as a detector false negative caused by identity-shape assumptions and absence-of-evidence handling, not as a lane stall. A lane-health query must resolve the exact session key first, report unmatched identities as unknown, and never convert an empty match into a negative health fact.

Fifth assignment-free PR-monitor tick, 01:25-01:27 UTC: corrected wake `w_dbec8cb1` delivered as low-effort turn `127241` in 120.3 seconds. It found no material change across the 11 open PRs, George's acknowledged bugs or internal PR-as-CI misuse. PR31 remained changes-requested at the reviewed head, and the consolidated readiness package was absent, so the monitor commissioned no review. It created no assignment, attest, artifact, test, gate or request and scheduled exactly one successor, `w_a91492c2`, for 02:25:52. The higher duration than earlier ticks is attention cost worth watching, but the fifth clean lifecycle confirms that the active readiness threshold prevents changed-head review churn while preserving hourly delta detection.

Unbound dependency wake remains invisible to assignment supervision, 01:28-01:33 UTC: Firehose assignment `asg_c8f9d0f8` already had concrete owner-disposition wake `w_18e37d51` pending for 01:32:14, but that wake has no `assignmentId`. Supervision still fired assignment-local check-in `w_9f3c58fb` at 01:28 and armed effort probe `w_5c73444b` for 01:43. Turn `127242` consumed 14.6 low-effort seconds. The holder detected the existing continuation and created no duplicate wake, receipt or decision request. The real dependency wake then fired as turn `127243`, consumed 28.6 low-effort seconds, found no new disposition and rearmed one exact check for 02:02:38. The assignment-local 01:43 probe remained pending. This is successful agent-side containment but not scheduler suppression: one real dependency wait is represented on two independent clocks because the supported wake path cannot bind its assignment. The case belongs to the already-escalated wake-assignment binding gap. Do not prod the holder or open a duplicate design lane. Later acceptance should let supervision recognize an existing exact dependency checkpoint before it emits a check-in or decision probe. Until then measure whether recent useful turns suppress the next probe; do not add a third coordination trigger.

The 01:43:15 effort probe then fired without creating a model turn or decision request because turn `127243` was recent. It armed only the next process probe for 01:58:15. This confirms process-side recent-turn suppression works, but the 30-minute dependency cadence and 15-minute supervision cadence remain out of phase. They can alternate between a free suppression and a false check-in four minutes before the useful dependency read. Binding the exact checkpoint would remove the false half-cycle without increasing dependency-read frequency.

Sixth shared-holder check-in fanout with agent-side containment, 01:38-01:40 UTC: seven assignment-local probes fired within four seconds on `s_0cd749fc` even though every assignment already had an exact dependency wake due 02:07:48-02:09:58. Turns `127247` through `127253` serialized on the holder, consumed 62.3 low-effort execution-seconds and reached 52.4 seconds of queue delay. Each turn found its exact pending wake and created no duplicate wake, receipt, review, test or work row. Supervision still armed seven decision probes for 01:53:34-01:53:37. This is partial containment: the holder now avoids multiplying durable work, but the scheduler still pays seven model turns to rediscover seven rows it already owns. Recent-turn suppression may prevent the 01:53 decision stage; verify that outcome before counting a sixth high-effort drain. The product fix remains session-window attribution before model execution, with assignment-local consequences preserved after the shared observation.

Sixth superseded-request drain, 01:53-01:57 UTC: recent-turn suppression did not prevent the decision stage. All seven probes raised fresh requests and turns `127258` through `127264` ran serially at high effort. The first request `dr_0c5f7ca7` was dismissed; the other six became superseded but still executed. The stage consumed 164.9 aggregate execution-seconds and reached 145.2 seconds of queue delay. Combined with the preceding check-in stage, this unchanged cohort cost 227.2 execution-seconds across fourteen coordination turns despite every holder response recognizing the already-pending exact dependency wake. Agent-side duplicate avoidance therefore cannot contain model or queue cost. Dismissing the first unchanged request restarts the same generation loop; authoritative pre-model cohort suppression remains necessary.

Serial-allocation carry stall broken without duplicate execution, 01:55-02:00 UTC: release coordination had consumed reviewed-clean Group 2 tree `244e9dd2` at 22:22 but selected no next execution for 3.5 hours. One bounded follow-through turn `127265` chose per-session instruction-channel wiring and affected isolation tests, filed `att_e87765c5` and woke the existing integrator. Integrator turn `127266` did not edit or test. It found that the current Codex route depends on adapter patches, pinned evidence for an unmodified channel is missing, and Identity guidance assembly overlaps the separate bundle. Owner turn `127267` then split the work: the same integrator may compose only the reviewed bundle-context hunks and run affected renderer tests, while replacement-channel compatibility remains blocked for pinned Codex 1.1.4 and Claude 0.73.0 and the old path stays intact. This is correct sequencing and parallelism. It advances an independent slice, preserves one source owner, and converts vague waiting into an exact compatibility prerequisite without commissioning another review or repeating green gates.

Cross-agent evidence relay was overlapping but sequentially useful, 01:58-02:02 UTC: Kestrel evidence that REST turn `126960` held no SHA-specific refusal reached Firehose holder `s_b0d2e367` through two near-simultaneous Mike-authored wakes. Both became model turns rather than deduplicating. Turn `127269` consumed 85.2 low-effort seconds, read the original source, recorded `art_6b9a4331` and `att_2ad6e29a`, and notified the owner. Queued turn `127270` then consumed 129.3 seconds and used that disposition to start the missing exact `54893761` isolated Eezo verification as handle `37490`, preserving the pending collection wake and every export, target and runtime hold. The two prompts shared evidence but produced different ordered effects, so do not label the second pure duplicate churn. The 85.2-second serialization delay is still coordination cost. Future cross-agent relays should name the already-filed evidence id and requested next effect so an evidence-intake turn can collapse into the executable follow-through when authority and custody already match.

Parallel evidence work with serialized source custody, 01:59-02:07 UTC: Morrow supplied a hash-verified 0.1.8 archive and corrected the prior metadata-only premise. Owner turns `127271` and `127274` recorded `att_51582dd1` and `art_ccf0bf3b`, narrowing the remaining upgrade evidence to a genuine old-binary-created fixture, final package binding and migration/rollback execution. Follow-through turn `127277` then filed `att_27b48255` and queued one fixture-preparation instruction to existing integrator `dd36`, explicitly after active Group 5 custody permits. Group 5 turn `127272` finished first: frozen tree `1d2c5c33` contained only the authorized Identity hunks, and its retained renderer log reported 10 tests with zero failures. The numeric process exit was unavailable, so the integrator did not rerun solely to manufacture that datum. Only after `127272` delivered did queued fixture turn `127281` start on the same integrator. It created no second integrator and did not start final-candidate testing or live installation. This is the intended shape for parallelism: evidence acquisition and prerequisite correction proceed independently, while source-affecting work stays serialized through one owner with an explicit order.

Prompt-difference false positive in the aligned patrol, 02:07-02:16 UTC: seven scheduled Case 7 dependency turns consumed 155.1 low-effort execution-seconds and reached 218 seconds of queue delay while unrelated useful turns interleaved. Authoritative assistant outcomes show two material advances, turns `127285` and `127286`, and five explicit no-change outcomes, turns `127289`, `127291`, `127293`, `127294` and `127297`. Scan 39 captured the full cohort but initially called all seven advanced because none reused its prior prompt text. That method measures wording, not downstream progress, and would reset the quiet counter indefinitely as prompts acquire new evidence anchors. One bounded correction turn `127306` consumed 55.6 high-effort seconds, amended the patrol ledger and set the window to two advanced and five unchanged. Those five now stand at 1/3 consecutive fired windows without material advance. The patrol canceled and recreated only its existing scan-40 wake at the same 04:15 deadline, leaving exactly one successor. Future advance requires a semantic downstream outcome or durable changed evidence; prompt identity may screen a definite repeat but cannot prove progress.

Shared-DB timeout and internal supervisor restart recurrence, 02:16-02:17 UTC: while serving a 200-message transcript query, `GenServer.call(Tightbeam.DB, ...)` exceeded its five-second timeout. The lane manager then timed out reading unpublished terminals, and the wake scheduler timed out reading due wakes. Both terminated. Turns `127295`, `127303`, `127304`, `127307`, `127308` and `127309` ended `failed_unknown` with interrupted outcomes; turn `127300` failed immediately with `adapter_unavailable: shutdown`. The systemd service remained active, and the internal router and supervisors restarted in process by 02:17:04. New turns `127314` and `127315` were running afterward, so do not restart the gateway or declare a provider outage. This reproduces S1 under current load and adds a concrete trigger shape: one broad transcript read can monopolize the serialized DB long enough to crash scheduling and lane reconciliation. Narrow external reads reduce observer contribution but do not fix internal callers. Later acceptance needs bounded or paged transcript reads that cannot starve scheduler queries, plus terminal reconciliation that preserves unknown-outcome semantics without replaying stale queued instructions.

Unknown-outcome reconciliation after the DB timeout, 02:17-02:24 UTC: recovery turns did not blindly replay the interrupted operations. Owner turn `127317` consumed the persisted channel investigation, filed `att_eb8c272b` and routed only its next evidence-binding step. Integrator turn `127318` reconciled retained fixture files and process state before execution: interrupted turn `127295` had completed the source trace, while the fixture directory held only the verified archive and extraction and no fixture process remained. It then used the exact 0.1.8 binary once to create and reopen an isolated synthetic schema, obtained exit 0 and `integrity_check = ok`, left no process, filed `att_4c9a7913` and scheduled one continuation for missing workload rows and pre-upgrade snapshot. No migration or final-candidate test ran. Independent-review recovery `127313` initially remained queued while four other Codex Tightbeam turns occupied capacity; Agentd returned a complete roster snapshot. It ran when capacity cleared and found that the interrupted review had already filed verdict `att_3e0d88ce`, completion `att_cdcd7fbc` and report `art_c420ba1f`. The 18.9-second recovery turn classified its prod as stale and did not repeat review. This confirms capacity delay rather than a dead reviewer and shows the required S1 discipline: inspect durable and filesystem effects first, preserve partial success, then execute only the missing suffix.

Cold-load scope reconciliation avoids an unnecessary seam expansion, 02:22-02:27 UTC: pinned adapter tracing established that warm Claude sessions ignore guidance-only changes and the selected Codex engine identity and hook contract remain unproven. The first owner read correctly framed a possible product choice between next-cold-load semantics and generalized turn-boundary close/reload. Before escalating, turn `127330` compared the approved architecture and acceptance requirements against existing behavior. It filed `att_ab941a9f`: approved records permit next-cold-load semantics, so the integrator may continue cold-load and first-request isolation proof without adding generalized reload or claiming warm refresh. Exact immutable Codex engine identity remains the prerequisite. This is productive scope control, not a weakened acceptance bar. It resolves the apparent decision from existing architecture, avoids a new active-session lifecycle seam and keeps implementation behind one evidence boundary.

Sixth assignment-free PR-monitor tick, 02:25-02:27 UTC: turn `127334` found no material delta across the 11 open PRs, George's acknowledged issues or internal PR-as-CI misuse. PR31 retained its head, green CI and changes-requested decision but still lacked the consolidated current-head readiness package, so no review was commissioned. PR30 retained its macOS failure. The turn created exactly one successor, `w_539a89a9`, for 03:25:52. Six consecutive assignment-free ticks now show that one delta monitor plus an explicit readiness threshold can preserve awareness without changed-head review churn or durable assignment noise.

Exact-checkout proof advanced into a new privacy hold without rerun, 02:12-02:29 UTC: existing Eezo run `37490` completed against clean `54893761` with Mix 1,940 tests plus nine doctests at zero failures, Rust 298 passing tests and local package checks passing. Collector turn `127299` verified the same run and log hashes, filed `att_71048569`, canceled its scheduled collection and did not launch another gate. A later read-only hosted-operation packet found no exact-SHA run and no candidate ref, but performed no push. Before publication, turn `127335` screened all 17 commits and 190 baseline-relative blobs and found unresolved credential/path and identity-pattern matches. It disclosed only object IDs and categories, filed sanitized report `art_927da3b6` and stopped. Turn `127342` then recorded `att_9bd240d0`: no public push or hosted CI while the candidate-publication decision remains undecided. This is correct gate ordering. Exact source verification finished once, then a distinct privacy prerequisite blocked external publication without invalidating or repeating the green proof.

Cold-load proof completed before fixture continuation, 02:27-02:30 UTC: integrator turn `127338` added one focused test for the approved close-then-cold-load path. Its first run stopped at compilation because the test was inserted inside the following declaration; no test executed. The agent fixed only that placement and reran the changed selector, which passed one test with zero failures and exit 0 on frozen tree `19f3ee33`, recorded in `att_fb523500`. This proves Tightbeam request ordering with separate guidance, unchanged session identity fields and model setup before resumed prompt. It does not claim warm refresh, provider behavior or the missing Codex engine contract. Only after `127338` delivered did queued fixture row-seeding turn `127340` start on the same integrator. The single failed compile and corrected selector are bounded implementation feedback, not repeated gate churn; broader reruns or review before engine evidence would be premature.

Fixture row seeding preserved the serialized suffix, 02:30-02:32 UTC: turn `127340` first inspected the existing 0.1.8 fixture and old APIs, then committed only missing core rows with exit 0: two synthetic tenants, three legacy facts, three pending wakes and two queued turns, with the condition wake at cursor 3. It filed `att_60dd3fbc` and left fired and canceled wakes, unknown terminal state, committed-effect evidence and the quiesced snapshot for one continuation `w_27a60735` at 02:37:02. The turn did not recreate the schema, recopy the archive, replay committed rows or modify Group 5 source. This is useful staged fixture construction because each turn adds a named disjoint suffix and preserves exact remaining work; it becomes churn only if a later continuation redoes committed rows or fragments the remaining terminal snapshot without a new safety boundary.

Old-version fixture suffix completed without replay, 02:37-02:40 UTC: continuation turn `127371` resumed from the committed core rows and used old terminal and wake-disposition APIs only for the missing states. The exact 0.1.8 binary completed terminal and wake state preparation with exit 0; scheduler delivery stayed inside the local fixture callback. The agent checked committed-effect evidence in `lifecycle_events`, reopened the quiesced snapshot with the old binary, verified selected rows remained exact and left no fixture process. It filed `att_5dc0a255` and snapshot `art_66cc1c83`; source `19f3ee33` remained unchanged. No final 0.1.9 migration, restart or rollback ran or was claimed. The old-version fixture is now a completed prerequisite. Do not schedule more fixture construction; the next serial operation must wait for exact final-candidate identity and preserve the recorded rollback coverage limit.

Cross-agent race on missing O2 fixture coverage, 02:42-02:50 UTC: Morrow correctly found that immutable snapshot `art_66cc1c83` had null assignment and job references and could not exercise legacy O2 episode reconciliation. While the external audit's owner wake waited about 150 seconds in `s_0cd749fc`'s queue, a separate R1 acceptance handoff independently fired `w_3a6bb8e1` to integrator `dd36` for the same gap. Owner turn `127394` then filed method supplement `att_bb2d02c4` and fired `w_a63b6e1e` one minute later. The two integrator turns serialized rather than running competing workers. Turn `127395` consumed 217.7 seconds, preserved the original snapshot hash and created a separate old-binary enriched copy with two assignments, one artifact, one attest and a live review-remedy episode; seed and reopen exited 0, recorded as `att_0f4b3f18` and `art_349775a9`. Turn `127398` consumed another 87.7 seconds, detected that the method was already satisfied, did not reseed or rerun reopen tests, and performed only a read-only original-versus-derived row comparison, filing `art_a13fe0dd` under `att_e47e0e13`. This is safe execution-time deduplication with useful residual evidence, but still 305.4 seconds of serialized attention. A cross-agent handoff registry keyed by obligation and intended effect could have merged the preservation method into the first pending request before either model turn, while retaining the independent evidence source.

Conflicting operator records on a public operation, 02:25-02:53 UTC: decision `dr_bd16e35d` asked whether to publish exact candidate `54893761` to one public candidate branch so hosted CI could run. Main recommended WITHHOLD at 02:26:29 because the required history privacy scan was incomplete. The authorized owner recorded that direction in `att_9cde24fd`. At 02:29:40 the durable decision row nevertheless changed to `authorize-candidate-only`, attributed to `user:mike` with no ruling session; its rationale says an intermediary used standing delegation. This was not the same judgment under a newer premise. The privacy scan had already stopped on unclassified matches, and later bounded classification retained 102 attribution occurrences, 66 environment-comment occurrences and 5,068 unresolved source or example occurrences. Agents therefore preserved the privacy stop despite the ruled row. Read-only `git ls-remote` at 02:55 returned no named candidate ref, and subsequent receipts explicitly report no push or hosted CI. No public mutation occurred. This is confirmed C3/C5 authority conflict plus C6 reconciliation cost: the same user attribution represented opposite decisions, the row could not express that the later privacy condition still governed, and several agents spent turns recovering provenance and precedence. A public or otherwise consequential operation needs one executable authorization record bound to its exact preconditions and evidence version. A later unresolved stop must make that authorization non-executable without erasing its history. Delegated actions also need the acting principal or session, not only the human user's transport identity. Detectors should flag opposite dispositions on the same operation, a ruling whose evidence predates a later stop, and execution requests that lack one current condition-complete authorization. The successful no-push outcome is a containment result, not proof that the seam is sound.

Seventh shared-holder effort fanout, 02:54-02:57 UTC: six `zero_effect` requests fired within six seconds for six open assignments held by the same session, `s_0cd749fc`. Bare Main serialized them as six high-effort turns while an unrelated release task and the remaining requests queued. All six eventually produced useful `continue` rulings and six distinct pending holder continuations, but consumed 189.9 model-execution seconds and reached 171.7 seconds of queue delay. The first request, `dr_5cf8be89`, reached its ruling after 30 seconds, but its model turn ran for 109.9 seconds. It first tried to dismiss the check-in, hit the standing-instruction refusal, then reconstructed the allowed continue path. The later five turns reused the correct standing rule and took 12.2 to 24.5 seconds each. Every assistant response then named the canceled deadline wake as the new “pending continuation wake”; read-only rows show the actual pending continuations have different IDs. The durable effects were correct, but all six human-facing receipts were false. This is confirmed C6 fanout with a C5 retry and a C5 provenance defect. It does not prove that all six obligations deserve the same final disposition; their product consequences differ. The shared work is the holder-liveness observation, current session state, applicable standing rule and continuation deduplication. Compute that once, attach it to every affected request, then present only the distinct obligation-specific choices. A detector should group simultaneously due effort checks by holder and observation interval before commissioning model turns, reject an action already barred by the request's governing rule without spending a model-tool round trip, and verify any reported successor ID against the post-ruling pending row rather than echoing the consumed deadline ID.

Cheap stale-wake reconciliation counterexample, 02:59 UTC: Firehose collection wake `w_f491ac39` fired 30 minutes after creation with two stale premises, that privacy classification remained pending and `dr_bd16e35d` was undecided. Turn `127426` took 7.8 low-effort seconds, recognized the completed and attached classification, corrected the decision to ruled candidate-only while preserving the later privacy stop, and created no scan, request or publication effect. This is healthy C5 suppression. Old queued text need not be canceled perfectly if execution starts with a cheap current-state comparison and exits without reconstructing the completed work.

One serial integration release after parallel evidence preparation, 02:57-03:00 UTC: source owner `s_0cd749fc` consumed the independently prepared engine recommendation and exact source check in turns `127422` and `127423`, filing one scoped provenance receipt without claiming binary or hook acceptance. In parallel, release owner turn `127419` consumed the already-green pure guard module and focused 7/0 log, but correctly treated that as intake rather than shared wiring completion. Turn `127427` then reconciled the no-edit state and issued one bounded release, `att_9dc5d4f2`, to the existing `dd36` integrator for all six reserved wiring paths, exact import, isolated verification and bounded corrections. It explicitly kept `f18` out of shared edits and claimed no implementation result. This is the desired sequencing shape: independent evidence and pure-module work can advance in parallel, then one owner establishes a single-writer transition for the connected source change. Do not split the six related paths into per-file requests or commission review until the integrated candidate and applicable gates freeze.

Valid design stop with broken ownership trail, 03:00-03:04 UTC: sole integrator turn `127428` ran 162.6 low-effort seconds, verified the staged `19f3` tree and locks, imported only the two hash-matched pure guard files and left all six shared wiring files unchanged. It found that the existing identity lock ends before database startup, while SQLite exclusion begins only after writable open and WAL setup. Independent processes can therefore both pass admission before marker publication. The accepted proposal explicitly says to report absent shared ownership and forbids inventing a lock or lease subsystem in this card. Stopping was correct and preserved useful partial work in `att_788ac753`; narrowing the implementation to same-process behavior would violate the accepted goal. Owner turn `127429` confirmed the gap and need for a bounded amendment. Coordination then failed in two ways. It filed `att_59542f56` against unrelated reminder-system assignment `asg_01feb67b`, rather than the release or `dd36` integration lineage, and its request for an owner amendment remained prose only with no wake or successor. The integrator is safely stopped but the decision has no executable route. This is confirmed S4/C3 at a product boundary, not review churn. Later detection should flag an attest whose named work, commit or dependency does not match its assignment subject, and any terminal turn that names a required actor or amendment without a durable handoff or condition wait. The next action must repair lineage and obtain the concurrency amendment before shared wiring resumes; it must not start a second integrator or review the two imported files as though they delivered the guard.

Overlapping recovery paths repaired lineage but did not create authority, 03:06-03:09 UTC: one external correction to active owner `s_0cd749fc` and a separate coordinator request to stop-listed release holder `s_7169b2a6` started six seconds apart for the same startup-ownership gap. They consumed 114.5 and 121.1 low-effort seconds respectively. Their effects were complementary rather than duplicative: the first bound the finding to release and integrator lineage in `art_69a4f788`, hit a truthful `not_holder` refusal and created condition wait `w_ba63c7b3`; the second produced one concrete kernel-managed external-lock amendment in `att_7b30a829` and routed it to the active owner. Turn `127433` hash-verified the packet but correctly refused to authorize wiring because its assignments do not own the accepted guard contract. The actual contract receipt `att_8b3f0dae` belongs to stop-listed holder `s_fde9b2be`, so the condition wait remains and `dd36` did not resume. This is contained C6 recovery overlap followed by a valid authority hold. A shared obligation registry would have allowed the second coordinator to attach its amendment to the already-running correction instead of starting an independent recovery turn. More important, artifact delivery must not be mistaken for authority delivery. The amendment is executable only after the accountable contract owner or Mike changes the restriction; no lower owner should convert a complete design packet into implied approval.

Replacing a broad timer with one hazard-specific fallback, 03:14-03:19 UTC: owner turn `127437` consumed 46.4 high-effort seconds on a standing continuation whose own text said both C6 patrol loops were closed. It found nothing moved, reported the unchanged state and armed another 30-minute broad sweep, `w_d1bb91ac`. Correction turn `127441` was itself expensive at 143.6 high-effort seconds, but canceled that timer, polled nothing and created no replacement cadence. The owner established that all but one held obligation already deliver their own ruling or deadline wake. The exception is public publication: current 0.1.8 emits no fact for decision-request consumption or publication, and the already-fired escalation ruling cannot signal later disclosure resolution. It retained one narrowly named 12-hour fallback, `w_b6acfd7e`, that checks only ruling consumption and evidence of an actual public push, suppresses unchanged reporting and forbids widening into a sweep. At the observed 46.4-second cost, the one-time correction repays its model cost after just over three avoided half-hour sweeps. This is useful C6 reduction, not free optimization. It also exposes a product gap: an irreversible outward operation needs a pre-execution condition or event that can block and notify before mutation; polling after publication can detect harm but cannot prevent it. Later evaluation should count broad sweeps avoided, whether the narrow fallback stays narrow, and any missed publication or ruling event.

Unknown-outcome recovery must include saved indexes and packets, 03:17-03:26 UTC: expanded native-bridge turn `127442` selected exclusive paths and filed `att_1c808b37`, then ran 328.6 seconds before terminating `adapter_unavailable: :normal`. Its normal working tree appeared clean, but that did not mean no effect. The failed turn had saved a complete partial tree through a temporary Git index, a 220-line patch packet and an exact terminal log. Automatic ancestor recovery `127444` mixed unrelated queued work into its response and ended with a safety refusal, producing no reconciliation. After credential recovery, the same integrator resumed in turn `127447`. It rehashed partial tree `87cb1c69` and patch `d241e4fc`, recovered retained handle `99962` with explicit exit 0 and focused native-lock 2/0 evidence, and filed `att_e45e2898` without recreating source or rerunning the unchanged test. Scope remained honest: same-VM contention, resource transfer and owner-death reacquisition passed; DB-close ordering, independent-VM behavior, macOS and six-file integration remained open. One one-minute continuation returned to the same holder, and one coordinator receipt carried the checkpoint. This is productive S1 recovery after a failed automatic ancestor path. Filesystem reconciliation must inspect ordinary worktree state, alternate indexes, patch packets, logs and retained process handles before deciding what survived. A clean checkout alone is insufficient, and an ancestor safety refusal is not proof that recovery work is absent or unsafe.

Native bridge continuation changed one distinct risk without replay, 03:26-03:27 UTC: same-holder turn `127451` resumed from saved partial tree `87cb1c69` and retained the earlier focused 2/0 evidence. It changed only native-library publication so the build writes a separate inode and atomically renames it, avoiding truncation of a library already loaded by another VM. It ran no test and claimed no new binary result. Inspection then exposed the next unsatisfied invariant: the process monitor can close the guard lock before Exqlite destroys its connection, because current DB ownership has no termination callback and the Exqlite resource destructor owns `sqlite3_close_v2`. Receipt `att_c21517fc` preserved that exact boundary and armed one same-holder continuation. This is useful staged implementation rather than C1 rerun because the turn made one named source correction and derived a new lifetime-ordering blocker from it. It becomes C2 churn if these partial steps reach independent review before the DB-lifetime and six-file candidate freezes.

Seventh assignment-free PR-monitor tick, 03:25-03:26 UTC: turn `127450` completed in 52.3 low-effort seconds. All 11 open PR heads, states, decisions and checks remained unchanged; PR31 still lacked its consolidated readiness package and PR30 retained the known macOS failure. George's mapped bugs and internal PR-as-CI carriers were unchanged. The monitor created no assignment, attest, artifact, review, test, request or gate, and scheduled exactly one successor for 04:25:52. The embedded audit continues to distinguish the real historical problem: 21 reviews covered 21 different PR31 commits, so exact-commit duplicate review count is zero, while premature changed-head review still cost 50 cards, 143 turns and 12,529 execution-seconds. The readiness threshold has now prevented new review work across seven consecutive hourly ticks.

False deduplication across one holder's unrelated obligations, 03:39-03:42 UTC: five effort-check turns on `s_0cd749fc` completed cheaply in 3.4 to 7.2 low-effort seconds and created no duplicate wake or receipt. Each claimed existing continuation `w_856b734d` covered its assignment. Direct wake inspection shows that continuation is explicitly scoped only to `asg_1a9ad783`, artifact-evidence survival. It does not cover `asg_18a7cde5` kungfu offers, `asg_4c9891d5` development mode, `asg_ac40a706` command guards, `asg_3c1a82c1` credential guidance or `asg_d984ca20` supervision cadence. This is C5 false suppression, the mirror image of fanout churn: shared holder identity was treated as shared obligation identity. Low model cost and absence of duplicate rows do not make the outcome correct. Deduplication keys must include the obligation or assignment and condition scope, not only holder, due window or destination session. A holder-level batch may compute shared liveness evidence once, but its result must map each assignment to a genuinely applicable event, condition or continuation. Missing coverage remains a stall even when another assignment on the same holder has a pending wake.

False-dedup repair found correct rows but reported false deadlines, 03:51-03:59 UTC: one holder-level correction turn `127475` replaced five separate analyses and created no wake. It consumed 448.9 low-effort seconds after fresh transcript access timed out, then mapped each open assignment to an existing scope-matched continuation and correctly confined `w_856b734d` to artifact survival. Direct row verification confirms the five wake IDs and prompts are applicable, but all five human-facing deadlines are about 20 minutes late: actual due times are 04:40:55 through 04:42:12 UTC, while the response says 05:00:55 through 05:02:12. This repeats C5 receipt corruption despite correct durable state. Batching removed duplicate effects but did not reduce model cost and still emitted false scheduling data. Later implementation should build the coverage map from authoritative wake rows and render exact stored `dueAt`; if transcript access times out, it must not substitute timestamps remembered from prompts or receipts. Measure batch cost against separate-turn cost, as coalescing that consumes 448.9 seconds is not automatically an efficiency gain.

Migration acceptance composition avoided a duplicate lane but paid a large filing retry, 03:51-03:58 UTC: Morrow established that old 0.1.8 fixture reopen proves provenance and readability, not cross-process exclusion before writable open or WAL. Release turn `127474` bound final migration acceptance to the existing guard candidate's independent-process contention, abnormal-exit release and refused-admission no-write evidence. It preserved both old snapshots and the sole integrator and started no test, wake or mechanism choice. The turn consumed 427.5 low-effort seconds because its first filing attempt timed out during approval review; one retry succeeded as `att_f6d60ba9`. This is correct sequencing with C6 infrastructure cost. Do not run migration acceptance in parallel with incomplete guard lifecycle proof, and do not count the old-binary reopen as lock coverage. Approval-review timeout should preserve an idempotent pending operation or exact receipt so a retry does not require another full model turn.

Eighth shared-holder decision fanout repeated false successor receipts, 03:54-04:01 UTC: six `zero_effect` requests for `s_0cd749fc` again became six serialized bare-Main high-effort turns. They consumed 402.3 model-execution seconds and reached 228.7 seconds of queue delay. All six produced useful `continue` rulings and distinct 30-minute pending continuations. Every response nevertheless named the consumed and canceled one-day deadline wake as the “pending continuation wake,” rather than the new row. This exactly repeats the seventh-wave receipt defect. The holder-level correction was still running and could not change already-generated decision turns, while bare Main remains stop-listed. Current containment is observation only; do not route another correction into the stopped lane. The installed fix must make the ruling operation return the newly created successor ID directly and render that value, rather than letting the agent infer it from the triggering request. Grouping by holder can reduce shared analysis cost, but each resulting decision and successor still needs its own assignment-bound identity.

Read-only WAL qualification found a real no-write failure, 03:56-04:01 UTC: same integrator turn `127484` compiled the new admission helper and ran only its changed focused cases. Four wrong-marker, alias, payload and refusal-cleanup cases passed. The read-only WAL case failed one of one with exit 2 because protected file inventory changed. Exact packet `f7f42bb7` and full tree `9db0c4df` preserve the failure. The integrator withheld all six startup wiring paths and did not ignore WAL files or weaken the no-write assertion. This is useful defect discovery, not C1 test churn. The correction must explain and prevent the observed side effect before startup wiring, then rerun only the affected qualification cases; prior native and DB-lifetime proofs remain applicable unless their bytes change.

Firehose manual and automatic clocks overlapped again, 03:58-04:04 UTC: assignment `asg_c8f9d0f8` fired its 15-minute process effort check as turn `127487`. The turn consumed 322.5 low-effort seconds and created one narrow manual disclosure-disposition check, `w_d3561cb6`, due at 04:33:41. It repeated no classification or test. The process simultaneously retained its next assignment-bound effort probe `w_08c46d40`, due at 04:13:22, so the automatic clock will fire first against the same unchanged publication hold. This reproduces C6 overlapping dependency and supervision clocks. A manual dependency check cannot be considered deduplicated merely because its prompt is narrow; its owner must reconcile or suppress the automatic probe, or the effort system must recognize the pending dependency wake and defer its own check. The 322.5-second cost produced only a timer, not changed evidence. No additional correction wake is justified while both existing timers remain visible; measure whether the first firing cancels or supersedes the second before intervening.

Incremental guard work crossed into material architecture expansion, 04:01-04:13 UTC: WAL correction turn `127489` ran 683.1 low-effort seconds and preserved the failed tree without replay. Exact fixture inspection found one new 32,768-byte `state.db-shm` file. Pinned SQLite immutable mode skips WAL discovery, while ordinary read-only WAL handling may create or modify shared-memory state. The turn therefore rejected immutable, copy and WAL-ignore shortcuts and changed no source. It then proposed a custom native read-only WAL observer with an in-memory WAL index and a file-access layer that rejects writes, truncation, creation and deletion. That proposal is materially larger than the allocated six-file guard wiring and native lock bridge. It adds SQLite file-format, WAL-index, version-compatibility and two-platform maintenance responsibilities. A one-minute implementation continuation was armed before an accountable architecture disposition. This is C2 scope expansion risk caused by locally correct incremental findings: each step follows the last defect, but their composition can silently change the product seam and maintenance burden. Preserve the exact failure and no-write invariant, but stop before implementing the custom observer. Require the existing accountable guard/release owner to accept this mechanism with exact supported SQLite version and Linux/macOS boundaries, or select a smaller compatible design. Do not commission review while the architecture and full candidate remain unsettled.

Scope hold lost a non-preemptible one-minute continuation race, 04:14-04:24 UTC: the scheduled implementation wake fired as turn `127492` seven seconds before the external scope checkpoint reached the same integrator as queued turn `127493`. The hold waited 543.9 seconds behind the work it intended to stop. During that interval the implementation turn ran 550.8 low-effort seconds, wrote 434 lines across saved observer and test patches plus a gate script and result report, and completed focused 4/0 exit-0 evidence on frozen tree `8071cb23`. The implementation delegates SQLite DB and WAL parsing to pinned SQLite 3.53.3 through a private deny-write VFS; it is not a handwritten WAL parser, but still creates new native VFS, version, packaging and platform obligations. When the hold finally ran, it stopped further source and tests, froze the result as experimental rather than architecture-approved, and canceled the pending successor. This is confirmed C2/C3 control latency. A queued instruction cannot protect against an already-running turn, and a one-minute continuation leaves almost no time for independent findings to intervene. Material scope-expansion candidates need a pre-execution approval condition checked by the scheduler or executor before source mutation, not a later message in the same session queue.

Hold enforcement created two forbidden authority paths, 04:24-04:25 UTC: after correctly freezing `8071cb23`, integrator turn `127493` sent simultaneous disposition wakes to `s_0cd749fc` and `s_7169b2a6`. The checkpoint explicitly prohibited contacting stopped sessions and asked for the existing accountable owner without a new parallel path. `s_0cd749fc` had already established that its assignments do not own this guard amendment, while `s_7169b2a6` is on the formal stop list. The resulting turns consumed 47.6 and 49.4 low-effort seconds. The non-owner correctly refused architecture acceptance, recorded the unresolved boundaries and made no continuation. The stopped release holder recorded the hold in `att_b4293a70`, did not restart the canceled continuation and left the decision explicit. No conflicting approval or source change occurred, so this is contained C3/C5 conflict-driven churn rather than an unsafe mutation. The two turns still created an extra artifact and attest and engaged a stopped lane for information already known. Later enforcement needs machine-readable prohibited targets and authority predicates checked before wake creation; prose constraints were insufficient. A later request for one reviewable decision packet should reuse `att_b4293a70` and the existing evidence, remain source-and-test frozen and produce one accountable disposition rather than another abstract hold.

Architecture packet converted the hold into one product choice, 04:25-04:28 UTC: release turn `127501` consumed 150 low-effort seconds, resumed no source or test and produced `guard-wal-architecture-decision.md`, SHA `076916e8`, recorded in `att_489f6059`. The packet confirms SQLite 3.53.3 itself parses DB and WAL; custom native code supplies the capability-bound deny-write VFS and cleanup. It counts 468 retained patch lines, including a 231-line observer/VFS block, and states the critical consistency assumption that every writer uses the same exclusion. Current 4/0 evidence covers only synthetic Linux observer cases. Runtime version checks, macOS behavior, packaging and lifecycle maintenance remain unproved. No smaller compatible mechanism is supported by current evidence. The choice is now concrete: conditionally accept the pinned VFS architecture with independent native and platform proof, or defer it fail-closed and leave WAL-backed upgrade, adoption and restart unavailable. This is a material product and maintenance tradeoff for Mike, not another routine implementation inference. Keep `8071cb23` frozen and do not commission implementation or review until that disposition is explicit.

PR-monitor prompt compaction preserved coverage after a transient duplicate, 04:31-04:34 UTC: eighth assignment-free tick `127502` found no material delta and created no review or work row, but consumed 352.8 low-effort seconds. Its recurring prompt copied the entire 21-review audit and readiness rationale on every hourly turn. An external attempt to cancel the monitor-owned successor returned false, then created a compact user-owned replacement at the same deadline. That briefly produced two wakes; the external caller immediately canceled its own replacement. One bounded monitor-owned correction turn `127504` then consumed 41 low-effort seconds, ran no PR query, canceled its original successor and created exactly one compact replacement at the same 05:25:52 deadline. The prompt fell from 1,431 to 588 characters, a 59 percent reduction, while preserving the 11-PR baseline, George acknowledgment checks, PR31 consolidated-package threshold, assignment-free behavior and hourly cadence. This is useful C6 input-token reduction with a contained C5 ownership mistake. A caller should check cancel authority before creating a replacement, or use one owner turn that performs both actions atomically. Later evaluation should compare actual turn duration and correctness under the compact prompt, not assume input reduction guarantees cheaper execution.

Migration fixture coverage remained serial, 04:42 UTC: Morrow reported that the genuine 0.1.8 fixture now preserves pending, fired and canceled wakes, a `failed_unknown` turn and its queued successor across close and reopen. This strengthens old-version snapshot provenance but does not prove upgrade, restart or rollback behavior. The remaining fixture gap is legacy O2 episode reconciliation because the snapshot contains no assignments. That gap remains with the existing R1 owner, and the snapshot is explicitly preserved for that continuation. This is healthy sequencing rather than S4: the next required evidence, accountable lane and retained input are all named. Starting another migration or fixture lane would create conflict churn without increasing coverage. Final-candidate acceptance must wait for the existing owner to seed representative assignments and then run the already-sequenced upgrade, restart and rollback proof; the unresolved WAL architecture decision remains a separate prerequisite rather than a reason to parallelize migration execution.

Manual and automatic shared-holder clocks stacked into eleven checks, 04:40-04:46 UTC: six distinct dependency wakes reached `s_0cd749fc` serially in 151.9 low-effort execution-seconds, with 17.5 seconds maximum queue delay. Five found no relevant change and again named their consumed triggering wakes as pending continuations. The sixth did consume a newly arrived guard-coordination decision, proving the obligations cannot be collapsed into one holder-level truth value. Before that burst could settle, five assignment effort probes ran against the same holder in another 28.4 low-effort seconds. They found the recent turns and created no effects; the sixth probe was suppressed by the new progress. This is C6 clock stacking with repeated C5 receipt corruption, not duplicate obligation identity. The safe batching rule is one shared evidence snapshot and one holder turn per due window, followed by an authoritative per-obligation mapping that returns each actual successor or changed fact. Assignment probes should absorb recent scope-matched dependency checks and defer rather than immediately inspect the same holder again. A batch must not claim one wake covers unrelated assignments, and changed evidence for one obligation must not reset the other five.

System effort routing bypassed a formal contact stop, 04:47 UTC: process-generated decision request `dr_e00add8a` entered stop-listed session `s_11812a8d` after 60 minutes of zero effect on assignment `asg_071760be`. The session consumed 20.6 high-effort seconds, ruled `continue` and reasserted existing condition wake `w_8f80d66e`; it created no competing implementation or new condition. The assignment holder is a different session, `s_2c37695b`, so this was decision-owner routing rather than holder contact. Containment happened by outcome, but the automatic route still violated the human stop boundary and spent stopped-lane attention on unchanged evidence. Formal stops need a machine-readable routing predicate applied before effort decision delivery, with a non-contact fallback to an authorized owner or a held decision row. External observers must not treat an automatic contact as lifting the stop or repeat it manually.

That stopped-session route is an hourly loop, not a one-off bypass, 22:44-05:47 UTC: seven completed high-effort decision turns on the same `asg_071760be` assignment consumed 23.2, 24.6, 23.5, 25.0, 24.6, 24.6 and 20.6 seconds, 166.1 seconds total. Each hourly generation ruled `continue` on unchanged zero-effect evidence. Eighth request `dr_32803b6f` entered the same formally stopped session at 05:47 and remained running at the observation boundary, with another temporary one-day wake already present. This is confirmed recurring C6 plus a persistent stop-enforcement failure. Safe individual outcomes do not excuse the loop. Stop state must suppress both delivery and rearming before model dispatch, while preserving the open request or routing it to an authorized non-stopped owner. The detector should group repeated `(assignment, decision owner, zero-effect class)` generations into one episode so hourly repeats increase cost and recurrence counters without masquerading as new problems.

The eighth stopped-owner turn made the recurring cost sharply worse, 05:47-05:52 UTC: `127574` consumed 327.9 high-effort seconds, ruled `dr_32803b6f` `continue` and reasserted already-pending condition wake `w_8f80d66e`. It added no changed evidence, work, test, source or new condition. The temporary one-day deadline row was process bookkeeping, not the useful continuation. Total model execution for the eight-turn hourly loop is now 494.0 seconds. This is not evidence that the stopped owner needed a deeper judgment; the outcome was identical to the preceding generations. Model effort variance makes a supposedly cheap periodic safeguard capable of dominating the episode's cost. Suppression must occur before model selection, and recurring-loop accounting must report aggregate cost rather than the median short turn.

Corrected semantic patrol avoided a premature stall escalation, 04:45-04:51 UTC: dependency scan 40 consumed 330.2 high-effort seconds and evaluated the six shared-holder turns against durable outcomes rather than prompt wording. Only `asg_1a9ad783` advanced, through new attest `att_a8ee4330`; the other five explicitly found no changed authority, allocation or disposition and created no durable rows. The patrol counts those five at one definite unchanged fired window because the prior five outcomes were not mapped reliably to assignment identities. It therefore withheld contact and requires two more mapped unchanged windows before a 3/3 stall report. This is the right conservative detector behavior, although the scan cost exceeded the six turns it assessed. Later implementation should compute the per-obligation outcome map from stored turn effects and attests, not spend another full high-effort model turn reconstructing it. The next scan is aligned to the one offset strand's actual pending wake rather than a fixed cadence.

Narrow user decision removed the WAL architecture expansion, 04:43-04:53 UTC: Mike allowed ordinary SQLite coordination artifacts during pre-admission inspection while continuing to forbid database or schema mutation, WAL checkpointing and migration. Sole integrator turn `127513` consumed 585.6 low-effort seconds, preserved historical custom-VFS tree `8071cb23`, removed that VFS from the working candidate and used ordinary read-only SQLite with checkpoint-on-close disabled. Focused evidence passed 10/0 at tree `5c516d77`; existing database and WAL bytes remained identical, including the committed-effect fixture, while only allowed SHM or a newly empty WAL may appear. Marker, capability and two attachment-lifecycle cases also passed. This is useful scope reduction after an explicit product decision, not a weakened invariant: the protected state boundary is narrower and directly tied to user harm, while the custom maintenance surface leaves the candidate. Do not start independent review yet. Six-file startup wiring, platform behavior and packaging acceptance remain outstanding and should proceed serially through the sole integrator and release route before one consolidated current-head review.

The same holder cluster then paid a second supervision layer, 05:00-05:02 UTC: five zero-effect probes escalated into five separate high-effort decisions on bare Main. They consumed 121.3 execution-seconds and all ruled `continue`; the sixth assignment had recent progress and stopped after a 5.1-second holder probe. Every Main response named a newly created one-day wake as its pending continuation, but each named wake was already canceled. The actual successors are different assignment-bound 30-minute wakes due 05:30:19 through 05:31:58. This exactly repeats the false-successor defect and rearms the next shared-holder wave. The observed stack for this cycle is therefore six dependency reads, five holder effort probes and five Main decisions before any of the five unchanged obligations can move. A correct atomic `continue` result must return the surviving successor ID and let assignment supervision absorb a recent scope-matched dependency read instead of escalating it. Do not cancel the surviving wakes without an alternate liveness path; containment must fix or suppress the redundant layer, not make the obligations dark.

One orphaned review was closed without commissioning another review, 04:58-05:02 UTC: `asg_dd66f64a` had remained open since 2 September with no verdict or pending continuation after its reviewed producer surrendered. One bounded reconciliation turn consumed 212.8 xhigh seconds, read durable evidence only, filed a single changes-requested verdict `att_1536b7b3`, report `art_ea035d56` and completion `att_d74adcdd`, then closed the existing review. The historical rollback artifact proved only its observation interval; missing exact host-state evidence and later drift prevented full certification. No host command, replacement review, owner contact, install, release or wake occurred. This is successful S4 cleanup with notable model cost. Stale-review detection should first distinguish a completed verdict blocked on lifecycle mechanics, a live bounded dependency and a verdictless orphan; only the last needs reconciliation, and it should reuse the existing reviewer and evidence.

Firehose clock overlap survived two full cycles, 04:28-05:04 UTC: assignment effort probes fired at 04:28 and 04:58 while manual disclosure checks fired at 04:33 and 05:04 against the same unchanged publication hold. The later automatic probe consumed 7.2 low-effort seconds and suppressed escalation because the manual wake already existed. The later manual check then consumed another 20.6 seconds, found no disposition and rearmed `w_e9d95668` for 05:34:18. The next automatic effort probe remains due earlier at 05:13:25. This confirms stable C6 clock stacking rather than a transient race: retaining the manual clock prevents a more expensive effort decision but guarantees an extra probe, while removing it may expose the assignment to false zero-effect escalation. Do not cancel either clock blindly. The live-safe correction needs the supervision layer to recognize a scope-matched manual dependency wake as credited liveness and defer its probe until that wake resolves; only then can the manual clock remain the sole bounded fallback without making the assignment appear dark.

Firehose effort suppression then avoided a model turn, 05:13 UTC: probe `w_cf33772e` fired while the manual disclosure wake remained pending, created no turn or decision request and rearmed only the next substrate probe `w_1d16de11` for 05:28:26. This corrects the strongest reading above: the overlapping automatic clock does not always burn model execution. The 04:58 probe did consume 7.2 seconds, so suppression is not proven uniform, but the 05:13 cycle shows the existing system can recognize enough liveness to avoid escalation. Measure clock overlap as model turns, decisions and durable effects, not timer rows alone. The remaining recurring cost is the manual 30-minute model check; preserve the silent effort behavior while replacing that poll only when a real disclosure-disposition event has an accountable emitter.

Guard implementation crossed from useful staging into micro-step risk, 04:43-05:27 UTC: after the architecture decision, four serial integrator turns consumed 585.6, 586.4, 390.5 and 927.9 low-effort seconds. Each changed a distinct boundary and avoided unchanged gate replay: ordinary read-only inspection 10/0, lock-path and input revalidation 4/0, DB-owned refusal wiring 3/0 plus a separate memory pass, then marker publication, stamp refresh and startup ordering 3/0. The fourth turn also retained a failed patch-context attempt before correcting it. This is not repeated review or source conflict, but each partial result triggered a separate stopped-release-holder receipt while the candidate remained unreviewable. The latest result still lacks persistent runtime-payload handoff and full platform/package proof. Continue the one already-armed same-integrator runtime-fixture step because it addresses a concrete blocker, but require the next readiness packet to consolidate persistent success, lifetime/order and remaining platform/package evidence before any independent review. Later orchestration should let one holder checkpoint locally across coherent sub-steps and notify the release lane only on a material blocker, authority need or reviewable candidate, rather than on every passing micro-gate.

Compact PR prompt preserved behavior but barely changed execution cost, 05:25-05:32 UTC: ninth assignment-free tick `127543` consumed 346.3 low-effort seconds, compared with 352.8 seconds for the prior unchanged tick. The 59 percent smaller prompt saved only 6.5 seconds, about 1.8 percent, in this single comparison. It correctly found all 11 heads and George acknowledgments unchanged, kept PR31 below the consolidated readiness threshold, created no review, assignment, attest, artifact or request, and scheduled exactly one compact successor for 06:25:52. Prompt compaction is therefore a correctness-preserving input reduction, not yet evidence of meaningful total-cost reduction. The next optimization should compute an external-state fingerprint before model dispatch and suppress the turn entirely when PR head/state/check and acknowledgment hashes match the stored baseline; invoke the model only for a delta that needs interpretation. Keep the hourly fallback until an accountable webhook or event source is proven.

Exact-review detection must normalize the whole candidate, 05:16-05:32 UTC audit: 16 individual commit hashes appeared in verdicts from more than one review card since 2 September, but dual-line and landing reviews make a single-hash join overcount. Normalizing each verdict to its complete sorted commit set leaves 12 repeated candidate sets. Several are legitimate different questions, such as source adequacy followed by landing-effect verification. Satellite CLI pair `4a1e30f3` plus `f5416c09` is a true evidence/gate repeat: `asg_9dca6d48` consumed 4,624.2 seconds and requested changes only because an unrelated required performance gate was red, then `asg_4fa87923` consumed another 824.5 seconds reviewing the unchanged pair after Mike routed that timing defect separately. The final source conclusion was clean. A bound verdict amendment or narrow gate-disposition review would have preserved independence without another full exact-pair pass. Surf Ace `865004f1` shows administrative duplication: one assignment filed two reviewed-clean verdicts 67 seconds apart with no source or execution change, then a later bookkeeping-only review filed a third same-commit verdict to link producer lineage. The detector must key on full candidate set, review question, evidence version and verdict lineage; flag same-candidate review only when neither source nor the question changed, and classify duplicate verdict filing separately from duplicate reviewer execution.

Historical internal reviews show changed-head and conflict-driven churn, 26 August-9 September UTC: session-environment work accumulated six review cards. Its recorded verdict chain covers distinct commits `440693ce`, `65364eb7`, `e83d0cf5`, `54c2515d` and later `afa175c6`, plus the initial artifact review whose commit reference was not stored. This is not exact-candidate duplication, but four consecutive changes-requested heads before `54c2515d` became clean show the same premature-head review pattern as PR31. The later `afa175c6` review found a new provisioning-facts defect and then remained open because lifecycle closure repeatedly failed; do not count those closure retries as fresh reviews. Fable work is a clearer conflict specimen: eight review cards, 12 bound turns and 6,675.6 execution-seconds. One same-tip evidence-correction review followed the first verdict without source change; a duplicate exact-tip card was revoked; and later `asg_d458f690` spent 2,444 seconds re-reviewing unchanged `c3299e3a`/`4742b533` solely to recover from the prior reviewer's overreach. Evidence corrections can justify a same-source verdict when the first verdict depended on bad evidence, but reviewer-scope conflict should be corrected against the existing verdict before commissioning another full independent pass. Track candidate identity, evidence identity and correction cause separately so necessary evidence repair does not hide conflict-generated review cost.

Cross-owner queue contention interrupted the guard integrator at a readiness boundary, 05:31-05:34 UTC: the sole integrator finished persistent runtime handoff proof 1/0 at tree `1e26d793` in turn `127545`; Boot/Gateway ordering remained its next serial step. Before that continuation could start, the REST owner queued unrelated O1/O2 dependency-closure turn `127549` onto the same session. It consumed 108.3 low-effort seconds, found that the accepted O1 carry was already in `eff3`, then needed one locator round trip because an abbreviated assignment ID and absent local commit object could not resolve the amendment. The guard continuation was created while this turn ran and waited until it ended. Its user prompt entered the shared transcript before the O1/O2 response, and the REST owner queued a second locator reply while the guard continuation was running. Execution remained serial, so no simultaneous source mutation occurred, but unrelated owners contaminated one session's context, delayed the release-critical proof and added two turns around a dependency that required no edit, gate or new review. This is C3/C5 conflict-driven churn. A session holding an active release-critical implementation sequence should have one routing owner until its readiness packet or named blocker. Other owners should send dependency questions to that routing owner, or wait for an explicit safe handoff, rather than enqueue work directly on the integrator. Queue serialization prevents concurrent execution; it does not provide scope isolation or priority control.

Firehose clock suppression failed expensively on the next cycle, 05:28-05:34 UTC: automatic effort turn `127546` consumed 310.6 low-effort seconds and only confirmed that existing manual disclosure wake `w_e9d95668` already covered the next check. It created no duplicate wake or receipt. That manual wake then fired as turn `127553` and consumed another 19.8 seconds to find no changed disposition and retain the publication stop. The pair spent 330.4 model-execution seconds on one unchanged condition within six minutes. This reverses the silent 05:13 outcome and confirms that suppression is nonuniform. The required rule is stronger than avoiding duplicate durable rows: when a scope-matched dependency wake is pending, the automatic effort path should end before model dispatch and credit that wake as bounded liveness. The manual turn should likewise use a cheap disposition fingerprint and skip the model when the governing evidence is unchanged.

Firehose repeated the same stacked-clock shape at 05:58 UTC: automatic effort turn `127582` consumed 7.8 low-effort seconds and correctly recognized pending manual disclosure wake `w_b19f0ff6`, adding no wake or receipt. The manual check remained due at 06:04, while the next automatic controller row remained due at 06:13. This is cheaper containment than the 310.6-second automatic turn at 05:28, but it still dispatched a model solely to discover a directly indexed pending wake. The product behavior is stable and no duplicate durable effect occurred; the remaining C6 cost is one avoidable model invocation followed by the still-scheduled manual invocation. Move the pending scope-matched wake predicate ahead of model dispatch and retain this 7.8-second cycle as the pre-fix baseline.

The adapter open-circuit lane has its own unchanged two-hour poll loop, 23:44-05:51 UTC: condition-bound owner-disposition turns `127139`, `127254`, `127472` and `127566` all read the same owner state through `att_5ed4930a`, preserved the frozen `8d67cb24` source and existing authentication holds, and rearmed the same condition. They consumed 20.2, 22.7, 23.8 and 323.7 low-effort seconds, 390.4 seconds total. The last turn produced no new direction or product evidence and scheduled `w_4698a45f` for 07:51. Earlier 15-minute effort checks also ran on the same assignment before the condition loop stabilized. This is recurring C6 whose tail cost dominates despite identical work. The condition wake should fire on a new owner fact and use one longer bounded fallback only when the owner has no accountable event path; unchanged fallback execution should compare the last consumed owner fact before model dispatch and rearm without a model. Preserve the source freeze and authentication restrictions while removing the repeated semantic read.

The paired manual check completed at 06:05 UTC: turn `127592` consumed 25.6 low-effort seconds, found no newer disclosure disposition, preserved the publication stop and rearmed `w_0d611dce` for 06:34. Together with automatic turn `127582`, the cycle cost 33.4 model-execution seconds on one unchanged condition. The automatic controller remained due at 06:13, which places another two 15-minute controller opportunities before the 30-minute manual fallback. Even if both automatic checks cheaply recognize the manual wake, that is repeated scheduling and possible model dispatch for no added liveness. A scope-matched dependency wake should advance the controller's next due time to the manual deadline or later, not merely suppress escalation after the controller fires.

Guard checkpoint fanout continued after the persistent handoff boundary, 05:34-05:40 UTC: integrator turn `127551` consumed 188.3 low-effort seconds and changed tree `1e26d793` to `c6e78c57`, proving wrong-base Boot and Gateway refusal before Identity writes plus admitted Boot success in one corrected focused test. It preserved an initial integer-versus-string assertion failure and did not claim full Gateway/Application or platform acceptance. Turn `127557` then spent 60.5 seconds inspecting the next fixture boundary without changing source or running a gate, and turn `127561` started the named retired-decision recovery proof on unchanged `c6e78c57`. These are distinct steps, but each checkpoint again prompted formally stopped release holder `s_7169b2a6`: turns `127556`, `127558` and `127559` consumed 43.6, 38.3 and 7.0 seconds within about two minutes. The last prompt carried only the next test idea from an unchanged-tree inspection. No premature independent review opened, but the release route received three model turns before a reviewable candidate existed. This confirms the earlier micro-step fanout and shows that prose stop rules still do not constrain producer-generated receipts. Keep the current sole integrator serial because each proof depends on the prior source and fixture state, but accumulate local checkpoints until a reviewable candidate, material blocker or authority choice. Enforce the stopped-recipient list when creating wakes, not after delivery.

The guard candidate reached its first assembled startup boundary, 05:47-05:50 UTC: sole-integrator turn `127573` consumed 177.9 low-effort seconds and changed tree `c4c312af` to `bcf78a95`. One staged Application test passed with exit 0 after guarded database and marker creation, synthetic preplacement for every adapter, fixture-local probes and fail-closed CLI, npm and SSH tripwires. It ran the real database, wake scheduler, supervision and lane-manager children, reached settled readiness, preserved the marker, stopped cleanly and reacquired the kernel lock. No real credential, provider, installer or remote path ran. This is materially broader integration evidence rather than a helper-only micro-step, but it does not prove broader startup compatibility, abnormal or cross-process lifecycle, macOS behavior, packaging or genuine 0.1.8 upgrade and rollback. No independent review opened. The producer nevertheless sent another receipt to stopped release holder `s_7169b2a6`; turn `127575` consumed 29.7 seconds and only recorded the result and limits. Hold review until the same integrator consolidates the remaining applicable compatibility and lifetime evidence into one readiness packet, and suppress stopped-holder checkpoint receipts in the meantime.

Cumulative guard fragmentation is now a material cost center, 04:43-05:53 UTC snapshot: since the narrow SQLite ruling, integrator session `s_76613413` completed 13 turns consuming 3,749.9 model-execution seconds and had a fourteenth running. Two completed turns, 182.0 seconds, were unrelated REST guidance closure injected into the same session; the rest advanced guard source, fixture or evidence boundaries. Messages from that producer also caused 13 turns on formally stopped release holder `s_7169b2a6`, consuming 395.9 seconds. The combined completed cost is 4,145.8 seconds before the current compatibility correction finishes. The work is producing real evidence and has avoided premature review, so do not label the entire sequence churn. The avoidable portion is the 13 stopped-recipient turns, the two cross-owner turns and planning-only checkpoints that immediately trigger both another producer continuation and a release receipt. Measure productive implementation and coordination overhead separately. Use one local multi-step integrator lease, one routing owner and one readiness packet, with release notification only at a blocker, authority choice or reviewable candidate.

Firehose automatic suppression succeeded again at 06:13 UTC: controller wake `w_6cc94941` fired while the unchanged manual disclosure wake `w_0d611dce` remained pending, created no model turn or decision request, and advanced directly to controller wake `w_282eb3d3` due at 06:28. This is a positive silent cycle and shows the substrate can suppress redundant execution before model dispatch. It does not resolve clock stacking: the automatic controller remains due six minutes before the manual fallback at 06:34, so another timer collision is already scheduled. Preserve the silent path, but let a scope-matched manual dependency wake move the controller due time to that wake's deadline or later. The acceptance check is no automatic model turn and no earlier redundant controller row while the credited manual wake remains pending, with immediate reactivation when its evidence changes or it expires.

By 06:11 UTC, that same episode had reached 17 completed integrator turns totaling 4,604.1 model-execution seconds and 17 producer-triggered turns on the stopped release holder totaling 564.6 seconds. Combined completed cost was 5,168.7 seconds, with the next serial integrator turn running and still no independent review. The added source turns remained productive and narrow: Application refusal 2/0, remaining Application 4/0, Gateway token 1/0 and Gateway cadence/liveness 3/0 all changed their exact trees and excluded already-passing selectors. The matching release-holder turns only recorded each checkpoint and its limits. This strengthens the distinction between useful iterative test repair and coordination churn. The source sequence should continue through the same holder, but its checkpoint stream should stay local until one consolidated readiness boundary. Counting receipt turns as a separate ratio makes improvement measurable without pressuring the producer to combine unsafe or unrelated tests.

The next shared-holder supervision wave amplified its own queue, 05:39-05:46 UTC snapshot: assignment `asg_2beffc49` escalated to bare-Main high-effort decision turn `127560`, which remained running when three more assignment probes reached `s_0cd749fc` as turns `127564`, `127565` and queued `127567` within 33 seconds. An unrelated substrate fallback entered the same ordered holder queue as `127568`, and a second bare-Main high-effort decision was already queued as `127570`. The first completed holder probe spent 37.6 seconds and scheduled a later obligation-scoped continuation rather than reporting changed evidence. This is confirmed C6 queue amplification even before the remaining turn costs and rulings are known: per-assignment timers can fill both the shared holder lane and stopped Main faster than either lane disposes them, while unrelated dependency traffic waits behind the burst. Preserve each assignment's identity, but batch the due-window evidence read into one holder turn and produce authoritative per-assignment outcomes. Admit unrelated fallback traffic between batches or by priority, and allow at most one unresolved Main decision for the holder at a time. Update this snapshot with terminal costs, successor identities and any useful changed evidence after the wave drains.

Early terminal results from that wave confirm both high cost and corrupt receipts, 05:45-05:47 UTC: holder probes `127564`, `127565` and `127567` consumed 37.6, 27.1 and 21.6 low-effort seconds, each scheduling a distinct later dependency check without changed evidence. Interleaved fallback `127568` consumed another 38.8 seconds and renewed its unchanged authority wait. Bare-Main decision `127560` consumed 416.6 high-effort seconds after its first idempotent ruling call hit an approval-review timeout; its one retry succeeded. Decision `127570` then consumed 17.3 seconds. Both ruled `continue` usefully, but again reported their temporary one-day deadline rows, `w_a26e7651` and `w_a302ba89`, as pending. Direct state shows both canceled. The actual successors are `w_fcd29527`, due 06:46, and `w_9261f0c0`, due 06:16. The four completed holder turns plus two Main decisions therefore cost 559.0 model-execution seconds before the remaining holder probes drained. This is the same C5 receipt defect plus C6 fanout, with approval-review latency dominating one decision. Idempotent mutation protected effects, but it did not preserve or return a trustworthy receipt. The ruling path must durably retain a pending operation/result across approval timeout and render the surviving successor from that committed result, without another model turn reconstructing the action.

The wave then drained without useful evidence, 05:47-05:48 UTC: final holder probes `127571` and `127572` consumed 25.4 and 21.7 low-effort seconds and scheduled separate 08:07 and 08:08 checks. Across the five assignment probes and one interleaved fallback, the shared holder spent 172.2 model-execution seconds. Adding the two Main decisions brings the bounded wave total to 606.1 seconds. Every result was timer maintenance or an unchanged authority check; no source, test, review, release disposition or product evidence moved. The unrelated fallback did not lose its distinct condition, so this episode supports batched reading with per-obligation output rather than holder-level false deduplication. It also gives the batching detector a concrete success baseline: the same six outcomes should require one holder evidence snapshot, six explicit mapped dispositions and no Main model turn for obligations already covered by a bounded scope-matched continuation.

One supposedly scoped successor lacked the assignment identity needed for supervision credit, 05:45-06:00 UTC: holder turn `127564` said it scheduled `w_5d973cc9` for 08:05 to check the bundle composition allocation. Direct state shows the wake is due at 07:45, twenty minutes earlier than reported, and its `assignmentId` is null even though the turn was an effort check for `asg_18a7cde5`. At 06:00 the assignment-bound supervision row fired again, declared zero effect and raised `dr_368732a9` into a high-effort bare-Main turn despite that pending manual continuation. This is C5 receipt corruption plus C3/C6 provenance loss. The manual wake's prose names the obligation, but the scheduler cannot safely infer assignment identity from text. Any continuation created to satisfy an effort probe must carry the exact assignment ID and return its stored due time in the receipt. Supervision may then credit it only when its condition scope and deadline satisfy the assignment, avoiding both false holder-level deduplication and needless escalation.

The missing provenance produced a five-decision echo fifteen minutes later, 06:00-06:02 UTC: bare Main consumed high-effort turns `127585`, `127586`, `127587`, `127589` and `127590` for the five holder obligations that had scheduled assignment-free manual continuations at 05:45-05:48. They cost 37.8, 18.5, 17.0, 17.7 and 17.0 seconds, 108.0 seconds total, and all ruled `continue` without changed product evidence. Every response again named its temporary one-day deadline wake as pending. Direct rows show all five reported wakes canceled and different assignment-bound successors surviving for 06:30-06:32. This proves a two-stage churn mechanism: an effort probe causes a manual wake whose missing assignment ID cannot earn liveness credit, then the next controller generation escalates to Main and creates the assignment-bound wake that should have existed in the first place. Fix continuation provenance at creation and return the committed successor ID atomically; batching Main decisions alone would reduce shared reasoning but leave the underlying false escalation intact.

Patrol scan 42 corrected a false cessation hypothesis without intervention, 05:51-05:52 UTC: turn `127576` consumed 116.0 high-effort seconds and found all six strand successors pending on one roughly two-hour cycle, due from 07:52 through 08:08. Scan 41 had observed no pending timed strand rows and suspected the polling fan might have ended; exact later rows show variable rearm lag of about 32 to 65 minutes instead. The patrol correctly retracted that inference, counted no automatic effort probe as a strand fire and sent no prod or recovery prompt. It classified `asg_2beffc49` as one definite unchanged fired window based on the actual holder outcome, while retaining zero empty opportunities because no strand was due in the measured interval. This is useful detector correction, not a stall or churn remedy. Future scans must infer cessation only from durable terminal state or an expired bounded rearm contract, never from historical lag, and should avoid spending a full model turn merely to discover whether rows appeared when a cheap row predicate can establish that fact.

Stopped-root audit separates deliberate holds from lost continuations, 05:08 UTC: four open assignments are held by bare Main. Retirement-lifecycle card `asg_5ca2d116` has no pending wake, but current decision row `dr_aac1e768` explicitly says to defer; this is a deliberate product hold, not S4. Provisioning parent `asg_643f3313` has a bounded 17:01 wake and correctly preserves one changes-requested review dependency. Fleet-doc parent `asg_e4397429` has no wake and points to acceptance card `asg_46259897`, which is open under stop-listed release holder `s_7169b2a6` with no pending continuation after six September 8 no-filing turns; that is a stopped-lineage stall, not authority to resume it. Credential-containment card `asg_fd786ed8` also has no live continuation: its latest note claimed `dr_94e30b85` and `dr_a6c8ce57` exposed no selected values, but current durable rows show concrete rulings, “No in-product reveal” and “Revoke-only v1.” Its cited wake fired on 28 August and was never replaced. This is a stale-projection stall, but formal contact stops and retained product scope forbid external revival. A detector must compare the latest note's claimed blocker with current decision rows and wake state, then route only to an authorized non-stopped owner or report the held lineage; it must not infer that stale notes remain true or treat a stopped owner as executable custody.

Resolved architecture still triggered its old fallback, 05:08-05:10 UTC: a fallback created before Mike's narrow SQLite decision woke `s_0cd749fc` to look for the missing amendment. Turn `127535` consumed 53.8 low-effort seconds, discovered existing `att_bdb1762e`, recorded another artifact `art_188f2a21` and correctly did not resume work or contact stopped sessions. The result was safe but redundant because the awaited disposition already existed and the sole integrator had consumed it. A condition-bound architecture wait should cancel or fire when its exact disposition is recorded; a plain fallback should perform a cheap durable predicate before model dispatch and self-dispose when the condition is already satisfied. Immediately afterward the offset sixth shared-holder assignment generated another 18.8-second high-effort Main decision, again reporting a canceled one-day wake `w_4b4f1ef2` instead of surviving 30-minute successor `w_cad9ae92`. This extends the same supervision-fanout and false-receipt defect to all six obligations; it is not a new independent class or evidence that the holder failed to continue.

Stopped owner self-rearmed an unchanged fallback, 05:13-05:14 UTC: pre-existing eight-hour wake `w_77801622` fired into formally stopped Visitor Identity recovery owner `s_7fa87968`. Turn `127539` consumed 33.9 low-effort seconds, found no attribution input or custody exception, made no source or review change, then created another eight-hour wake `w_3532c4b2` to the same stopped session. The prompt correctly preserved revoked custody and prohibited reopening, but safe no-op behavior does not satisfy the stop. This is recurring C6 attention burn plus a stop-enforcement failure: a formal stop must suppress future self-wake delivery and creation, not only implementation effects. Pending wakes owned by a newly stopped session need a machine-readable fence or authorized cancellation path; if a fallback still fires, it must terminate without rearming unless the stop has been explicitly lifted. External observers must not wake the stopped owner to correct its own timer.

A different stale Visitor continuation self-terminated correctly, 05:53 UTC: fallback turn `127579` consumed 29.4 low-effort seconds, reconciled its premise against the parent record and found that the old assignment had been revoked with draft and evidence accountability accepted elsewhere. No minimal attribution brief had arrived. The holder ended the obsolete continuation without renewal and made no source, test, review, cleanup or reopening action. The prompt still cost one model turn because cancellation did not occur when the parent state changed, so count it as contained C5/C6. Its execution-time reconciliation is the desired last defense: stale work should inspect current authority and stop without recreating itself. Event-bound cancellation at the parent revocation would have avoided the remaining 29.4-second cost.

### Owner answer without a delivered handoff

A bounded read on 8 September confirms REST owner turn `125109` answered the B2 executor at 08:45 with a concrete disposition: command-local Git fixture identity, a locked offline CLI build, then the same gate. The lead read that assistant response directly. A read-only wake query for that owner and executor from 08:43 through 09:10 found the earlier Rebar/cache disposition and two later reviewer-recovery notices, but no wake carrying the 08:45 fixture/CLI decision. This establishes a missing supported handoff in that interval, not absence of every conceivable communication or failure of the test itself. Kestrel requested the existing owner deliver its already-made decision in turn `125251`; that request is not proof the executor received or acted on it.

This differs from a queued but delayed wake: the decision existed in the owner's conversation, while the requesting actor lacked the corresponding delivery. Preserve this case when comparing internal follow-through after installation. Record the decision time, actual notification time, useful resumed execution and whether an external agent had to connect those steps. Do not score an owner answer as fulfilled delegation merely because its turn ended successfully.

### Comparison at the authorized installation-plus-seven-days review

Conduct the follow-up on schedule even if the evidence is sparse. For each hypothesis record actual installed software, loaded guidance/rules and affected agent exposure. A week after installation may contain less than a week of relevant use. Compare similar asks and failures, include unresolved cases, and keep the following questions in the external report:

1. Did the DB-timeout, terminal-finalize, child-restart, full-process exit or per-session queued/no-running signatures recur? Record the observed sequence and affected outcomes; do not infer the same root cause merely from the same shutdown string.
2. How long did safe recovery take? Distinguish first failure, detection, first useful resumed work, reconciliation of unknown effects and fulfillment of the affected obligation. Report missing timestamps and still-open intervals. Service uptime or a running turn is not the completion timestamp.
3. Were unknown outcomes preserved and reconciled before unsafe replay? Inspect actual external effects, authorized repair lineage and late callbacks. Count observed duplicate effects separately from duplicate messages, proposed staffing and prevented races.
4. Did internal POs and accountable agents restore capable ownership and carry results forward without external intervention? Record each external diagnostic, routing, succession, design or verification intervention that was needed. Include decisions answered locally but not delivered to the actor who must execute them. Fewer interventions only supports improvement if work still completes safely; unattended stalled work is not success.
5. Did the revised guidance, rails and actual served projections agree? Recheck current spirit opportunity, proportionate small fixes, honest early review, evidence applicability and the canonical-runner case. Keep actual product/credential/publication holds distinct from stale workflow instructions.
6. Did reminders become less repetitive while preserving attention to distinct obligations, changed consequences and delivery failures? Compare successful continuation and missed work alongside notification volume. Fewer notices after removing a gate or during a dead delivery path do not establish success.
7. Did recovery avoid the commissioning and start races observed here? Examine pending future-start instructions as well as live processes. Record whether the system made ownership and cancellation effective soon enough to prevent duplicate work, including mistakes by external supervisors.

The evaluation records a dated supported, mixed, contradicted or inconclusive judgment with its next responsible action. It can finish without declaring the treatment successful. Persistent defects and unmet delivery commitments retain owners. These observations inform the next design; they do not automatically change rails or waive release acceptance. No new metrics service, per-turn report, release/install action or follow-up wake is created by this baseline section.

## Example: the posture design decision

Problem: agents have put small understood bugs through full specification/review cycles. Light/heavy posture was introduced to prevent this. The current predicates accept either token, while guidance defaults uncertainty to heavy.

Hypothesis: making proportionate execution an explicit orchestrator responsibility, removing that default and preserving the direct small-fix path can reduce unnecessary process without increasing substantive defects. The selected design removes both token gates with the composed guidance changes. Exercise the contrasting acceptance cases before release; retain actual outcomes for later external assessment.

Compare an understood checkout-depth CI correction, a one-line authorization change and work with actual architectural uncertainty. Record the chosen work, why extra process was useful where invoked, time to usable outcome, failures/rework and the independent assessment of adequacy. Do not classify the authorization change as light merely because its diff is small, or the CI correction as heavy merely because it touches infrastructure.

A first assessment might be mixed: routine repairs became simpler, but uncertain tasks were expanded too slowly. That would justify refining escalation of consequential uncertainty, not declaring the entire philosophy false or reinstating every old gate. This is an illustrative possible finding, not observed data. The actual assessment must cite real cases and preserve contrary findings.

## Relationship to existing self-tuning work

`self-tuning-rails-core-FUTURE.md` sections 6e-6g already discuss versioned guidance, production/user evidence and remedy rates. Retain the link between changes and outcomes, and the requirement for evidence beyond agents agreeing with one another. A first implementation of this learning practice does not require that future autonomous tuning system.

Do not use falling remedy rate as the definition of success. Removing a rail mechanically removes its remedies; agents can also avoid a troublesome action altogether. Less enforcement activity may mean improvement, missing protection or stalled work. Read it alongside comparable outcomes, process cost, lost attention and real defects. No metric automatically rewrites policy or grants implementation authority.

## Next concrete step

Use these entries as the initial 0.1.9 change ledger. Link implementing commits and pre-release acceptance evidence as delivery proceeds, then record actual exposure. Arrange an external evaluation owner and an internal owner for responding to findings. Capture a representative baseline where possible. Missing baseline evidence limits later attribution; it does not excuse missing release verification. The next review recommends a small set of justified changes and links them to the prior rationale and evidence.


## Review lineage

Rowan's `d2c8b26f-8778-4a55-ad68-fd9fbed6b656` clarified bounded evaluation completion, treatment-specific exposure and actual PO influence. Orchestrator Editor's `005d6086-3103-4b62-8b40-31f139ce3f5a` clarified delegation, exposure groups and separation of fulfilled delivery from later evaluation. Mike then challenged the internal-evaluation premise; the lead revised the proposal to external primary forensics, retaining useful internal support and product accountability. The earlier internal-owner model is superseded.

The orchestration contribution recommends selecting removal of the two posture-token vetoes together with the positive small-fix path, with underscoping consequential uncertainty as the counter-risk. The lead selected that composed approach after Mike challenged the earlier loss of purpose. This records the release design choice and its rationale, not empirical proof or implementation authority.

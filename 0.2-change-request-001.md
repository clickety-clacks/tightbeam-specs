# 0.2 Change Request 001 — 2026-08-13 rulings integration

Status: AUTHORIZED (Mike, 2026-08-13). Executor: the 0.2 orchestrator, as
folding authority. Sources: `emergent-formalization-candidates-2026-08-13.md`
(all sections including razor, tiers, naming, staffing) and program doc §7.
Execute as one fold sitting AFTER the in-flight Phase 1 landing completes.
Record each item's landing place in the program doc, same-breath commits.
Do not acknowledge this document; the fold's ledger entry shows compliance.

## A. Immediate, communications-level (no code, effective now)
- The no-acknowledgment law governs all program communications: directives
  never request acknowledgment turns; receipt is proven by the fired wake
  and the next filing.

## B. In-flight impact: NONE
Phase 1 seams proceed unchanged to landing. No rework, no scope additions.
Typed specimen attests and typed blocker rows are NEXT-iteration seam
candidates, not amendments to the current build.

## C. Fabric spec fold (r5)
1. Naming: the §5 behaviour renames avasarala → `prodder` (the deterministic
   starvation floor keeps the legacy name; bones get boring names). The name
   AVASARALA moves to the triage ROLE. Record the rule: name the minds, keep
   the machinery boring.
2. New anatomy section: Avasarala, the triage role — charter template,
   casebook as data (entries MUST cite evidence rows and record hit counts),
   adjudicate-before-escalate, provisional class minting, ring-once-per-
   class. Seed content ships EMPTY. The 0.1 stall patrol is the reference
   implementation.
3. Phase 4 rewrite: build the nurse contract over the bare watermark; the
   prodder floor stays bone beneath her; unbraid per original plan.
4. Phase 5+ rewrite: formalize Miller as a seed playbook (tenet-audit
   discipline; three duties: conformance, effect evidence, promotion/
   demotion scouting with the shipped-vs-emergent razor as rubric; probe
   list ships EMPTY — the program's C1-E8 stay local).
5. §7 contract additions: `fyi` never obliges a reply turn; external-
   notification dedup keyed on blockage identity (one doorbell per
   blockage; the row is the record).
6. Promotion pipeline section: witnesses file specimens → Avasarala mints →
   Miller recommends with risk tiers → tier 1 auto-executes with notice,
   tier 2 default-executes at deadline, tier 3 requires the owner's word.
   The owner's station is veto-with-context.
7. First-boot posture: prodder always on, escalating to Main; Avasarala and
   Miller seeded as archetypes, unstaffed; staffing triggers live in seed
   guidance. The substrate never staffs.

## D. Workstream 2-3 queue (author when Phase 2 runs)
Seed guidance items: specimen discipline + vocabulary; the no-ack law;
verify-then-relay for authority messages; the anti-busywork test ("does this
filing record work or manufacture it"); the security-incident playbook
(quarantine-of-authority as convention, never mechanism); staffing triggers
for Avasarala and Miller.

## E. Program doc upkeep
Reflect C and D in the phases and workstreams; note CR-001 executed in the
build ledger. The election ledgers are unaffected.
